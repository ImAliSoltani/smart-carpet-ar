# نقشه‌ی راه — فروشگاه اینترنتی اختصاصی فرش

> سند برنامه‌ریزی و مرجع اصلی پروژه. آخرین به‌روزرسانی: ۱۴۰۵/۰۶/۰۱ — **لایه‌ی حرکت بسته شد و با آن فاز ۳ تمام شد.** هر چهار شرط پذیرشِ جهت بصری ساخته شده‌اند؛ ترنزیشن اشتراکی که سه هفته «سیم‌کشی‌شده ولی نامرئی» بود، با یک اعلان CSS حل شد. پیش از آن: ارزیابی، سخت‌سازی امنیتی، تست E2E و **CI سبز** (بازیابی بصری ۱۷٫۵٪ → ۴۵٫۸٪ رتبه‌ی اول). فایل‌های استقرار نوشته شدند ولی **اجرا نشده‌اند** — VPS و Docker هیچ‌کدام در دسترس نبودند.
>
> **ددلاین: ۱۰ شهریور ۱۴۰۵ (۲۰۲۶/۰۹/۰۱).** کارفرما در ۱۴۰۵/۰۶/۰۱ تعیین کرد و جایگزین ۲۸ مرداد شد. بند ۱۱ سر جایش است: **هیچ قابلیتی به‌خاطر تقویم حذف نمی‌شود.**
>
> **بند ۱۴ بسته شد (۱۴۰۵/۰۶/۰۲):** هر هفت باگ و کارِ گزارش‌شده‌ی کارفرما انجام شد — از جمله دو موردی که خریدِ تأییدنشده ثبت می‌کردند و صفحه را پس از لاک گوشی قفل نگه می‌داشتند، و **۲۰ فرشِ کاتالوگ زنده که در AR تکه‌ای از خودشان را کل خودشان نشان می‌دادند.** جزئیات هرکدام در بند ۱۴.
>
> **بند ۱۶ بسته شد (۱۴۰۵/۰۶/۰۳):** فهرست سوم کارفرما، هفت مورد — از جمله **ویرایشگر گوشه که اصلاحِ ادمین را روی صفحه دور می‌ریخت** در حالی که فایل‌های ساخته‌شده نگهش داشته بودند، و **جست‌وجوی نام که قابلیتش از اول بود و دیده نمی‌شد**. دو موردِ AR و تایپوگرافی هر دو با **اندازه‌گیری** بسته شدند نه با سلیقه: فرش با نصفِ روشناییِ عکس خودش رندر می‌شد، و `tabular-nums` رقم‌های فارسی را ۷۲٪ پهن می‌کرد. جزئیات در بند ۱۶.
>
> **باقی‌مانده:** فاز ۶ (پایان‌نامه) بزرگ‌ترین تکه است؛ سه قلم فاز ۵ به VPS و گوشی و Docker گره خورده‌اند؛ دو تأیید روی گوشی واقعی از بند ۱۴؛ و **تأیید روشناییِ تازه‌ی AR روی گوشی** از بند ۱۶.

---

## ۱. تعریف محصول

یک **فروشگاه اینترنتی کامل و حرفه‌ای فرش** — از جنس محصولی که یک کسب‌وکار واقعی سفارش می‌دهد؛ نه دمو، نه نیمه‌کاره. کاربر وارد سایت می‌شود، بین ده‌ها فرش می‌گردد، فیلتر می‌کند، جزئیات می‌بیند، و برای **هر فرش** می‌تواند:

- آن را با **مقیاس واقعی** روی کف خانه‌ی خودش ببیند (AR)،
- شبیهش را با یک عکس پیدا کند (جست‌وجوی بصری)،
- از سیستم بپرسد چه فرشی به اتاقش می‌آید (مشاور چیدمان)،
- اندازه‌ی مناسب کفش را بگیرد (راهنمای اندازه)،
- و به زبان آدمیزاد جست‌وجو کند (جست‌وجوی گفتگویی).

سپس سبد می‌بندد و سفارش ثبت می‌کند. فروشنده هم از پنل خودش با **یک عکس معمولی** فرش جدید اضافه می‌کند و همه‌چیز (امبدینگ جست‌وجو، فایل‌های AR با ابعاد دقیق) خودکار ساخته می‌شود.

سقف زمانی بیرونی: **۲۸ مرداد ۱۴۰۵ / ۲۰۲۶-۰۸-۱۹** — محدودیت تحویل است، نه بودجه‌ی کیفیت؛ دامنه به‌خاطر آن کوچک نمی‌شود.

## ۲. دو ستون هم‌وزن

**ستون الف — تجربه‌ی فروشگاه:** طراحی بصری، معماری اطلاعات، جریان خرید، RTL فارسی، موبایل، سرعت، دسترس‌پذیری. کاربر باید حس کند وارد یک فروشگاه تمام‌شده شده است.

**ستون ب — عمق فنی و هوش:** مقیاس واقعی AR، تولید خودکار دارایی از یک عکس، جست‌وجوی بصری، درک صحنه و اندازه.

ضعف هرکدام کل محصول را پایین می‌آورد.

## ۳. استاندارد کیفیت (قابل‌سنجش)

1. **مقیاس واقعی AR:** خطای ابعاد < ~۳٪ با تأیید متر. baseline: نمونه‌ی تجاری بررسی‌شده (فرش «۳×۴» را ۰٫۲×۰٫۲ متر رندر می‌کند).
2. **تولید تمام‌خودکار دارایی AR از یک عکس** — صفر کار دستی، برای هر سایز فروشیِ همان فرش.
3. بافت AR حداقل 2K.
4. هر قابلیت هوشمند **روی ورودی دیده‌نشده** کار کند و در نوت‌بوک ارزیابی عدد داشته باشد.
5. فروشگاه: RTL بی‌عیب، موبایل‌اول، کنتراست ≥۴٫۵:۱، هدف لمسی ≥۴۴px، تصاویر WebP/AVIF چندسایزه با lazy-load و بدون پرش چیدمان، حالت‌های خالی/بارگذاری/خطا در همه‌ی صفحات، اعداد و قیمت‌ها با قالب فارسی.
   - **حرکت، در دو رده‌ی جدا (بازنگری ۱۴۰۵/۰۵/۱۲):** بازخورد وضعیت (hover، فشردن، باز و بسته شدن) **۱۵۰–۳۰۰ms**؛ ورود و گذار (ورود پلکانی، پرده، ترنزیشن بین صفحه‌ها) **۴۰۰–۹۰۰ms**. تفکیک از آن‌جا آمد که سقف ۳۰۰ms برای همه‌ی حرکت‌ها با جهت بصری انتخاب‌شده در تضاد بود: چیدمان عمداً آرام است و جبرانش بر عهده‌ی حرکت است. احترام به `prefers-reduced-motion` در هر دو رده الزامی است و بی‌استثنا.
6. محصول مستقر با HTTPS + پنل ادمین امن + تست خودکار + CI سبز.

## ۴. استک و تصمیم‌های معماری (صریح، تا بعداً بحث نشود)

| موضوع | تصمیم | چرا |
|---|---|---|
| کلاینت | Next.js + TypeScript + Tailwind، PWA (منیفست + آیکون + قابل نصب) | بهترین اکوسیستم UI؛ SSR برای سرعت و SEO |
| وضعیت کلاینت | TanStack Query (سرور) + Zustand (سبد/علاقه‌مندی، ماندگار در localStorage) | ساده، استاندارد، بدون بویلرپلیت |
| فرم‌ها | react-hook-form + zod (اعتبارسنجی هم‌قواره با بک‌اند) | خطاهای فوری کنار فیلد |
| AR | تولید `.glb`/`.usdz` اختصاصی به‌ازای هر سایز + لایه‌ی جای‌گذاری اختصاصی WebXR/three.js + `<model-viewer>` و Scene Viewer پشتیبان + Quick Look برای iOS | تنها راه مقیاس واقعی + تجربه‌ی مخصوص جسم تخت |
| بک‌اند | FastAPI (Python 3.12) + SQLAlchemy async + Alembic | اکوسیستم AI پایتون |
| دیتابیس | PostgreSQL + pgvector (ایندکس HNSW) | جست‌وجوی برداری در همان دیتابیس، بدون سرویس اضافه |
| امبدینگ | DINOv2 ViT-B/14 (۷۶۸بعدی) — پیش‌محاسبه برای کاتالوگ، محاسبه‌ی زنده فقط برای عکس کوئری | روی الگو/بافت قوی‌تر از CLIP؛ روی CPU سرور هم سریع |
| کارهای پس‌زمینه (امبدینگ، ساخت AR، مشتقات تصویر) | صف داخلی asyncio + فیلد وضعیت در دیتابیس (`processing/ready/failed`) — بدون Celery/Redis | مقیاس یک فروشگاه؛ سادگی = پایداری. محدودیت در گزارش ذکر می‌شود |
| تصاویر | در آپلود: تبدیل به WebP در چند سایز (thumb/card/full/texture-2K) | سرعت لیست‌ها؛ بافت AR جدا |
| ذخیره‌ساز فایل | توسعه: دیسک محلی با همان قرارداد URL؛ استقرار: MinIO | جابه‌جایی بدون تغییر کد |
| احراز هویت ادمین | یک کاربر ادمین، رمز در `.env`، سشن‌کوکی HttpOnly/SameSite + محدودیت نرخ ورود | سطح حمله‌ی کوچک، امن و ساده |
| حساب خریدار | **بدون ثبت‌نام** — خرید مهمان با نام/تلفن/آدرس + پیگیری سفارش با کد رهگیری | الگوی رایج فروشگاه‌های کوچک ایرانی؛ اصطکاک کمتر |
| پرداخت | تا «ثبت سفارش» واقعی؛ درگاه شبیه‌سازی‌شده | درگاه واقعی شخصیت حقوقی می‌خواهد؛ در گزارش شفاف می‌آید |
| جست‌وجوی متنی | ILIKE + pg_trgm روی نام/توضیح (فارسی) | ساده و کافی؛ گفتگویی جدای آن است |
| مدل زبانی گفتگویی | تصمیم باز: API ابری در برابر مدل محلی کوچک — فاز ۴ | وابسته به VPS و دسترسی |
| استقرار | Docker Compose (فرانت standalone + بک‌اند + pg + MinIO) پشت Caddy (HTTPS خودکار) روی VPS + دامنه | کم‌ترین قطعات متحرک |
| پشتیبان‌گیری | اسکریپت شبانه‌ی `pg_dump` + همگام‌سازی پوشه‌ی فایل‌ها | حداقلِ حرفه‌ای |
| زبان | فقط فارسی (بدون کتابخانه‌ی i18n) + ابزار قالب اعداد/قیمت/تاریخ فارسی | تمرکز؛ افزودن زبان بعداً ممکن |

## ۵. معماری

```
┌─────────────────────────────────────────────────────┐
│  فروشگاه — Next.js PWA                                  │
│  خانه · لیست+فیلتر+جست‌وجوی متنی · صفحه‌ی محصول          │
│  (گالری/سایزها/AR/مشابه‌ها) · جست‌وجوی بصری · مشاور       │
│  چیدمان · راهنمای اندازه · گفتگویی · سبد/ثبت سفارش ·      │
│  پیگیری سفارش · علاقه‌مندی/مقایسه · درباره/تماس/سؤالات     │
└──────────────────────┬──────────────────────────────┘
                       │ REST API (/api/v1, OpenAPI)
┌──────────────────────┴──────────────────────────────┐
│  بک‌اند — FastAPI                                       │
│  کاتالوگ/فیلتر/جست‌وجو · سفارش‌ها · پنل ادمین (auth)       │
│  ┌─ پایپ‌لاین محصول جدید (پس‌زمینه) ──────────────────┐   │
│  │ عکس → مشتقات WebP → امبدینگ DINOv2 → pgvector     │   │
│  │      → تصحیح پرسپکتیو → glb/usdz به‌ازای هر سایز    │   │
│  └───────────────────────────────────────────────┘   │
│  جست‌وجوی بصری · درک صحنه (کف/دیوار/رنگ/سبک)            │
│  تطبیق اندازه (A4 + عمق) · گفتگویی (LLM → فیلتر)         │
└────────┬──────────────────────────┬─────────────────┘
   ┌─────┴─────┐              ┌─────┴─────┐
   │ Postgres  │              │ ذخیره‌ساز   │
   │ +pgvector │              │ عکس/AR     │
   └───────────┘              └───────────┘
```

## ۶. دامنه‌ی کامل فروشگاه (فهرست بسته — «تمام» یعنی همین)

**خریدار:**
1. **خانه** — معرفی فروشگاه، دسته‌ها، منتخب‌ها، تازه‌ها، ورودی قابلیت‌های هوشمند.
2. **لیست محصولات** — فیلتر: اندازه، رنگ، طرح، جنس، بازه‌ی قیمت، نوع اتاق؛ مرتب‌سازی؛ صفحه‌بندی؛ **جست‌وجوی متنی فارسی**.
3. **صفحه‌ی محصول** — گالری با زوم، جدول مشخصات، **انتخاب سایز (هر سایز قیمت/موجودی/AR خودش)**، دکمه‌ی برجسته‌ی «در خانه‌ی من ببین»، **فرش‌های مشابه** (با همان امبدینگ‌ها)، اشتراک‌گذاری.
4. **AR** — جای‌گذاری اختصاصی + راهنمای بار اول + **QR دسکتاپ→گوشی** (کاربر دسکتاپ دکمه‌ی AR را بزند، QR همان فرش را می‌گیرد).
5. **جست‌وجوی بصری** — آپلود/دوربین → مشابه‌ترین‌ها.
6. **مشاور چیدمان** — عکس اتاق → فرش‌های هماهنگ + توضیح چرا.
7. **راهنمای اندازه** — عکس اتاق (+ برگه‌ی A4 برای حالت دقیق) → سایز پیشنهادی.
8. **جست‌وجوی گفتگویی** — «فرش روشن برای اتاق کودک تا فلان قیمت» → نتایج فیلترشده.
9. **سبد و ثبت سفارش** — مهمان، با اعتبارسنجی کامل → **کد رهگیری**.
10. **پیگیری سفارش** — کد رهگیری + تلفن → وضعیت.
11. **علاقه‌مندی‌ها** و **مقایسه‌ی فرش‌ها** (localStorage، بدون حساب).
12. **درباره / تماس / سؤالات متداول** — شامل لینک واتس‌اپ و اینستاگرام (الگوی فروش واقعی ایرانی) و «AR چطور کار می‌کند».
13. **PWA + SEO** — منیفست و آیکون‌ها، متادیتا و OpenGraph هر محصول، sitemap.

**فروشنده (پنل ادمین، پشت ورود):**
14. **داشبورد** — شمارنده‌ها و سفارش‌های اخیر.
15. **مدیریت فرش‌ها** — CRUD کامل + سایزها + آپلود چندعکس با مرتب‌سازی و انتخاب عکس اصلی.
16. **بازبینی AR** — پیش‌نمایش تصحیح پرسپکتیو با **گوشه‌های قابل جابه‌جایی**، وضعیت ساخت دارایی‌ها، دکمه‌ی «دوباره بساز».
17. **مدیریت سفارش‌ها** — لیست، جزئیات، تغییر وضعیت (در انتظار/تأیید/لغو).

**خارج از دامنه (آگاهانه، نه از قلم‌افتاده):** درگاه پرداخت واقعی، حساب کاربری خریدار، چندفروشگاهی، اپ نیتیو استور، بلاگ/CMS، چندزبانه، سیستم تخفیف/کوپن، نظرات کاربران.

## ۷. برنامه‌ی داده

- **فرش‌های قهرمان (۵–۱۰):** فرش‌های واقعی در دسترس، عکس‌برداری اختصاصی + ابعاد مترشده. دموی AR دفاع با این‌ها.
- **حجم کاتالوگ (۵۰–۱۰۰):** دیتاست‌های عمومی فرش ایرانی (Kaggle / HuggingFace)؛ متادیتای واقع‌گرایانه‌ی تولیدی، شفاف در گزارش.
- در ingest: استخراج رنگ‌های غالب، ساخت مشتقات تصویر، امبدینگ — با همان پایپ‌لاین محصول جدید (یعنی پایپ‌لاین از روز اول با داده‌ی واقعی تمرین می‌شود).
- **وضعیت (۱۴۰۵/۰۵/۰۹):** ۴۴ فرش با متادیتای نوشته‌شده (نام، توضیح، جنس، شهر، ۱۲۲ سایز فروشی با قیمت از ۴٫۷ تا ۲۴۷٫۶ میلیون تومان) وارد دیتابیس محلی شدند. محیط توسعه‌ی محلی: PostgreSQL 18 + pgvector داخل WSL؛ آغاز هر جلسه با `scripts/dev-db.ps1`. برای نخستین بار همه‌ی آزمون‌ها — از جمله آزمون‌های یکپارچه‌ی دیتابیسی — به‌صورت محلی سبز می‌شوند.
- **کاتالوگ نمایشی (۱۴۰۵/۰۵/۰۹):** برای آنکه فروشگاه در جلسه‌ی دفاع از نظر تعداد محصول پر به نظر برسد، ۲۹ فرش ماشینی از یک فروشگاه اینترنتی داخلی به‌عنوان داده‌ی نمایشی افزوده شد (مجموع کاتالوگ: ۷۳ محصول، ۲۶۳ سایز). فقط محصولاتی برداشته شدند که نمای رو‌به‌روی تخت با تفکیک‌پذیری بالای ۱۲۰۰ پیکسل داشتند تا پایپ‌لاین AR رویشان کار کند؛ ۱۱ محصول به‌دلیل کیفیت پایین تصویر و ۵۳ تصویر گالری به‌دلیل داشتن بنر تبلیغاتی، برچسب کارخانه یا نشان فروشگاه کنار گذاشته شدند.
  - در فروشگاه هیچ تفاوتی با بقیه‌ی محصولات ندارند — همان جدول، همان پایپ‌لاین، بدون نشان یا استثنای ویژه. شناسه‌شان پیشوند `dk-` دارد تا هنگام جایگزینی با موجودی واقعی فروشنده با یک پرس‌وجو جدا شوند.
  - **الزام فاز ۶ (پایان‌نامه):** در فصل داده صریحاً نوشته می‌شود که تصاویر و مشخصات کاتالوگ از دو منبع عمومی تهیه شده‌اند — دیتاست فرش ایرانی و یک فروشگاه اینترنتی داخلی — و این کاتالوگ برای ارزیابی و نمایش سامانه است نه فروش.
  - عکس‌های گالری در `data/catalog-demo/covers/` نگهداری می‌شوند و امبدینگ نمی‌گیرند تا وارد نتایج جست‌وجوی بصری نشوند؛ در فاز ۳ در گالری صفحه‌ی محصول استفاده می‌شوند.
- **شمارش واقعی دیتابیس (اندازه‌گیری‌شده ۱۴۰۵/۰۵/۱۳): ۷۰ فرش، ۲۵۴ سایز — همه فعال.** عدد ۷۳/۲۶۳ بالا مربوط به لحظه‌ی ingest است؛ اختلاف سه محصول هنوز ردیابی نشده. پیش از ثبت اعداد در فصل داده‌ی پایان‌نامه باید علتش روشن شود.
- ~~**یافته‌ی ارزیابی (نیازمند رسیدگی پیش از فاز ۴):** امبدینگ DINOv2 ساختار نقشه را بسیار خوب می‌گیرد (لچک‌ترنج در برابر افشان)، اما به رنگ کم‌حساس است؛ برای فرش سبز، فرش‌های قرمزِ هم‌ساختار را نزدیک می‌داند.~~ **رسیدگی شد (۱۴۰۵/۰۵/۲۳) — اولین کار فاز ۴.** توصیفگر رنگ ساخته شد (هیستوگرام HSV، ۴۲ سطل: ۱۲ فام × ۳ روشنایی + ۶ سطل بی‌رنگ، در `app/services/color.py`) و `search_by_embedding` حالا دومرحله‌ای است: مرحله‌ی اول HNSW روی امبدینگ با over-fetch هشت‌برابر، مرحله‌ی دوم رتبه‌بندی مجدد با ترکیب وزنی ۰٫۷ ساختار + ۰٫۳ رنگ. **عدد ارزیابی همچنان کار فاز ۵ است** (Precision@k در برابر رتبه‌بندی تک‌مرحله‌ای)؛ آنچه اینجا ثبت می‌شود مشاهده است نه عدد: برای فرش سرمه‌ای، از ۸ نتیجه‌ی اول تنها ۱ نتیجه سرمه‌ای بود و حالا دو نتیجه‌ی نخست سرمه‌ای‌اند.
  - **درس تنظیم آستانه‌ها:** سه بار تنظیم لازم شد و هر سه بار خطا با **نگاه کردن** به کانتکت‌شیت ۷۰ فرش پیدا شد، نه از روی کد یا آمار. دو خطای اصلی: (۱) باند «کرم» خیلی تنگ بود و ۱۲ فرش با زمینه‌ی نخودی زیر «نارنجی» می‌رفتند؛ (۲) کف گذاشتن روی **value** برای تشخیص بی‌رنگی، زمینه‌ی سرمه‌ای را «مشکی» حساب می‌کرد و فرش را به رنگ حاشیه‌اش می‌سپرد — شش فرش سرمه‌ای زیر قرمز/قهوه‌ای/کرم افتاده بودند. تاریکی یعنی نبودِ نور، نه نبودِ رنگ؛ تنها بی‌اشباعی یعنی نبودِ رنگ.
  - **تأیید مستقل:** فیلتر `color=blue&color=turquoise` یازده فرش می‌دهد که اسلاگ‌هایشان `sormei`، `firoozei` و `nafti` است — نامی که دستی و جدا از این پایپ‌لاین نوشته شده بود.

## ۸. فازها (تعریف با معیار اتمام — نه تاریخ)

> هیچ قابلیتی به‌خاطر تقویم حذف یا «اختیاری» نمی‌شود. کار وقتی تمام است که معیارش برآورده باشد.

### فاز ۰ — پایه + اسپایک AR ✅
اسکلت monorepo، CI، ساخت GLB مقیاس‌دار از تصویر، GitHub Pages، تست موفق روی Galaxy S23 FE.

### فاز ۱ — بک‌اند کامل کاتالوگ و سفارش ✅ انجام شد
> معیار اتمام برآورده شد: چرخه‌ی کامل فروشگاه به‌صورت آزمون یکپارچه روی PostgreSQL واقعی در CI اجرا می‌شود (افزودن فرش → فیلتر → جست‌وجوی بصری با عکس دیده‌نشده → ثبت سفارش → پیگیری). کاتالوگ اولیه با ۴۴ فرش گزینش‌شده آماده‌ی ورود است.

مدل داده (فرش/سایز/عکس/سفارش) + مهاجرت‌ها؛ قرارداد کامل `/api/v1`؛ آپلود + مشتقات WebP؛ پایپ‌لاین امبدینگ → pgvector؛ **API جست‌وجوی بصری + مشابه‌ها**؛ جست‌وجوی متنی + فیلترها؛ APIهای سفارش/رهگیری؛ احراز هویت ادمین؛ ingest دیتاست‌ها؛ تست‌های یکپارچه روی Postgres واقعی در CI.
**معیار اتمام:** با `curl`/OpenAPI بشود کل چرخه را زد: افزودن فرش → دیدن در لیست با فیلتر → جست‌وجوی بصری با عکس جدید → ثبت سفارش → رهگیری. همه‌ی تست‌ها سبز.

### فاز ۲ — پایپ‌لاین خودکار AR + تجربه‌ی جای‌گذاری اختصاصی ✅ انجام شد
> یک عکس معمولی، بدون هیچ کار دستی، به فایل‌های `glb` و `usdz` هر اندازه تبدیل می‌شود و آزمون خودکار تطابق ابعاد دو قالب را تضمین می‌کند. لایه‌ی جای‌گذاری اختصاصی روی دستگاه واقعی تأیید شد.
> **تأیید شد (۱۴۰۵/۰۵/۲۰): Quick Look روی آیفون کار می‌کند.** کارفرما روی دستگاه واقعی آزمود و فایل `usdz` باز شد. یعنی اصلاح نوع محتوا (پاراگراف بعد) واقعاً همان خرابی را برداشته بود، و **مسیر iOS دیگر باز نیست**.
> **باقی‌مانده:** فقط اندازه‌گیری خطای مقیاس با متر روی هر دو گوشی — نیازمند حضور کارفرما (بند ۱۲).
> **اصلاح ۱۴۰۵/۰۵/۱۳ — یک خرابی نهفته‌ی iOS برداشته شد.** سرور فایل‌ها را با نوع محتوای `application/octet-stream` می‌فرستاد، چون `mimetypes` پایتون جدولش را از خود ماشین می‌سازد و این ویندوز هیچ‌کدام از `.webp`، `.glb` و `.usdz` را نمی‌شناسد. Quick Look نمایشگر را از روی نوع محتوا انتخاب می‌کند و فایلی را که به‌صورت بایت خام برسد باز نمی‌کند — یعنی دکمه‌ی AR روی آیفون بی‌صدا هیچ کاری نمی‌کرد و در هیچ لاگی هم دیده نمی‌شد. نوع‌ها حالا در `app/main.py` صریح اعلام می‌شوند و آزمون دارند. **تست ۱۴۰۵/۰۵/۲۰ روی آیفون این را بست: همین بود.**

تصحیح پرسپکتیو (خودکار + گوشه‌های دستی ادمین)؛ تولید `.glb` (بافت 2K+، ابعاد از متادیتای هر سایز) و `.usdz`؛ اتصال به «افزودن محصول»؛ **لایه‌ی جای‌گذاری اختصاصی WebXR/three.js**: قفل روی کف، کشیدن تک‌انگشتی، چرخش دوانگشتی، بدون تغییر مقیاس، سایه‌ی تماس نرم، تخمین نور محیط؛ fallback: model-viewer / Scene Viewer / Quick Look؛ راهنمای بار اول + QR دسکتاپ→گوشی.
**معیار اتمام:** فروشنده یک عکس می‌دهد؛ بدون هیچ کار دستی، همان فرش در همه‌ی سایزهایش با ابعاد درست (خطا < ~۳٪ با متر، روی هر دو گوشی) و ظاهر قانع‌کننده روی کف می‌نشیند و روان جابه‌جا می‌شود.

### فاز ۳ — طراحی و ساخت فروشگاه ۰ تا ۱۰۰ ✅ انجام شد (۱۴۰۵/۰۶/۰۱)

> **این فاز با بسته شدن لایه‌ی حرکت تمام شد.** هر ۱۷ آیتم بند ۶، کنترل کیفیت، ورودی سینمایی، و هر چهار شرط پذیرشِ جهت بصری — توالی ورود، رانش پارالاکس، سرآیند جمع‌شونده، و ترنزیشن اشتراکی — ساخته شده‌اند.
>
> **یک باقی‌مانده‌ی کوچک و نام‌دار:** توالی ورود فقط روی بلوک‌های **بالای تای صفحه** نشست. بخش‌های زیرِ تای صفحه‌ی خانه تریگر اسکرول می‌خواهند نه تریگر لود؛ شرحش در بند ۲ همین بخش. این فاز را باز نگه نمی‌دارد ولی ثبت‌شده می‌ماند تا انجام شود.

> **بند ۶-۴ (صفحه‌ی AR) بسته شد (۱۴۰۵/۰۵/۲۳).** موتور AR از فاز ۲ آماده و روی دستگاه واقعی تأییدشده بود، ولی صفحه‌اش در فروشگاه ساخته نشده بود — دکمه‌ی «در خانه‌ی من ببین» به `/carpets/{slug}/ar` لینک می‌داد و آن مسیر ۴۰۴ بود. حالا هست.
> - **لایه‌ی جای‌گذاری از `spike/` منتقل شد، نه بازنویسی.** فقط دو خط `import`ش عوض شد تا `three` را از npm بگیرد. دلیلش ساده است: این تنها بخش پروژه است که روی سخت‌افزار واقعی (اندروید با WebXR و آیفون با Quick Look) آزموده شده، و بازنویسی‌اش آن را به ستون اثبات‌نشده برمی‌گرداند در ازای چند نوع. سطح عمومی‌اش در `carpet-placer.d.ts` دستی اعلام شد تا فایل دست‌نخورده بماند.
> - **سه دستگاه به این آدرس می‌رسند و فقط یکی می‌تواند جلسه را اجرا کند:** اندروید WebXR دارد و ژست‌ها و سایزهای خودمان را می‌گیرد؛ آیفون WebXR ندارد و Quick Look می‌گیرد (جای‌گذاری و مقیاس واقعی کار می‌کند، جابه‌جایی بین سایزها نه — و همین صریح نوشته شده به‌جای دکمه‌ای که بی‌صدا کاری نمی‌کند)؛ **دسکتاپ QR همین صفحه را می‌گیرد**، که بند ۶-۴ صریحاً خواسته بود.
> - **🔑 باگی که مرورگر گرفت: کروم دسکتاپ `navigator.xr` دارد.** تشخیص اولیه بر پایه‌ی وجود همان API بود، پس دسکتاپ جواب «immersive-ar پشتیبانی نمی‌شود» می‌گرفت و به شاخه‌ی «مرورگرت پشتیبانی نمی‌کند» می‌رفت — یعنی **هیچ کاربر دسکتاپی هیچ‌وقت QR را نمی‌دید**، که کل نکته‌ی آن مسیر بود. حالا گوشی‌بودن با `maxTouchPoints` تصمیم‌گیری می‌شود، نه با وجود API.
> - **راهنمای بار اول** یک‌بار نشان داده و در `localStorage` به‌خاطر سپرده می‌شود؛ همان چهار ژست، با همان کلمات، هم پیش از شروع و هم داخل جلسه.
> - **باگ جانبی که همین‌جا پیدا و بسته شد:** صفحه‌ی `/ask` با `AnimatePresence` بدون `mode="wait"` نتایج قدیم و جدید را هم‌زمان mount نگه می‌داشت، پس هر فرشی که در هر دو پاسخ بود دو بار نام‌گذاری می‌شد — دقیقاً شرطی که ترنزیشن اشتراکی تحمل نمی‌کند. خطایش فقط در کنسول دیده می‌شد و صفحه سالم به نظر می‌رسید.
> - **باقی‌مانده‌ی فاز ۳: فقط لایه‌ی حرکت.**

> ## وضعیت جاری (۱۴۰۵/۰۵/۱۶) — نقطه‌ی شروع جلسه‌ی بعد
>
> ### تصمیم‌های بسته (باز نشوند)
>
> جهت بصری، پالت، تایپوگرافی و نام برند در همین بخش پایین‌تر آمده‌اند و **تمام‌اند**. سبک شیشه‌ای هم ساخته شد و رد شد — پرونده‌اش بسته است.
>
> ### ساخته‌شده و کار می‌کند
>
> | قطعه | کجا | نکته |
> |---|---|---|
> | **کلاینت API** | `frontend/src/lib/api/` | تایپ‌ها از `backend/openapi.json` تولید می‌شوند؛ CI انحراف را می‌گیرد. خطاها در همان لایه به جمله‌ی فارسی تبدیل می‌شوند. |
> | **هدر و ناوبری** | `components/toranjan/site-header.tsx` | مگامنو + کشوی موبایل + جمع شدن با اسکرول. جست‌وجو عمداً اینجا نیست. |
> | **صفحه‌ی لیست** | `app/carpets/` | کارت، شبکه، حالت‌های بارگذاری/خطا/خالی. |
> | **پنل فیلتر** | `components/toranjan/filter-panel.tsx` | آکاردئون + چیپ چندانتخابی + اسلایدر بازه با هیستوگرام + جست‌وجوی متنی. |
> | **اندپوینت فست‌ها** | `GET /api/v1/carpets/facets` | شمارش واقعی هر فیلتر + توزیع قیمت. |
> | **صفحه‌ی محصول** | `app/carpets/[slug]/` | گالری با زوم تمام‌صفحه، انتخاب سایز، «در خانه‌ی من ببین»، مشخصات، مشابه‌ها، متادیتای OpenGraph. |
> | **مشتق‌های تصویر** | مهاجرت `0002` + `scripts/backfill_image_derivatives.py` | هر چهار اندازه‌ی مشتق حالا در دیتابیس‌اند، نه فقط ۸۰۰ پیکسلی. |
> | **صفحه‌های درباره/تماس/سؤالات** | `app/about/` · `app/contact/` · `app/faq/` | هر سه ایستا پیش‌رندر می‌شوند. پاسخ‌ها در هر حالت داخل HTML می‌مانند (ارتفاع صفر، بدون unmount) تا صفحه‌ی سؤالات ایندکس‌شدنی بماند. |
> | **آکاردئون** | `components/ui/accordion.tsx` | هوک‌های بدون‌ظاهر `useAccordion`/`useAutoHeight`: فوکوس چرخشی با کلیدهای جهت، `aria-expanded`/`aria-controls`، `inert` روی پنل بسته. **هر آکاردئون بعدی روی همین سوار شود، نه یک کامپوننت تازه.** |
| **PWA و SEO** | `app/manifest.ts` · `app/sitemap.ts` · `app/robots.ts` · `lib/site.ts` · `carpets/[slug]/product-schema.tsx` · `scripts/build_brand_icons.py` | منیفست با آیکون `any` و `maskable`، نقشه‌ی سایت با هر ۷۰ فرش، `robots.txt`، و JSON-LD محصول با `AggregateOffer`. آیکون‌ها **خروجی اسکریپت‌اند**؛ جایگزینی با لوگوی واقعی = گذاشتن `assets/brand/mark.png` و یک دستور. |
| **مقایسه‌ی فرش‌ها** | `app/compare/` · `components/toranjan/compare-tray.tsx` · `lib/store/compare.ts` | سقف چهار فرش، صافی «فقط تفاوت‌ها»، نوار انتخاب چسبان. **دو ریخت از یک جدول:** از `sm` به بالا ستون ویژگیِ چسبان و یک ستون به‌ازای هر فرش؛ زیر `sm` نام ویژگی می‌رود بالای مقدارهایش و فقط دو فرش کشیده می‌شود، با انتخابگر «کدام دو تا». |
| **کنترل‌های گوشه‌ی کارت** | `components/toranjan/corner-toggle.tsx` | قرص ۳۶ پیکسلی درون دکمه‌ی ۴۴ پیکسلی. قلب و ترازو هر دو از این می‌آیند. |
| **اسکلت پنل ادمین** | `app/admin/layout.tsx` · `app/admin/admin-shell.tsx` · `app/admin/(panel)/layout.tsx` · `components/toranjan/admin-nav.tsx` | ریل راست جمع‌شونده + کشوی موبایل + نوار بالا. دروازه‌ی نشست **امنیت نیست** (API خودش هر درخواست بی‌اعتبار را رد می‌کند) — فقط کاری می‌کند که بازدیدکننده‌ی خارج‌شده صفحه‌ی ورود ببیند نه چهار درخواست شکست‌خورده. |
| **ورود ادمین** | `app/admin/login/page.tsx` | متن خطا هرگز نمی‌گوید کدام نیمه غلط بود. `next` پیش از دنبال شدن بررسی می‌شود (`//evil.example` مبدأ دیگری است). |
| **داشبورد** | `app/admin/(panel)/dashboard-view.tsx` + `GET /api/v1/admin/stats` | چهار شمارنده + سفارش‌های اخیر. همه در دیتابیس شمرده می‌شوند، نه با طول پاسخ. |
| **سفارش‌ها** | `app/admin/(panel)/orders/` | چهار تب با شمارش، جست‌وجو، صفحه‌ی جزئیات، و هر سه تغییر وضعیت. |
| **مدیریت فرش‌ها** | `app/admin/(panel)/carpets/` · `components/toranjan/carpet-form.tsx` | جدول صفحه‌بندی‌شده با فرش‌های غیرفعال، فرم ساخت/ویرایش، ویرایشگر سایز، مدیریت عکس. |
| **تم تیره و شیشه‌ای پنل** | `globals.css` بلوک `[data-surface="admin"]` | **تعویض توکن، نه استایل‌شیت دوم** — جدول و نشان و دکمه بدون ویرایش تیره شدند. سه غلظت شیشه: `glass` (کارت روی عکس)، `glass-strong` (کارت ورود)، `glass-overlay` (کشو، روی متن صفحه). |
| **بازبینی AR** | `app/admin/(panel)/ar/` · [corner-editor.tsx](frontend/src/components/toranjan/corner-editor.tsx) | صف رسیدگی + ویرایشگر چهار گوشه با کشیدن و کیبورد + وضعیت هر سایز + دو مسیر بازسازی. جزئیات پایین‌تر. |
>
> ### قواعدی که در حین ساخت تثبیت شد
>
> - **حالت (state) همان URL است.** هر فیلتر یک پارامتر می‌نویسد و از همان می‌خواند. نمای فیلترشده قابل اشتراک است، بعد از رفرش می‌ماند، و نسخه‌ی دومی وجود ندارد که با شبکه از هم بیفتد.
> - **فیلترهای تکرارشونده:** `?material=silk&material=wool`. داخل یک دسته مقادیر جایگزین‌اند، بین دسته‌ها باریک می‌کنند.
> - **دام‌های راست‌به‌چپ که یک‌بار خوردیم:** `translate` فیزیکی است و منطقی نمی‌شود (به `:dir()` بسته شود)؛ کلیدهای چپ/راست باید با جهت صفحه جابه‌جا شوند؛ کلاس‌های `slide-in-from-left/right` پلاگین انیمیشن هم فیزیکی‌اند.
> - **`framer-motion` از بلوک `prefers-reduced-motion` در CSS پیروی نمی‌کند** (با JS استایل اینلاین می‌گذارد) — هرجا استفاده شد باید `useReducedMotion()` بیاید. و **هرگز روی آن شاخه نزنید تا `initial` را تعیین کنید**: سرور و مرورگر دو جواب می‌دهند و ناهماهنگی hydration می‌شود.
> - **به `NavigationMenuViewport` هیچ `style` ندهید** — Radix اندازه‌ی اندازه‌گیری‌شده را همان‌جا اینلاین می‌گذارد و پراپ ورودی کل attribute را جایگزین می‌کند.
> - **فیلد لیستی در FastAPI:** مدل باید به‌صورت `Annotated[Model, Query()]` پاس شود، نه `Depends()`؛ وگرنه مقادیر تکراری بی‌صدا دور ریخته می‌شوند و درخواست ۲۰۰ با کل کاتالوگ برمی‌گردد.
> - **پلاگین انیمیشن نصب است** (`tw-animate-css`) تا کامپوننت بعدی دستی پورت نشود؛ ولی زمان و easing همیشه از مقیاس خودمان.
> - **CSS کتابخانه‌ها در `globals.css` وارد شود، نه داخل کامپوننت.** وقتی از داخل کامپوننت وارد شود، ترتیبش را گراف باندلر تعیین می‌کند و بازنویسی ما گاهی می‌بَرد و گاهی می‌بازد. وارد کردن در `globals.css` بالای فایل، ترتیب را قطعی می‌کند.
> - **`\D` رقم فارسی را هم می‌بلعد.** «۰۲۱ ۱۲۳۴ ۵۶۷۸».replace(/\D/g, "") رشته‌ی خالی می‌دهد، چون `\D` یعنی «رقم اسکی نیست». نتیجه `tel:` بی‌شماره بود و بی‌صدا هم بود — برچسب درست نشان داده می‌شد. هرجا از عدد فارسی رقم می‌کشید، اول به لاتین تا کنید. (`lib/content/contact.ts`، تابع `dialable`.)
> - **حالت خالی را هم باید اجرا کرد.** این باگ فقط وقتی پیدا شد که ثابت‌های تماس موقتاً پر شدند و صفحه واقعاً رندر شد. شاخه‌ای از کد که در حالت پیش‌فرض هرگز اجرا نمی‌شود، آزمون‌نشده است.
> - **حداقل ۴۴ پیکسل، حدِ *ناحیه‌ی لمس* است نه حدِ چیزی که کشیده می‌شود.** جهت بصری می‌گوید رابط روی فرش سبک بنشیند، و قرص کاغذی ۴۴ پیکسلی روی بافت سبک نیست. دکمه ۴۴ می‌شود و قرص درونش ۳۶ می‌ماند: هم بند ۳-۵ رعایت می‌شود، هم تصویر عوض نمی‌شود. `corner-toggle.tsx` همین است.
> - **قیمت‌ها تومان‌اند و `IRR` یعنی ریال.** ISO 4217 کدی برای تومان ندارد. هر جا قیمت برای ماشین منتشر می‌شود (JSON-LD، و بعداً هر فید محصول) باید در ۱۰ ضرب شود، وگرنه فروشگاه قیمت خودش را یک‌دهم اعلام کرده است.
> - **پستگرس ~۷۰٪ قیمت‌ها را نمایی برمی‌گرداند** (`1.0530E+8` به‌جای `105300000`) — اندازه‌گیری‌شده: ۱۷۷ از ۲۵۴ سایز. `lib/format.ts` از قبل با `Number()` حلش کرده بود، ولی **هر مصرف‌کننده‌ی تازه‌ی `price` باید خودش این را بداند**؛ رشته را خام جایی نفرستید.
> - **`metadataBase` نباشد، `og:image` نسبی می‌رود و هیچ خزنده‌ای بازش نمی‌کند.** دو فاز صفحه‌ی محصول تگ درست داشت و هیچ کارتی رندر نمی‌شد. هشدارش در لاگ بیلد بود و خوانده نشده ماند.
> - **آدرس مطلقِ فایل باید مبدأ *سایت* باشد، نه مبدأ بک‌اند.** `absoluteMediaUrl` وقتی `window` نبود به `API_BASE` می‌افتاد و `http://localhost:8000/files/…` را داخل `og:image` می‌گذاشت — میزبانی که در استقرار پشت Caddy اصلاً عمومی نیست.
> - **جدول عریض روی گوشی ستون کم می‌کند، نه پیکسل.** فشرده کردن چهار ستون در ۳۷۵ پیکسل یعنی ۸۴ پیکسل فرش در هر ستون و اسکرول افقی — و دو چیزی که هم‌زمان دیده نمی‌شوند، مقایسه نمی‌شوند. الگوی درست (همان که دیجی‌کالا هم گرفته): روی گوشی فقط **دو** ستون، و نام ویژگی به‌جای ستون کناری، **بالای** مقدارهایش و وسط‌چین. عرض کم است، ارتفاع نه.
>   - جدول یکی می‌ماند و دو بلوک با `hidden` نمی‌شود: ستون‌ها فقط وقتی بین ردیف‌ها تراز می‌مانند که واقعاً ستون یک جدول باشند.
>   - **اگر سقف بیش از دوتاست، انتخابگر لازم است.** «دوتای اول را نشان بده و بقیه هیچ» یعنی کسی که روی گوشی چهار فرش انتخاب کرده، نصفش را بی‌هیچ توضیحی از دست می‌دهد.
>   - **برچسب انتخابگر باید با ستون بخواند.** جفت انتخاب‌شده به ترتیب فهرست مرتب می‌شود، وگرنه «فرش نخست: تبریز» می‌گوید و تبریز در ستون دوم می‌ایستد.
> - **فرزند `absolute` هم در `scrollWidth` ظرف اسکرول حساب می‌شود.** صلیب حذفِ ستون که ۱۰ پیکسل از گوشه بیرون می‌زد، به جدول ۴ پیکسل اسکرول بی‌خود داد.
> - **سلول چسبان به لبه‌ی *پدینگِ* اسکرول‌پورت می‌چسبد، نه لبه‌ی بیرونی‌اش.** جدول مقایسه با `-mx-5 px-5` تا لبه‌ی گوشی پهن شده بود؛ نتیجه این شد که ستون ویژگی ۲۰ پیکسل مانده به لبه می‌ایستاد و ستون‌های فرش از آن نوار کنارش رد می‌شدند. اندازه‌گیری‌شده، نه حدس: `elementFromPoint` روی x=۳۶۵ از ۳۷۵ یک `<td>` برگرداند. راه‌حل: پهن نکردن.
> - **نوار پایین صفحه `sticky` باشد، نه `fixed`.** نوار ثابت روی دو خط آخر فوتر می‌نشیند، و علاج معمولش — پدینگ گرفتن بدنه به اندازه‌ی ارتفاع نوار — یعنی اندازه‌گیری ارتفاعی که با متن داخلش تغییر می‌کند. `sticky bottom-0` روی آخرین عنصر جریان همان تصویر است بدون هیچ‌کدام: تا وقتی صفحه هست بالای آن شناور می‌ماند و در انتها زیر فوتر می‌نشیند.
> - **`letter-spacing` روی فارسی، کلمه را می‌شکند.** خط فارسی متصل است؛ حروف یک کلمه به هم می‌چسبند و `letter-spacing` همین اتصال‌ها را باز می‌کند، پس «مشتری» از کلمه بودن درمی‌آید و چهار تکه می‌شود که کنار هم ایستاده‌اند. tracking ابزار حروف‌بزرگ **لاتین** است — روی TORANJAN درست، روی هر برچسب فارسی غلط. در ۱۱ پیکسل آسیبش شدید است. کلاس `.panel-label` همین را حل می‌کند: ۱۲٫۵ پیکسل، بدون فاصله‌گذاری، `--ink-2`.
> - **گذشتن از AA کفِ متن بدنه است، نه هدفِ برچسب.** `--muted` با ۵٫۶۵:۱ استاندارد را رد می‌کرد و در ۱۲–۱۳ پیکسل روی زمینه‌ی تیره «مه خاکستری» خوانده می‌شد. بالا بردن **خودِ توکن** (به ۷٫۶۴:۱) همه‌ی رشته‌های کم‌رنگ را یک‌جا درست می‌کند؛ اصلاح تک‌تک کلاس‌ها هیچ‌وقت تمام نمی‌شود.
> - **`AnimatePresence initial={false}` روی همه‌ی نوادگان اثر می‌گذارد.** به نظر می‌رسد فقط ورود خودِ wrapper را خاموش می‌کند، ولی framer آن را روی presence context منتشر می‌کند و هر `motion` زیردست از آن پیروی می‌کند — یعنی در بار **اول** پنل، شمارنده‌ها و همه‌ی سطرها ورودشان را رد می‌کردند. انیمیشن فقط در ناوبری دوم اجرا می‌شد.
> - **این استایل‌شیت بیرون از layer است، پس از هر utility تیلویند بالاتر می‌ایستد.** `.glass` یک `position: relative` داشت (برای `::before`) و همان یک خط `fixed` کشوی موبایل را می‌بلعید: کشو درست باز می‌شد ولی در جریان سند، ۱۳۸۲ پیکسل پایین‌تر از صفحه‌ی ۸۱۲ پیکسلی. **یک کلاس ظاهری هرگز نباید `position` تعیین کند.** خط نورانی حالا لایه‌ی پس‌زمینه است، نه pseudo-element.
> - **پرده‌ها ضرب می‌شوند، جمع نمی‌شوند.** ۰٫۸۶ زیر ۰٫۶۲ می‌شود ۰٫۹۵ و عکس سیاه می‌شود. دو زمین جدا (`admin-ground` و `admin-ground-hero`) به‌جای یک زمین با پرده‌ی دوم.
> - **شیشه روی *عکس* و شیشه روی *متن* دو غلظت‌اند.** کارت روی عکسِ پرده‌خورده با ۰٫۶۶ کار می‌کند؛ همان ۰٫۶۶ زیر کشویی که روی متن صفحه می‌افتد، متن‌های کم‌رنگ را به ۲٫۴۶:۱ می‌رساند و دو لایه تایپ از هم رد می‌شوند. کشو ۰٫۹۲ می‌خواهد.
> - **Radix محتوایش را به `document.body` پورتال می‌کند.** یعنی بیرون از المانی که توکن‌های `[data-surface]` را دارد: هر مقدار تیره به مقدار روشن برمی‌گردد و کشو سفید درمی‌آید. `data-surface` باید **روی خودِ پنل** تکرار شود، و قواعد شیشه باید «همان المان» را هم مچ کنند نه فقط فرزند.
> - **ترتیب فایل بین دو مدیاکوئری هم‌وزن تصمیم می‌گیرد.** قاعده‌ی «روی گوشی عکس را بردار» قبل از قاعده‌ی `prefers-reduced-transparency` نوشته شده بود و دومی دوباره عکس را برمی‌گرداند.
> - **`prefers-reduced-transparency` را بیش از اندازه نخوانید.** آن ترجیح درباره‌ی شفافیت و بلور است — زمینه‌ی نیمه‌دیده و متحرک زیر متن — نه درباره‌ی عکس ثابت پشت پنل مات. نسخه‌ی اول عکس را هم حذف می‌کرد و برای کسی که «جلوه‌های شفافیت» ویندوز را خاموش کرده، کل طراحی می‌شد یک مستطیل سیاه.
> - **دو ویرگول داریم.** کیبورد فارسی «،» می‌سازد. فیلد رنگ‌های فرم با «،» **پر** می‌شد و اعتبارسنج فقط روی `,` لاتین split می‌کرد — پس هر فرشی با دو رنگ یا بیشتر بی‌صدا جلوی ثبت را می‌گرفت و «ذخیره» ظاهراً هیچ کاری نمی‌کرد، چون خطا به فیلدی چسبیده بود که کسی دستش نزده بود. هر جا رشته‌ای هم نوشته و هم خوانده می‌شود، **یک تابع** باید هر دو کار را بکند.
> - **شکل عمومی و شکل ادمین یکی نیستند، و این تصمیم امنیتی است.** `OrderOut` عمداً `id` و آدرس و تلفن ندارد: پیگیری با کد رهگیری و شماره‌ی تلفن اثبات می‌شود که هر دو حدس‌زدنی‌اند. `AdminOrderOut` و `AdminCarpetRow` و `AdminCarpetDetail` جداگانه تعریف شده‌اند. **نبودِ `id` در شکل عمومی یک‌بار باعث شد پنل بتواند سفارش‌ها را نشان بدهد و هیچ‌کدام را عوض نکند** — و تست یکپارچه چون `/orders/1` را دستی صدا می‌زد، سبز می‌ماند.
> - **`framer-motion` در پنل بسته‌ی مرورگر یخ می‌زند.** صفحه‌ی پنهان `requestAnimationFrame` اجرا نمی‌کند، پس انیمیشن ورود در فریم صفر می‌ماند و `transform` روی مقدار `initial` قفل می‌شود. هر اندازه‌گیری هندسی روی عنصر متحرک تا آن مقدار جابه‌جا خوانده می‌شود. باگ نیست؛ برای اندازه‌گیری، `transform` را موقتاً با `style.setProperty(..., 'important')` صفر کنید.
> - **ذخیره‌ساز محتوا-آدرس است:** نام هر فایل هش بایت‌های خودش است. یعنی از روی URL یک مشتق نمی‌شود مشتق دیگر را ساخت؛ بازیابی فقط با اجرای دوباره‌ی پایپ‌لاین روی عکس مبدأ ممکن است. هر جا URL جدیدی لازم شد، همان لحظه ذخیره‌اش کنید.
> - **جعبه‌ای که می‌بُرد، در راست‌به‌چپ محتوایش را جابه‌جا می‌کند (۱۴۰۵/۰۵/۲۰).** بدترین باگ این جلسه و از یک قاعده‌ی از قبل ثبت‌شده آمد: **فرزند `absolute` هم در `scrollWidth` ظرف حساب می‌شود.** دستگیره‌ها روی گوشه مرکز می‌شوند، پس نصف ۴۴ پیکسلشان بیرون قاب می‌افتد و `scrollWidth` را ۲۲ پیکسل از `clientWidth` بزرگ‌تر می‌کند. با `overflow: hidden` قاب می‌شود ظرف اسکرول، و **ظرف اسکرول در صفحه‌ی راست‌به‌چپ از راست شروع می‌شود** — یعنی عکس ۲۲ پیکسل جابه‌جا *نقاشی* می‌شد در حالی که `offsetLeft` هنوز صفر بود و هر گوشه ۳۰ پیکسلِ مبدأ خطا می‌داد. علاجش بریدن نیست: `overflow-hidden` برداشته شد و گردی روی خود عکس رفت. **درس عمومی: `getBoundingClientRect` و `offsetLeft` وقتی ظرف اسکرول راست‌به‌چپ در میان است دو چیز متفاوت می‌گویند، و آن که چشم می‌بیند اولی است.**
> - **کلید جهت را فقط جایی برعکس کنید که خودِ کنترل آینه می‌شود.** برای اسلایدر بازه لازم بود چون کمینه‌اش در راست‌به‌چپ سمت راست می‌ایستد. **عکس آینه نمی‌شود:** گوشه‌ی `x=0` برای هر بیننده‌ای لبه‌ی چپ تصویر است، پس ArrowLeft باید `x` را کم کند. بریف این صفحه برعکسش را انتظار داشت و اجرا نشد؛ اجرا کردنش یعنی کاربر چپ را بزند و دستگیره برود راست.
> - **`hasPointerCapture` را به‌جای state بپرسید.** نگهبانِ «آیا این دستگیره در حال کشیده شدن است» اگر state باشد یک رندر عقب است: فشار و حرکت در یک تسک — کشیدن سریع، یا رویداد dispatch‌شده — یعنی اولین حرکت‌ها با مقدار `null` سنجیده و دور ریخته می‌شوند. `setPointerCapture` هم‌زمان در `pointerdown` ست می‌شود، پس جواب DOM همیشه به‌روز است. state فقط برای *ظاهرِ* دستگیره‌ی فعال بماند.
> - **`touch-action` ارث نمی‌رسد.** روی قاب `none` بود و روی خودِ دکمه اندازه‌گیری شد: `auto`. طبق استاندارد `none`ِ نیا باید لمسِ فرزند را هم بپوشاند، ولی مرورگرها همیشه هم‌نظر نبوده‌اند — همان‌جا که لمس می‌نشیند اعلامش کنید.
> - **«طلایی فقط برای نشانه‌های ریز» یک استثنا پیدا کرد (۱۴۰۵/۰۵/۲۰، تصمیم کارفرما).** کنش شاخص صفحه‌ی محصول — «در خانه‌ی من ببین» — از `--cta` (نزدیک به مشکی) به `--accent` رفت، همان طلایی قلبِ علاقه‌مندی. قاعده‌ی قبلی در [components/ui/README.md](frontend/src/components/ui/README.md) بود و برای *سطح‌های اتفاقی* کامپوننت‌های واردشده نوشته شده بود، نه برای مهم‌ترین دکمه‌ی فروشگاه؛ حالا صریح است که این یک مورد استثناست و بقیه‌ی سطح‌ها همچنان طلایی نمی‌شوند. کنتراست اندازه‌گیری شد: `#fafafa` روی `#a16207` برابر **۴٫۷۲:۱** — از AA رد می‌شود. **اگر پالت روزی تیره شد این باید دوباره سنجیده شود**، چون طلایی روشنِ حالت تیره (`#d9a842`) با متن سفید رد نمی‌شود.
> - **`flex-1` در ستون، ارتفاع را می‌بلعد — نه عرض را (۱۴۰۵/۰۵/۲۰).** ردیف دکمه‌های صفحه‌ی محصول روی گوشی `flex-col` است، و `flex-1` آن‌جا یعنی `flex-basis: 0` روی **محور اصلی که ارتفاع است**: دکمه‌ی «در خانه‌ی من ببین» تا یک خط متن جمع شد — **۲۳ پیکسل، نصفِ کف بند ۳-۵** — در حالی که `h-13` سر جایش در فهرست کلاس‌ها بود و هیچ کاری نمی‌کرد، چون flex-basis روی محور اصلی از `height` جلو می‌زند. روی دسکتاپ سالم به نظر می‌رسید، چون آن‌جا ردیف واقعاً ردیف است و همان اعلان عرض را تنظیم می‌کند. **قاعده: در ظرفی که ممکن است `flex-col` شود هرگز `flex-1` تنها ننویسید؛ `w-full sm:flex-1`.** این را کارفرما از روی گوشی گزارش داد، نه اندازه‌گیری.
> - **`preventDefault` روی `pointerdown` فوکوس را هم لغو می‌کند.** دستگیره‌ها بعد از فشردن فوکوس نمی‌گرفتند، پس کلیدهای جهت به صفحه می‌رفتند و «کیبورد کار نمی‌کند» — گزارش از دسکتاپ. برداشتنِ `preventDefault` راه‌حل نیست (همان است که جلوی انتخاب متن و درگِ بومی عکس را می‌گیرد)؛ `focus()` صریح صدا زده می‌شود. **آزمون اولیه‌ی من این را نگرفت چون خودش `focus()` را صدا می‌زد** — آزمونی که مقدمه‌ای را که کاربر ندارد فراهم کند، چیزی را اثبات نمی‌کند.
> - **ستون جدول را نامِ داخلش پهن می‌کند، مگر `table-fixed` (۱۴۰۵/۰۵/۲۰، گزارش کارفرما).** در صفحه‌ی مقایسه عکس فرش‌ها «کوچیک‌بزرگ» دیده می‌شد و دو علت داشت، هر دو اندازه‌گیری‌شده:
>   - **ستون‌ها هم‌عرض نبودند:** با چیدمان خودکار و `w-auto`، عرض ستون از طول نام فرش می‌آمد — دو قاب کنار هم ۵۲۴ و ۴۷۵ پیکسل. `table-fixed` عرض را از همین ردیف می‌گیرد و متن را نادیده می‌گیرد، که دقیقاً خواسته‌ی این جدول است: ستون‌هایش قرار است قابل‌تعویض باشند. `min-w` نگه داشته شد تا جدول تنگ، به‌جای فشرده شدن، اسکرول کند.
>   - **قاب مربع بدترین شکل برای فرش است:** فرش‌ها عمودی عکس گرفته می‌شوند (دو نمونه‌ی اندازه‌گیری‌شده ۰٫۵۸ و ۰٫۷۲)، و `contain` داخل مربع به هرکدام نوار خالی متفاوتی در دو طرف می‌دهد — یکی ۵۸٪ عرض قابش را پر می‌کند و بعدی ۷۲٪. با نسبت ۲:۳ هر دو تقریباً قاب را پر می‌کنند: **اندازه‌گیری‌شده، اختلاف پرشدگی از ۱۴ واحد به ۶ رسید و هر چهار کاشی دقیقاً هم‌اندازه شدند** (۲۲۵×۳۳۷ در ۱۲۸۰، ۱۵۴×۲۳۰ در ۳۷۵).
>   - **`cover` نشد و عمداً:** بریدن، کاشی‌ها را به روش تنبل یکسان می‌کند — با قیچی کردن حاشیه‌ی نقشه، و حاشیه یکی از همان چیزهایی است که آدم برای مقایسه‌اش به این صفحه آمده.
>   - **علت سوم، که بعد از دو تای بالا هنوز باقی بود و کارفرما روی گوشی گرفتش: `align-bottom`.** سرستون یعنی «عکس، بعد نام»، و نام به هر تعداد خط که لازم باشد می‌شکند — دو خط برای «فرش ماشینی هانوکس کد HAM.136.03» و چهار خط برای یکی از نام‌های ۱۰۰۰ شانه‌ی اورکوم. وقتی سلول‌های هم‌ارتفاع از **پایین** تراز شوند، بلوک بلندتر از بالاتر شروع می‌شود: **کاشی‌ها دقیقاً یکی بودند (۱۵۴×۲۳۰، اندازه‌گیری‌شده) ولی سرِ عکس‌ها ۵۱ پیکسل با هم فاصله داشت** و ردیف نامرتب دیده می‌شد بدون اینکه چیزی بداندازه باشد. با `align-top` هر عکس روی یک خط می‌نشیند و نامرتبی می‌رود زیر عکس‌ها، جایی که کسی مقایسه‌اش نمی‌کند. اندازه‌گیری بعدش: اختلاف **صفر** پیکسل در ۳۷۵ و ۱۲۸۰.
>   - **با کوتاه کردن نام حل نشد، عمداً:** در یک مقایسه، کلمه‌ی متمایزکننده معمولاً آخرین کلمه است («زمینه کرم» در برابر «زمینه طوسی»)، پس بریدن متن دقیقاً همان چیزی را پنهان می‌کند که صفحه برایش هست.
>   - **درس عمومی:** وقتی چند سلول هم‌ارتفاع، محتوایی با ارتفاع متغیر دارند، `vertical-align` تعیین می‌کند کدام لبه تراز بماند. لبه‌ای را تراز کنید که چشم مقایسه‌اش می‌کند.
> - **هدری که ارتفاع خودش را عوض می‌کند، به دو آستانه نیاز دارد نه یکی (۱۴۰۵/۰۵/۲۰، گزارش کارفرما).** با یک خط در ۲۴ پیکسل، نوار دقیقاً روی همان نقطه بی‌نهایت بین دو اندازه می‌پرید. **بازتولید شد** در عرض ۳۹۰: ارتفاع بین ۷۳ و ۷۷ می‌لرزید و `scrollY` بین ۲۱ و ۲۵ بالا و پایین می‌رفت، بدون اینکه کسی دست به اسکرول بزند.
>   - **علت:** هدر `sticky` است یعنی **در جریان سند**، پس کوچک شدنش از ۷۸ به ۶۲ یعنی ۱۶ پیکسل کم شدن از بالای صفحه. لنگر اسکرول مرورگر (scroll anchoring) تغییر اندازه‌ی محتوای بالای دید را با جابه‌جا کردن `scrollY` جبران می‌کند — دقیقاً به آن‌سوی خط ۲۴، که هدر را بزرگ می‌کند، که `scrollY` را دوباره جابه‌جا می‌کند. ترنزیشن ۵۰۰ms ارتفاع هم پیوسته به این حلقه سوخت می‌رساند.
>   - **علاج:** هیسترزیس. کوچک شدن از ۷۲ به بالا، بزرگ شدن زیر ۲۴. فاصله‌ی ۴۸ پیکسلی را یک تصحیح ۱۶ پیکسلی نمی‌تواند رد کند. به‌علاوه‌ی `overflow-anchor: none` روی خود نوار، که به مرورگر می‌گوید تغییر اندازه‌ی این المان را جبران نکند. **قاعده‌ی عمومی: هر چیزی که بر اساس موقعیت اسکرول اندازه‌ی خودش را عوض می‌کند و در جریان سند است، باید دو آستانه داشته باشد که فاصله‌شان از تغییر ارتفاعش بیشتر است.**
> - **فهرست مقایسه به‌اندازه‌ی یک بازدید عمر دارد، نه به‌اندازه‌ی دستگاه (۱۴۰۵/۰۵/۲۰، گزارش کارفرما).** باز کردن سایت و دیدن نواری در پایین صفحه با چهار فرشِ نشستِ قبلی، «فهرست ذخیره‌شده» نیست؛ ته‌مانده است — جا می‌گیرد، می‌گوید فروشگاه در وضعیتی است که کاربر نگذاشته، و اولین کاری که از او می‌خواهد پاک کردنش است. مقایسه پرسشی است که همان لحظه پرسیده می‌شود و تا آخر بازدید جواب می‌گیرد. پس `sessionStorage`، نه `localStorage`: با رفرش و گشتن در سایت می‌ماند و با بستن سایت می‌رود. **علاقه‌مندی‌ها و سبد عمداً دست‌نخورده ماندند** — آن‌ها دقیقاً از جنس مخالف‌اند: آگاهانه انتخاب می‌شوند تا بعداً پیدا شوند.
>   - مکانیزم همان ذخیره‌ساز است و بس؛ نه پاک کردن هنگام بارگذاری، نه قاعده‌ی «این فهرست چقدر کهنه است» که باید نوشته و بعد اشتباه تنظیم شود. کلید قدیمی `localStorage` هنگام import یک‌بار پاک می‌شود تا از دستگاه‌هایی که قبلاً آمده‌اند باقی نماند.
>   - این یک استدلال ثبت‌شده در همان فایل را هم اصلاح کرد: نوشته بود فهرست مقایسه «روی این دستگاه نوشته می‌شود، دقیقاً مثل سبد و علاقه‌مندی‌ها». دیگر درست نیست و متن عوض شد.
> - **جاروی کنترل‌های لمسی (۱۴۰۵/۰۵/۲۰) — بدهی ۱۰.** هر صفحه‌ی فروشگاه و پنل در سه عرض، با اسکریپت اندازه‌گیری روی `getBoundingClientRect` نه با چشم. **هفت خرابی واقعی پیدا شد که هیچ‌کدام به چشم نمی‌آمدند:** پنج تریگر مگامنو (۳۶)، هشت چیپ فیلتر (۳۶)، دو دستگیره‌ی اسلایدر قیمت (۲۰×۲۰)، کلید «دیدن نتیجه‌ها»ی برگه‌ی فیلتر (۲۶٫۶)، دو نان‌ریزه‌ی صفحه‌ی محصول (۲۰)، نقطه‌های گالری (۲۰×۸)، دکمه‌ی «فرش‌های مشابه» (۳۶) و انتخابگر سایز (۴۲).
>   - **دو تا از این‌ها ارتفاع اعلام‌شده داشتند و رعایت نمی‌شد.** «دیدن نتیجه‌ها» `h-12` داشت و ۲۶٫۶ پیکسل بود، چون برگه یک ستون فلکس است و محتوایش بلندتر از پنل — پس فلکس‌باکس **کوچکش کرده بود**. علاجش `shrink-0` است. این دقیقاً هم‌خانواده‌ی باگ `flex-1` است: **در فلکس، `height` یک آرزوست نه یک اندازه؛ اگر ظرف فلکس است، اندازه را اندازه بگیرید.**
>   - **دو مورد خرابی نبودند و باید ثبت شوند تا دوباره وقت نگیرند:** دکمه‌ی زوم کتابخانه‌ی گالری `pointer-events: none` و `clip-path` دارد — یعنی اصلاً هدف اشاره‌گر نیست و فقط برای کیبورد و صفحه‌خوان است (لمس روی خودِ عکس کار می‌کند). و ورودی فایل پنل `sr-only` است با برچسب دیدنی جدا. **هر جاروی بعدی باید این دو را کنار بگذارد.**
>   - **نقطه‌های گالری تنها استثناست و عمدی است:** ارتفاع کامل ۴۴، ولی عرض ۲۴. عریض‌ترین فرش کاتالوگ هفت عکس دارد و هفت هدفِ ۴۴ پیکسلی می‌شود ۳۰۸ پیکسل که کنار دکمه‌ی «فرش‌های مشابه» در ۳۷۵ جا نمی‌شود — و ردیف نقطه‌ای که ۴۴ پیکسل از هم فاصله دارند دیگر یک کنترل خوانده نمی‌شود. ۲۴ با فاصله‌ی بدون همپوشانی، به‌علاوه‌ی سوایپ و خودِ عکس که به همان تصویرها می‌رسند. ردیف هم `flex-wrap` گرفت تا در بدترین حالت بشکند نه سرریز کند.
> - **«ساخته نشده» قرمز شد و برگردانده شد — خاکستری می‌ماند (۱۴۰۵/۰۵/۲۰).** استدلال قرمز از جدول فرش‌ها آمده بود («غیرفعال» آن‌جا قرمز است چون خاکستری شکل هر چیز تمام‌شده‌ای است)، ولی به این صفحه منتقل نمی‌شود: **اینجا قرمز از قبل صاحب دارد** — `failed` قرمز است و چیز بدتر و کمیاب‌تری می‌گوید. دو قرمزِ نزدیک‌به‌هم فهرست را سخت‌خوان‌تر می‌کنند نه آسان‌خوان‌تر، و سایزی که هنوز ساخته نشده حالتِ عادیِ بیشترِ کاتالوگ است نه یک خرابی. **قاعده‌ی عمومی: پیش از بلند کردن صدای یک حالت، ببینید آن رنگ در همان صفحه قبلاً چه می‌گوید.**
> - **ذره‌بین به *حرکت* تعلق دارد، نه به فوکوس (۱۴۰۵/۰۵/۲۰).** رها کردن کلیک یا برداشتن انگشت خودش یعنی «همین‌جا درست است»؛ ذره‌بینی که بعد از آن روی عکس بماند خلافش را می‌گوید. اولین نسخه با «تا وقتی فوکوس دارد» ساخته شد و آن یعنی **برای همیشه بعد از اولین فشار**، چون فشردن دستگیره فوکوسش هم می‌کند. حالا دو حالت جدا دارد: `dragging` که با `pointerup` تمام می‌شود، و `keying` که فقط با کلید جهت روشن می‌شود و با ترک دستگیره یا `Escape` خاموش. کیبورد هم ذره‌بین می‌خواهد و دلیلش برعکس است: چیزی نقطه را نپوشانده، ولی جابه‌جایی چندپیکسلیِ مبدأ در اندازه‌ای که عکس کشیده شده دیده نمی‌شود.
> - **دو دکمه‌ای که روی گوشی زیر هم می‌آیند، با محتوای وسط‌چین هم‌تراز نمی‌شوند.** طول برچسب‌ها یکی نیست («در خانه‌ی من ببین» ۱۰۷ پیکسل، «افزودن به سبد خرید» ۱۱۹)، پس بلوکِ آیکون‌و‌متن دو عرض دارد و وسط‌چین شدن، آیکون‌ها را ۸ پیکسل و متن‌ها را ۱۰ پیکسل از هم دور می‌اندازد. **علاجش هم‌اندازه کردن جعبه‌ی برچسب است، نه هم‌اندازه کردن کلمه‌ها:** یک `min-width` مشترک (`ACTION_LABEL`) روی برچسبِ هر دو. حداقل، نه عرض ثابت — تا برچسب بلندتر (جمله‌ی «هنوز آماده نیست») به‌جای بیرون‌زدن، جا باز کند. همین، سه حالتِ خودِ دکمه‌ی سبد را هم که با هر سوییچ آیکون را تکان می‌داد، ثابت کرد. اندازه‌گیری‌شده در ۳۷۵: آیکون‌ها و مرکز عنوان‌ها هر دو با اختلاف **صفر**.
>   - بقیه‌ی جعبه هم صریحاً یکی شد: ارتفاع، گردی، فونت، اندازه‌ی آیکون، فاصله، پدینگ، و **حاشیه‌ی یک‌پیکسلی** — دکمه‌ی سبد حاشیه داشت و آن یکی نه، و همین دو جعبه‌ی محتوا را متفاوت می‌کرد.
>   - **تنگ‌ترین جا `lg` است، نه گوشی:** آن‌جا ستون اطلاعات باریک می‌شود و دو دکمه دقیقاً ردیف را پر می‌کنند (۴۴۹ در برابر ۴۴۹، بدون کلیپ). هر بار که این `min-width` بزرگ شد باید همان‌جا دوباره اندازه گرفته شود.
> - **آن‌چه زیر انگشت است دیده نمی‌شود.** گذاشتن گوشه یعنی دیدن دقیقاً همان نقطه‌ای که نوکِ انگشت پوشانده است. راه‌حل همان است که برنامه‌های ویرایش عکس و پاک‌کن دارند: ذره‌بین. همان فایل به‌صورت `background` در یک دایره، بزرگ‌نمایی نسبت به اندازه‌ی *نقاشی‌شده* (نه مبدأ، چون هدف نشان دادن بیش از آن چیزی است که صفحه نشان می‌دهد)، با ضربدر در مرکز. زیر گوشه‌های بالا و بالای گوشه‌های پایین می‌نشیند.
>   - **ذره‌بین خودش می‌توانست صفحه را افقی اسکرول کند:** روی گوشه‌ای که لبه‌ی عکس است نصف دایره بیرون می‌زند. جعبه داخل قاب مهار شد؛ چون آفست پس‌زمینه نسبت به *مرکز دایره* نوشته شده، جابه‌جا کردن جعبه ضربدر را از روی گوشه برنمی‌دارد. اندازه‌گیری‌شده: در ۳۷۵، هر چهار گوشه داخل قاب و بدون سرریز.
> - **بلندی ویرایشگر باید به ارتفاع پنجره بسته باشد.** عکس عمودی فرش با عرض کامل ستون ۱۱۶۶ پیکسل بلند شد در پنجره‌ی ۸۰۰ پیکسلی: دستگیره‌ی بالا و پایین هیچ‌وقت با هم روی صفحه نبودند، و گذاشتنِ چهار گوشه یعنی دیدن چهار گوشه. `max-h-[65vh]` با `w-auto` نسبت را نگه می‌دارد و هر کدام از دو حد که زودتر بست، می‌بندد.
>
> ### تمام‌شده‌های فاز ۳ (۱۴۰۵/۰۵/۱۶)
>
> | آیتم بند ۶ | کجا |
> |---|---|
> | ۱ خانه | [app/page.tsx](frontend/src/app/page.tsx) — بلوک قابلیت‌ها، هیروی بادبزنی، تازه‌ترین‌ها، دسته‌بندی اتاق، ابریشم |
> | ۲ لیست + فیلتر + صفحه‌بندی | [app/carpets/](frontend/src/app/carpets/page.tsx) |
> | ۳ صفحه‌ی محصول + زوم + گالری چندعکسی + سوایپ | [product-detail.tsx](frontend/src/components/toranjan/product-detail.tsx) |
> | ۹ سبد و ثبت سفارش | [cart-view.tsx](frontend/src/components/toranjan/cart-view.tsx) · [checkout-form.tsx](frontend/src/components/toranjan/checkout-form.tsx) |
> | ۱۰ پیگیری سفارش | [app/track/](frontend/src/app/track/page.tsx) |
> | ۱۱ علاقه‌مندی‌ها | [app/favourites/](frontend/src/app/favourites/page.tsx) · `lib/store/favorites.ts` |
> | ۱۱ مقایسه‌ی فرش‌ها | [app/compare/](frontend/src/app/compare/page.tsx) · [compare-table.tsx](frontend/src/app/compare/compare-table.tsx) · [compare-tray.tsx](frontend/src/components/toranjan/compare-tray.tsx) · [lib/store/compare.ts](frontend/src/lib/store/compare.ts) |
> | ۱۲ درباره / تماس / سؤالات | [app/about/](frontend/src/app/about/page.tsx) · [app/contact/](frontend/src/app/contact/page.tsx) · [app/faq/](frontend/src/app/faq/page.tsx) · [faq-accordion.tsx](frontend/src/components/toranjan/faq-accordion.tsx) |
> | ۱۶ بازبینی AR | [app/admin/(panel)/ar/](frontend/src/app/admin/\(panel\)/ar/ar-view.tsx) · [ar-review.tsx](frontend/src/app/admin/\(panel\)/ar/[id]/ar-review.tsx) · [corner-editor.tsx](frontend/src/components/toranjan/corner-editor.tsx) |
> | اسکلت | [site-header.tsx](frontend/src/components/toranjan/site-header.tsx) · [site-footer.tsx](frontend/src/components/toranjan/site-footer.tsx) |
>
> جزئیات هر تصمیم در سرصفحه‌ی خودِ فایل‌ها و در پیام کامیت‌هاست؛ اینجا تکرار نمی‌شود.
>
> ### سه سؤال بی‌جواب که پرسیده شد و جوابش نیامد (۱۴۰۵/۰۵/۱۵)
>
> هیچ‌کدام جلوی قدم بعدی را نمی‌گیرند. اگر جلسه‌ی تازه‌ای شروع شد و کارفرما چیزی نگفت، همان قدم بعدی را بگیرید و این سه را سر فرصت بپرسید.
>
> ۱. **فرم تماس بسازیم یا نه؟** صفحه‌ی `/contact` فعلاً فقط کانال‌های تماس را نشان می‌دهد، که دقیقاً همان چیزی است که بند ۶-۱۲ خواسته (لینک واتس‌اپ و اینستاگرام، نه فرم تیکت). فرم واقعی یک تکه کار مستقل است، نه یک فرم: مدل پیام + مهاجرت Alembic + `POST /api/v1/contact` + `export_openapi.py` + `npm run gen:api` + تست. **فرم بدون اندپوینت ساخته نشود** — دکمه‌ی «ارسال» که هیچ‌جا نمی‌رود همان وعده‌ی شکسته‌ای است که فوتر عمداً از آن دوری کرد.
>
> ۲. **مقدار واقعی راه‌های تماس.** بدهی ۸ پایین‌تر. چهار ثابت خالی در `lib/content/contact.ts`.
>
> ۳. **`26dea33` را cherry-pick کنیم یا بماند برای کنترل کیفیت؟** بدهی ۷ پایین‌تر.
>
> ### قدم بعدی، به همین ترتیب
>
> **کنترل کیفیت انجام شد (۱۴۰۵/۰۵/۲۰).** جاروی کنترل‌های لمسی (بدهی ۱۰) روی هر صفحه و سه عرض اجرا شد و هفت خرابی واقعی داد که همه اصلاح شدند؛ بدهی‌های ۶ و ۷ و ۹ هم بسته شدند. به‌علاوه هفت گزارش از استفاده‌ی واقعی کارفرما روی گوشی و دسکتاپ، که همه در «قواعد» بالا ثبت‌اند.
>
> **ورودی سینمایی ساخته شد (۱۴۰۵/۰۵/۲۰).** پنل و هر ۱۷ آیتم بند ۶ تمام‌اند. بدهی‌های ۲، ۳ و ۴ هم بسته شدند. شرح کامل در بخش «ورودی سینمایی — چه ساخته شد» پایین‌تر.
>
> ### لایه‌ی حرکت — ✅ بسته شد (۱۴۰۵/۰۶/۰۱)
>
> **این بند دیگر بدهی نیست؛ سابقه است.** هر چهار مورد ساخته شد و متن هرکدام سر جایش مانده چون درس‌هایش تکرارشدنی‌اند — مخصوصاً بند ۱، که سه هفته باز بود و جوابش سه خط CSS شد. تنها چیزی که از این بخش می‌ماند، نیمه‌ی «زیرِ تای صفحه»ی بند ۲ است.
>
> کارفرما پس از دیدن ورودی گفت «حرکت و انیمیشن‌ها کم است» و به [gooshtiran.com](https://gooshtiran.com/درباره-ما/) به‌عنوان نمونه اشاره کرد. **این حس درست است و ریشه‌اش در همین سند مستند بود** — جهت «گالری موزه‌ای» در ۱۴۰۵/۰۵/۱۲ با یک **شرط پذیرش** انتخاب شد:
>
> > این چیدمان عمداً ساده است و اگر حرکت نداشته باشد خشک می‌شود. جبرانش با لایه‌ی حرکت است: توالی ورود صفحه، رانش پارالاکس داخل هر قاب، سرآیندی که با اسکرول جمع می‌شود، و ترنزیشن اشتراکیِ تصویر از شبکه به صفحه‌ی محصول.
>
> **هر چهار مورد ساخته شد (۱۴۰۵/۰۶/۰۱).** آنچه ماند و در هر بند نوشته شده:
>
> 1. ~~**ترنزیشن اشتراکی تصویر، شبکه → صفحه‌ی محصول.**~~ **✅ حل شد (۱۴۰۵/۰۶/۰۱) — یک اعلان CSS.**
>
>    ریشه: React در `<ViewTransition>` آزمایشی، محوشدن پیش‌فرضِ ریشه را با دو انیمیشنِ اسکریپتی خاموش می‌کند، و دومی همه‌چیز را می‌کشد:
>
>        ::view-transition-group(root)  →  { opacity: 0; pointer-events: none }
>        ::view-transition              →  { width: 0px; height: 0px }
>
>    `::view-transition` **ظرفِ اسنپ‌شات** است؛ صفر که شود، گروه‌های نام‌دار داخلش هم با آن می‌روند. برای همین ترنزیشن تا آخر اجرا می‌شد، هندسه‌اش درست بود، و یک پیکسل هم نقاشی نمی‌شد. درمان، سه خط در `globals.css`: `::view-transition { width: 100% !important; height: 100% !important }` — انیمیشن اسکریپتی با هیچ قاعده‌ی عادی گرفته نمی‌شود، ولی آبشار، اعلانِ `!important` نویسنده را **بالاتر** از انیمیشن می‌نشاند.
>
>    **چطور پیدا شد، تا کسی دوباره درنیاوردش:** همان DOM، منجمد وسط راه، دو بار اندازه‌گیری شد. از راه ناوبری `::view-transition` صفر در صفر بود؛ از راه یک ترنزیشنِ دستی روی همان صفحه، اندازه‌ی ویوپورت. باقی همه‌چیز — نام‌ها، هندسه، شفافیت‌ها، حالت ترکیب — یکسان و درست بود. `group(root)` عمداً دست‌نخورده ماند: پنهان‌کردن محوشدنِ ریشه همان رفتاری است که می‌خواهیم.
>
>    **یازده فرضیه‌ای که رد شد** (وقتشان را دوباره نگیرید): کاهش حرکت؛ پشتیبانی مرورگر؛ باندل کهنه؛ `loading.tsx`؛ المان اضافه‌ی `<ViewTransition>`؛ نحو سلکتور CSS؛ بازنویسی‌های `block-size`/`object-fit` خودمان؛ سند مخفی؛ بارگذاری‌نشدن عکس مقصد؛ کش سرد روتر؛ و `transform`/`filter`/`contain` روی هر نیای کارت.
> 2. **توالی ورود صفحه** — **بخش بالای‌تای صفحه بسته شد (۱۴۰۵/۰۶/۰۱)؛ زیرِ تا باز است.**
>    - **ساخته شد:** `.toranjan-rise` در `globals.css` — همان قرارداد `--stagger` که `.toranjan-row` از قبل داشت، با `--dur-entrance: 700ms` (سرِ کندِ باند بند ۳-۵؛ پنل عمداً سرِ تندش را می‌زند). روی سرآیند `/carpets` نشست: برچسب، تیتر، جمله — ۷۰ میلی‌ثانیه فاصله. اندازه‌گیری‌شده و منجمد وسط راه: شفافیت ۰٫۸۶ / ۰٫۷۳ / ۰٫۴۴.
>    - **CSS خالص، نه framer.** این بلوک‌ها سرور-رندرند و در نخستین نقاشی روی صفحه‌اند؛ و همین سند دو دلیل ثبت کرده که چرا جاوااسکریپت ابزار غلطی برایشان است — framer استایل اینلاین می‌نویسد پس بلوک `prefers-reduced-motion` به آن نمی‌رسد، و شاخه‌زدن روی `useReducedMotion()` برای تعیین `initial` سرور و مرورگر را دو جواب می‌دهد. کی‌فریم هیچ‌کدام را ندارد و باندل هم نمی‌گیرد. تأخیرِ پله زیر کاهش حرکت جداگانه صفر می‌شود، چون قاعده‌ی فراگیر فقط مدت را جمع می‌کند نه تأخیر را.
>    - **🔑 و یک چیز که با نگاه کردن معلوم شد و کد نمی‌گفت: روی صفحه‌ی خانه اعمالش نکردیم.** بخش «برای کدام اتاق؟» **زیر تای صفحه** است و تا وقتی ورودی سینمایی پخش می‌شود پشت آن می‌ماند — انیمیشنِ لود اجرا می‌شد، تمام می‌شد، و بازدیدکننده هیچ‌وقت نمی‌دیدش. زیرِ تا **تریگر اسکرول** می‌خواهد، همان چیزی که کارت فرش از قبل دارد. اعمال شد، دیده شد، و برگردانده شد.
>
>    **این بند نصفش کهنه بود و ۱۴۰۵/۰۶/۰۱ اصلاح شد.** کارت فرش از قبل ورود پلکانی دارد (`whileInView`، ۴۲۰ms، پله‌ی ۳۰ms با سقف ۴۲۰ms، در `carpet-card.tsx`)، و سیزده سطح دیگر فروشگاه هم حرکت دارند. آنچه واقعاً ندارد **بلوک‌های بالای صفحه** است: تیتر و مقدمه‌ی `/carpets` و بخش‌های صفحه‌ی خانه. پیش از ساختن، همان‌جا را بشمارید — نه کل فروشگاه را.
>    - `admin-motion.tsx` عمداً سرِ **تندِ** باند ۴۰۰–۹۰۰ms را می‌زند (۵۰۰ms) چون کسی تمام روز داخلش کار می‌کند. فروشگاه سرِ **کندش** را می‌خواهد، و آن عدد از قبل در CSS هست و هیچ‌جای JS استفاده نشده: `--dur-entrance: 700ms`.
> 3. ~~**رانش پارالاکس داخل قاب عکس.**~~ **✅ ساخته شد (۱۴۰۵/۰۶/۰۱) — و با این، هر چهار شرطِ پذیرشِ جهت بصری بسته شد.**
>    - **اسکرول‌محور با CSS، بدون هیچ شنونده‌ی اسکرول.** `animation-timeline: view()` پیشرفت را به سفر خود المان در کادر دید می‌بندد و کرومیوم آن را خارج از نخ اصلی اجرا می‌کند؛ ۲۴ کارت تقریباً هزینه‌ی یک هندلر `scroll` را دارند. `@supports` گاردش می‌کند چون بازگشتِ نبودِ پشتیبانی «بدون پارالاکس» نیست، **بدتر** است: بدون تایم‌لاین همان انیمیشن روی تایم‌لاین سند یک بار موقع لود اجرا می‌شود و هر عکس یک تکان می‌خورد و می‌ایستد.
>    - **🔑 دو دامی که هر دو با اندازه‌گیری پیدا شدند، نه با خواندن کد:**
>      1. **شورت‌هندِ `animation` مدت را `0s` می‌گذارد.** انیمیشن اسکرول‌محور `auto` می‌خواهد، یعنی «کل بازه را پر کن». با شورت‌هند همه‌چیز معتبر گزارش می‌شد — `scale` درست، تایم‌لاین درست — و ترنسفورم در هر چهار موقعیت اسکرول همانی بود. فقط لانگ‌هند می‌تواند `auto` بگوید.
>      2. **`overflow: hidden` خودش یک اسکرول‌کانتینر است.** قاب عکس برش می‌زند، پس تایم‌لاینی که *داخل* آن بنشیند، لایه را در برابر جعبه‌ای می‌سنجد که دقیقاً پرش کرده و هرگز ترکش نمی‌کند: `currentTime` روی ۵۰٪ قفل، در هر اسکرولی. همان تایم‌لاین روی خودِ کارت — بیرون از برش — روی همان چهار موقعیت ۱۴٫۶٪ → ۳۸٫۳٪ → ۶۲٫۳٪ → ۸۶٫۲٪ می‌دهد.
>    - **راه‌حل: کارت تایم‌لاین را می‌برد و یک متغیر را انیمیت می‌کند؛ لایه‌ی بریده‌شده متغیر را می‌خواند.** یک custom property ثبت‌شده (`@property --drift`) از مرز برش رد می‌شود، جایی که تایم‌لاین نمی‌تواند.
>    - **فقط روی لایه‌ای که `object-cover` است**، یعنی عکس اتاق. آن‌جا برش از قبل در جریان است و رانش، بیشترِ همان عکس را نشان می‌دهد؛ روی فرشِ `object-contain` که لبه‌به‌لبه بریده شده، لغزاندنش داخل حاشیه‌ی خودش پارالاکس نیست، عکسی است که سر جایش نمی‌ماند.
>    - اندازه‌گیری‌شده: ‎−۷٫۳ تا ‎+۸ پیکسل روی قاب ۳۵۸ پیکسلی، `scale: 1.08` که ۴٪ سرریز در هر طرف می‌دهد و رانش ۳٪ می‌خواهد.
>
> **جهت بصری عوض نمی‌شود.** نمونه‌ی ارجاع‌شده برند پرصدا با بلوک رنگی اشباع است و «گالری موزه‌ای» از میان چهار جهتِ **ساخته‌شده** و با دیدن انتخاب شد، نه با استدلال — پس بازکردنش هزینه‌ی همان تصمیم را دوباره می‌دهد. آن‌چه کم است حرکت است، نه رنگ.
>
> ### ترنزیشن اشتراکی — کجا ماند (۱۴۰۵/۰۵/۲۳)
>
> **وضعیت: سیم‌کشی کامل، حرکت نامرئی.** کد روی `main` است، چیزی را خراب نمی‌کند، و ناوبری مثل قبل کار می‌کند — فقط آن مورفی که قرار بود دیده شود، دیده نمی‌شود. مسیر همان بود که این سند گفته بود: `View Transitions API`، از راه `<ViewTransition>` خودِ React.
>
> - **کلید فعال‌سازی:** `experimental.viewTransition` در `next.config.ts`. بدون آن React هرگز `startViewTransition` را صدا نمی‌زند و هر `view-transition-name` دستی بی‌اثر می‌ماند. **کانال آزمایشی React را وارد پروژه نمی‌کند** — فهرست `needsExperimentalReact` در Next فقط `taint` و `transitionIndicator` و `gestureTransition` است، و خود کامپوننت از بیلد پایدار همین نسخه صادر می‌شود.
> - **نام‌گذاری از slug** (`lib/view-transition.ts`): دو سر ترنزیشن هیچ‌وقت از هم خبر ندارند و تنها روی «کدام فرش» توافق می‌کنند. نام باید در لحظه‌ی عکس‌برداری در کل سند **یکتا** باشد؛ نام تکراری کل ترنزیشن را بی‌صدا می‌اندازد، و امروز فقط قاب کارت و قاب صفحه‌ی محصول نام دارند.
> - **دو نسبت متفاوت** (۳:۴ در شبکه، ۴:۵ در صفحه): بدون `object-fit: contain` روی دو شبه‌المان، بافت فرش در راه کش می‌آمد. قاب عوض می‌شود، نسبت عکس نه.
> - **زمان‌بندی:** توکن تازه‌ی `--dur-morph: 520ms` با منحنی `--ease` برای خود عکس، و ۳۸۰ms برای محو شدن بقیه‌ی صفحه (`root`) — صفحه‌ی تازه باید تا پایان سفر عکس خوانا باشد، نه بعد از آن.
> - **`prefers-reduced-motion` جداگانه پاسخ داده شد.** قاعده‌ی فراگیر بالای `globals.css` به این‌جا نمی‌رسد: شبه‌المان‌های ویو ترنزیشن درختی جدا از ریشه‌اند و `*` داخلشان را نمی‌گیرد.
>
> **یک محدودیت شناخته‌شده:** ناوبری **برگشت** (دکمه‌ی back و پاپ‌استیت) ترنزیشن ندارد — اندازه‌گیری شد: در رفت `startViewTransition` یک‌بار صدا زده می‌شود و در برگشت هیچ‌بار. رفتن مورف می‌شود، برگشتن آنی است. اگر بعداً ارزشش را داشت، جای کار همان‌جاست.
>
> #### آن‌چه اندازه‌گیری شد — از این‌جا ادامه دهید، نه از صفر
>
> همه‌ی این‌ها با اجرای واقعی سنجیده شده‌اند، نه فرض:
>
> - `startViewTransition` در هر ناوبریِ رفت **یک‌بار** صدا زده می‌شود. پس فلگ زنده است و React کارش را می‌کند.
> - قاب کارت در صفحه‌ی مبدأ و قاب گالری در صفحه‌ی مقصد **هر دو** همان نام را می‌گیرند، با کلاس `carpet-photo`، و هر نام **دقیقاً یک‌بار** در سند هست.
> - هر هشت قاعده‌ی CSS در مرورگر زنده‌اند (از CSSOM خوانده شد)، و سه‌تای `animation: none` واقعاً داخل مدیاکوئری کاهش حرکت‌اند.
> - در کروم ۱۴۸ کارفرما: `prefers-reduced-motion` خاموش است، `transition.ready` با موفقیت resolve می‌شود، و `document.getAnimations()` انیمیشن‌های واقعی با مدت‌های خودِ ما گزارش می‌کند — `[1000, 2000, 2000, 2000, 2000, 520, 380, 520, 520, 520, 520, 0, 0]`. **و با این حال روی صفحه هیچ حرکتی دیده نمی‌شود؛ صفحه آنی عوض می‌شود.**
>
> **رد شد (وقت دوباره‌شان را نگیرید):** کاهش حرکت؛ نبودن پشتیبانی مرورگر؛ باندل قدیمی (هارد رفرش شد)؛ اسکلت `loading.tsx` وسط راه (این مسیر اصلاً `loading.tsx` ندارد)؛ و اینکه `<ViewTransition>` المان اضافه کند (نمی‌کند — قاب مستقیم خودش نام می‌گیرد). **افزوده‌ی ۱۴۰۵/۰۶/۰۱:** نحوِ سلکتورِ CSS هم رد شد — گمان رفت `::view-transition-group(.carpet-photo)` بخشِ نامِ اجباری را ندارد و بی‌صدا دور ریخته می‌شود، ولی از خودِ پارسر مرورگر پرسیده شد و هر پنج شکل نگه داشته می‌شوند؛ کروم `.carpet-photo` را به `*.carpet-photo` نرمال می‌کند. **قاعده‌ها سالم‌اند.**

> **🔑 دامی که وقت می‌گیرد و باگ نیست (۱۴۰۵/۰۶/۰۱):** اگر این را از پنجره‌ی مرورگرِ بسته دیباگ کنید، `transition.ready` با `InvalidStateError: Transition was aborted because of invalid state` رد می‌شود و به نظر می‌رسد ریشه را پیدا کرده‌اید. نیست: `document.visibilityState` برابر `hidden` است و مرورگر در سند نمایش‌داده‌نشده اصلاً ویو‌ترنزیشن اجرا نمی‌کند. **این باگ فقط با پنجره‌ی مرورگرِ باز قابل بازتولید است.** ← [[browser-pane-hidden-quirks]]
>
> **دو نشانه‌ی مشکوک در همان اعداد:** **پنج** انیمیشن ۵۲۰ms در حالی که برای یک گروهِ نام‌دار سه تا انتظار می‌رود (`group`/`old`/`new`)، و **فقط یک** انیمیشن ۳۸۰ms در حالی که `old(root)` و `new(root)` باید دو تا باشند. یعنی احتمالاً یا بیش از یک گروه نام گرفته، یا اسنپ‌شات `root` ساخته نشده.
>
> ### پیشرفت ۱۴۰۵/۰۶/۰۱ — یک باگ بسته شد، یک باگ دقیق‌تر شد
>
> **باگ اول، بسته شد: منحنی شتاب.** ترنزیشن نامرئی نبود، **پیش از دیده شدن تمام می‌شد.** با نمونه‌برداری از عرض گروه در طول تایم‌لاین خودش: عکس از ۲۴۰ به ۵۰۴ پیکسل می‌رود و با `--ease` در **۱۰٪ زمان، ۵۰٪ مسیر** و در نیمه‌ی راه ۹۷٪ را رفته بود — یعنی کل سفر داخل ۵۲ میلی‌ثانیه‌ی اول یک انیمیشن ۵۲۰ میلی‌ثانیه‌ای. توکن تازه‌ی `--ease-morph` ساخته شد (در ۱۰٪ زمان ۴٪ مسیر، در نیمه ۷۹٪) و `--ease` دست نخورد چون شعاع انفجارش کل سایت است.
>
> **کلید تشخیص: سوییچ اسلوموشن.** `:root[data-motion="slow"]` کل حرکت را شش برابر می‌کند و `--dur-page-fade` هم به همین دلیل از عدد ثابت به توکن تبدیل شد. بدون آن هیچ اسکرین‌شاتی به ترنزیشن نمی‌رسد. برای دیباگ: `a.playbackRate = 0.00005` روی انیمیشن‌های شبه‌المان، ترنزیشن را عملاً منجمد نگه می‌دارد.
>
> **باگ دوم، هنوز باز — ولی حالا محاصره شده. اسنپ‌شات‌ها نقاشی نمی‌شوند.**
>
> **🔑 آزمون کنترل که همه‌چیز را جدا کرد:** یک ترنزیشن دستیِ **درون همان صفحه** ساخته شد (یک جعبه‌ی رنگی که جابه‌جا و بزرگ می‌شود، با `document.startViewTransition` خام). **دیده می‌شود** — وسط راه، با رنگ ترکیبی و اندازه‌ی میانی. پس ویو‌ترنزیشن در این مرورگر و این پنجره درست کامپوزیت می‌شود و اسکرین‌شات هم می‌گیردش. **مشکل در اپ است، نه در محیط.**
>
> **دو تفاوت ساختاری بین آن ترنزیشنِ سالم و ناوبری اپ، اندازه‌گیری‌شده:**
> - ترنزیشن دستی هم `::view-transition-new(root)` دارد هم `old(root)`. **ناوبری اپ `new(root)` را اصلاً نمی‌سازد.**
> - در ناوبری اپ `::view-transition-group(root)` شفافیت **صفر** دارد.
>
> **و آنچه سالم است (پس دنبالش نگردید):** گروه عکس هندسه‌ی درست دارد و سر جای درست است (وسط مورف: ۴۱۹×۵۳۱ در `translate(573, 331)`)، `old(carpet-photo)` شفافیت واقعی دارد (۰٫۳۲)، `image-pair` درست `isolate` است و بلندمود `plus-lighter`.
>
> **دو فرضیه‌ی رد‌شده — وقت دوباره‌شان را نگیرید:**
> - **CSS خودمان مقصر نیست.** قواعد `block-size/inline-size/object-fit` با پیش‌فرض مرورگر جایگزین شدند و رفتار **مو‌به‌مو یکسان** ماند.
> - **پیش‌بارگذاری عکس مقصد مشکل را حل نمی‌کند.** ابتدا این فرضیه‌ی قوی مطرح شد که کارت و صفحه‌ی محصول دو **فایل متفاوت** می‌خواهند (`/files/card/ef49ca1a…` در برابر `/files/full/20ea684c…`) و مقصد هنگام عکس‌برداری هنوز عکسی ندارد. فایل مقصد **کامل پیش‌بارگذاری شد** (`naturalWidth: 640`) و بعد ناوبری شد: قاب باز هم خالی است. تفاوت دو فایل واقعی است و ارزش رسیدگی دارد، ولی **علت این باگ نیست.**
>
> **قدم بعدی، مشخص:** هر دو تفاوت ساختاری بالا مخصوص مسیر ناوبریِ React/Next است، نه CSS ما و نه عکس‌ها. آزمون بعدی: همان دو صفحه را با `document.startViewTransition` **خام و بدون React** جابه‌جا کنید (مثلاً با `router.push` داخل کال‌بک، یا یک ناوبری دستی). اگر آن‌جا مورف دیده شد، مقصر یکپارچگی `<ViewTransition>` ری‌اکت است و جای اصلاح همان‌جاست.

>
> **جدا از این، یک بدهی کوچک که همان‌جا پیدا شد:** در صفحه‌ی محصول، `next/image` با `fill` اخطار می‌دهد چون والدِ مستقیم عکس `div[data-rmiz-content]` است و `position: static` دارد؛ قاعده‌ی ما در `globals.css` یک لایه بالاتر (`[data-rmiz]`) را گرفته. الان چیزی را خراب نکرده ولی درست نیست.
>
> ### چرا فاز ۳ باز ماند و رفتیم فاز ۴ (تصمیم کارفرما، ۱۴۰۵/۰۵/۲۳)
>
> سه مورد باقی‌مانده‌ی لایه‌ی حرکت عمداً باز گذاشته شدند — از جمله اشکالِ نامرئی‌بودنِ ترنزیشن اشتراکی، که وسط ردیابی رها شد و هرچه پیدا شده بود بالاتر ثبت است — و ترتیب کار به فاز ۴ رفت. **این کم‌کردن دامنه نیست؛ عوض‌کردن ترتیب است** — هر دو مورد سر جایشان در همین سند می‌مانند تا بعد از فاز ۴ بسته شوند.
>
> **چرا این ترتیب بهتر است:** لایه‌ی حرکت یک **پاس روی کل فروشگاه** است، و فاز ۴ سه سطح تازه به فروشگاه اضافه می‌کند (جست‌وجوی بصری، مشاور چیدمان، جست‌وجوی گفتگویی). حرکت اگر حالا تمام شود، آن سه سطح خشک متولد می‌شوند و پاس دوم لازم می‌شود؛ برعکسش یک پاس همه را می‌گیرد. به‌علاوه فاز ۴ تکه‌ی اصلی پایان‌نامه است و اعداد ارزیابی فاز ۵ از دلش درمی‌آیند، در حالی که لایه‌ی حرکت جلوی هیچ کار دیگری را نگرفته.
>
> **هزینه‌اش، صریح:** شرط پذیرش جهت بصری تا آن موقع برآورده‌نشده می‌ماند. یعنی هر دمویی که این وسط داده شود همان بازخورد «حرکت و انیمیشن کم است» را می‌گیرد. این پذیرفته شد.
>
> **یک نکته برای ساختِ فاز ۴:** صفحه‌ی نتایج جست‌وجوی بصری کارت فرش دارد، پس ترنزیشن اشتراکی را مجانی به ارث می‌برد — به شرطی که یک فرش دوبار در یک صفحه نام نگیرد. قاعده‌اش در سرصفحه‌ی [lib/view-transition.ts](frontend/src/lib/view-transition.ts) نوشته است.
>
> ### سؤال باز: قلم فارسی نمایشی
>
> کارفرما گفت «فونت‌های فارسی زیاد جالب نیست». نمونه‌ی ارجاع‌شده از **انجمن (Anjoman)** استفاده می‌کند، وزن ۴۰۰ تا ۷۰۰، تیتر در ۴۸ پیکسل — اندازه‌گیری‌شده روی خود سایت.
>
> فعلاً وزیرمتن است (وزن خطوط ورودی از ۲۰۰ به ۵۰۰ رفت، چون ۲۰۰ روی فیلم متحرک کم‌نور خوانده می‌شد). اگر قلم تازه‌ای امتحان شد — استعداد، مربا، انجمن — **اول لایسنس، بعد دانلود.** درس بدهی ۳ همین بود و ارزان به دست نیامد.
>
> ### دو بدهی که به کارفرما گره خورده‌اند
>
> - **راه‌های تماس** (بدهی ۸): چهار ثابت خالی در `lib/content/contact.ts`.
> - **رکوردهای آزمایشی کاتالوگ** (بدهی ۵): یکی از ۷۴ فرش اسمش `unluckiest` است. پیش از فصل داده باید پاک شوند و شمارش نهایی ثبت شود.
>
> ### آیتم ۱۶ — بازبینی AR: چه شد (۱۴۰۵/۰۵/۲۰)
>
> دست‌ساز، همان‌طور که نقشه از اول گفته بود؛ **هیچ سهمیه‌ی ۲۱st خرج نشد و هیچ کار بک‌اندی لازم نداشت** — هر سه اندپوینت از فاز ۲ آماده بودند و لایه‌ی کلاینتشان (`getArStatus`/`suggestCorners`/`generateArAssets`) هم از قبل نوشته شده بود و فقط رابط نداشت.
>
> - **`/admin/ar`** — صف رسیدگی. دو تب («رسیدگی می‌خواهد» / «همه») با شمارش، جست‌وجو، مرتب‌سازی «عقب‌مانده‌ترین اول»، صفحه‌بندی. حالت (state) در URL است، مثل بقیه.
> - **`/admin/ar/{id}`** — عکس اصلی با چهار دستگیره، هشدار `needs_review`، وضعیت هر سایز با لینک `glb`/`usdz` و متن خطا، و دو مسیر بازسازی.
>
> **نگاشت مختصات هیچ ضریب مقیاسی نگه نمی‌دارد، و این عمدی است.** ضریب ذخیره‌شده نسخه‌ی دومی از حقیقت است که با هر تغییر اندازه بیات می‌شود. به‌جایش: *کشیدن* با درصد (`x / imageWidth`) که مرورگر پیوسته حسابش می‌کند، و *خواندن اشاره‌گر* با `getBoundingClientRect()` در همان لحظه‌ی حرکت. تنها جایی که ضریب لازم است، تنها جایی است که نمی‌تواند کهنه باشد.
>
> **اندازه‌گیری‌شده روی ۳۷۵ و ۱۰۲۴ و ۱۲۸۰:** خطای هر چهار گوشه صفر پیکسلِ مبدأ، قاب دقیقاً برابر جعبه‌ی عکس، ناحیه‌ی لمس ۴۴×۴۴، بدون سرریز افقی، و کشیدن دقیقاً همان‌جا می‌نشیند که اشاره‌گر بود. کیبورد روی صفحه‌ی راست‌به‌چپ: ArrowLeft به چپ، Shift گام بزرگ، و ناحیه‌ی `aria-live` موقعیت را می‌گوید.
>
> **هر دو مسیر بازسازی روی داده‌ی واقعی آزموده شدند:** مسیر با گوشه‌ها درجا `ready` داد و وضعیت در دیتابیس نشست؛ مسیر بدون گوشه روی فرشی با ۱۱ سایز `missing → processing → ready` را نشان داد و نظرسنجی خودش ایستاد. **حین همین آزمون برای دو فرش (شناسه‌های ۴۶ و ۷۳) فایل AR واقعی ساخته شد** — یعنی «رسیدگی می‌خواهد» از ۷۳ به ۷۱ رسید. داده‌ی نمایشی است و ضرری ندارد، ولی اگر عدد جای دیگری نقل شد، علتش این است.
>
> **سه گزارش از استفاده‌ی واقعی، همان روز (۱۴۰۵/۰۵/۲۰):** انگشت روی نقطه‌ای می‌افتد که باید دیده شود (**ذره‌بین** اضافه شد)، کلیدهای جهت روی دسکتاپ کار نمی‌کردند (**`preventDefault` فوکوس را می‌خورد**)، و دکمه‌ی «در خانه‌ی من ببین» روی گوشی خراب بود (**`flex-1` در ستون**). هر سه بالاتر در «قواعد» ثبت شده‌اند. هیچ‌کدام را اندازه‌گیری من پیدا نکرد؛ هر سه از دست گرفتن محصول آمدند.
>
> **روی گوشی واقعی آزموده شد و کار می‌کند (۱۴۰۵/۰۵/۲۰).** کشیدن با انگشت و ذره‌بین هر دو تأیید شدند — یعنی `PointerEvent` به‌جای دو مسیر جدا، و `touch-action: none` روی خودِ دستگیره، همان کاری را کردند که از آن‌ها انتظار می‌رفت. **این آیتم دیگر بند بازی ندارد.**
>
> ### ورودی سینمایی — چه ساخته شد (۱۴۰۵/۰۵/۲۰)
>
> نمونه‌ی `docs/prototypes/intro/chaptered.html` به محصول تبدیل شد. چهار فایل در [components/toranjan/intro/](frontend/src/components/toranjan/intro/):
>
> | فایل | کار |
> |---|---|
> | `intro-engine.ts` | خودِ فیلم. **هیچ state ری‌اکتی ندارد** — دنباله‌ی فریم شصت بار در ثانیه ده‌ها مقدار را عوض می‌کند و گذاشتنش در `useState` یعنی رندر کل درخت برای عددهایی که هیچ‌چیز رندرشان نمی‌کند. ری‌اکت مالک «کِی وجود دارد» است، این مالک «چه می‌کند». |
> | `intro-gate.ts` | آیا اصلاً پخش شود. `prefers-reduced-motion` → نه. `?intro=1` → بله. `?intro=0` → نه. **در حالت توسعه همیشه بله.** وگرنه: نیم ساعت از آخرین دیدن. |
> | `intro-sequence.tsx` | پوسته و **راه خروج** (بدهی ۴). |
> | `intro-curtain.tsx` | درز میان فیلم و فروشگاه. |
>
> **مدت «دیده شده» از ۲۴ ساعت به نیم ساعت آمد.** بریف ۲۴ ساعت گفته بود و در عمل استراحت نبود، بازنشستگی بود: کارفرما همان بعدازظهر برگشت و ورودی رفته بود. کاری که این قاعده باید بکند، مزاحم نشدن وسط یک بازدید است — نه حذف لحظه‌ی برند از عمر کاربر.
>
> **قاب‌ها:** با همان اسکریپت موجود از فیلم `assets/` بازتولید می‌شوند و در `frontend/public/intro/` می‌نشینند — **گیت‌ایگنور، مثل بقیه‌ی `assets/`**. اسکریپت حالا در پایان **هرس** هم می‌کند: در حالت هیبرید فقط پنج فریمِ توقف از ست‌های `avif-*` خوانده می‌شود و ۱۳۲ تای دیگر از `motion-*`، و `webp-1920` (بزرگ‌ترین پوشه) برای مرورگری بود که AVIF ندارد و یک عرض کفایتش می‌کند. **۸۲۲ فایل / ۶۱ مگابایت → ۴۲۱ فایل / ۲۰٫۹ مگابایت.** شماره‌ی فریم‌های توقف در هر دو جا (اسکریپت و موتور) اعلام شده، چون هرسی که با پخش‌کننده اختلاف داشته باشد فریمی را پاک می‌کند که بعداً خودش می‌خواهد.
>
> **ورود به فروشگاه — ایده‌ی کارفرما، و بهتر از نسخه‌ی اول بود.** صفحه مثل یک **ورقه با گوشه‌های گرد** از پایین می‌آید بالا روی آخرین فریم، با زمینه‌ی خودش و سایه‌ای رو به بالا؛ گوشه‌ها هنگام نشستن صاف می‌شوند، چون صفحه‌ای که برای همیشه گوشه‌ی گرد داشته باشد لحظه‌ای که اسکرول شود پشتش خالی است.
>
> - **باگی که کارفرما گرفت و واقعی بود:** فیلم لایه‌ی `fixed` با `z-index: 100` است و صفحه هیچ z-index نداشت — پس **تمام ۹۰۰ میلی‌ثانیه از زیر فیلم بالا می‌آمد و دیده نمی‌شد**، بعد یک‌باره ظاهر می‌شد. یعنی تنها لحظه‌ای که کل ورودی برای تولیدش وجود دارد، هرگز روی صفحه نبود. حالا ۱۱۰ در برابر ۱۰۰.
> - **فیلم باید ۹۰۰ میلی‌ثانیه از خودش عمر بیشتر کند** (حالت `rising`)، وگرنه ورقه روی مستطیل خالی بالا می‌آید. در همان پنجره پوسته دیگر مودال نیست و فوکوس را رها می‌کند.
> - **`overflow: hidden` فقط در همان پنجره زندگی می‌کند** و بعدش برداشته می‌شود — همان چیزی است که `sticky` را می‌کشد و این مخزن دو بار بابتش هزینه داده.
>
> **متن روی صحنه‌ها:** چهار خط روی صحنه‌های ۱ تا ۴ که مسیر «ساختن» را می‌روند نه «فروختن» (تارهای خالی، هزاران گره، پیدا شدن نقش، ساعت‌ها در هر سانتی‌متر). صحنه‌ی پنجم **عمداً بی‌متن** است؛ آن‌جا جای وردمارک است و شعاری هم‌زمان با نام برند هر دو را ضعیف می‌کند. متن صفحه‌خوان **تصویر** را توصیف می‌کند نه شعار را: کسی که دار قالی را نمی‌بیند، با «هر فرش از چند تار خالی آغاز می‌شود» چیزی دستگیرش نمی‌شود.
>
> **اندازه‌گیری‌شده:** پنج صحنه با کلید جهت جلو می‌روند و اعلام می‌شوند، پرده بالا می‌آید، اسکرول برمی‌گردد، بازدید ثبت می‌شود، `Escape` هر لحظه بیرون می‌برد، بازدید دوم زیر نیم ساعت با **صفر درخواست فریم** رد می‌شود، و **۶۳۴ کیلوبایت** تا نخستین حرکت در برابر هدف ۷۰۰ بریف. دکمه‌ی «پخش دوباره‌ی مقدمه» در فوتر، ۱۶۲×۴۴.
>
> **باقی‌مانده‌ی صادقانه:** ورودی روی **۳۷۵ و ۱۰۲۴ اندازه‌گیری نشده** (فقط ۱۲۸۰)، و **با انگشت روی گوشی واقعی لمس نشده** — سوایپ بین صحنه‌ها نوشته شده ولی آزموده نشده.
>
> ### «قابل نصب» تا فاز ۵ قابل تأیید نیست، و این نقص کار نیست (۱۴۰۵/۰۵/۱۶)
>
> منیفست و آیکون‌ها ساخته شدند و از همه‌ی شرط‌های کروم رد می‌شوند — روی `localhost` اندازه‌گیری شد: `name`، `short_name`، `start_url`، `display: standalone`، و آیکون ۱۹۲ و ۵۱۲ با `purpose: any` (منیفستی که فقط آیکون `maskable` دارد رد می‌شود).
>
> **ولی نصب واقعی به HTTPS گره خورده و هیچ راهی دورش نیست.** روی گوشی با `http://<آی‌پی لپ‌تاپ>:3000`، `isSecureContext` نادرست است؛ کروم آن‌جا «نصب برنامه» را اصلاً پیشنهاد نمی‌دهد و فقط «افزودن به صفحه‌ی اصلی» می‌دهد، که یک **بوکمارک با آیکون** است: آیکون و نام درست می‌آیند، ولی نوار آدرس سرجایش می‌ماند و حس اپ نمی‌دهد. این گزارش از کارفرما آمد و علتش همین بود، نه ایرادی در منیفست.
>
> - **برای آزمایش پیش از استقرار:** روی گوشی `chrome://flags/#unsafely-treat-insecure-origin-as-secure` را Enabled کنید و همان مبدأ را در کادرش بنویسید، بعد Relaunch. شورتکات قبلی باید اول پاک شود. نشانه‌ی درست بودن، عوض شدن جمله‌ی منو به **«نصب برنامه»** است.
> - **سرویس‌ورکر عمداً ساخته نشد، و دلیلش عوض شد.** دلیل اولیه «کش کردن قیمت» بود؛ دلیل واقعی محکم‌تر است: **ثبت سرویس‌ورکر خودش secure context می‌خواهد**، پس روی HTTP شبکه‌ی محلی اصلاً register نمی‌شود و ساختنش یعنی ساختن چیزی که تا فاز ۵ آزمایش‌شدنی نیست.
> - **بدون سرویس‌ورکر، کروم خودش پیشنهاد نصب نمی‌دهد.** شرط اجباری بودنِ `fetch handler` برای نصب از منو در کروم ۱۰۸ موبایل برداشته شد، ولی الگوریتمِ *پیشنهاد خودکار* هنوز آن را می‌خواهد — تأیید شد: روی `localhost` رویداد `beforeinstallprompt` شلیک نشد. یعنی نصب از منو کار می‌کند و بنر خودکار نه.
> - **جای درستش فاز ۵ است**، کنار Caddy و دامنه: آن‌جا هم HTTPS هست، هم بار اول است که این ادعا واقعاً سنجیدنی می‌شود. اگر سرویس‌ورکر اضافه شد، فقط ناوبری و دارایی‌های اثرانگشت‌دار — **هرگز پاسخ‌های API**، چون قیمت و موجودی کهنه بدتر از نبودِ آفلاین است.
>
> ### کجای کل پروژه ایستاده‌ایم (۱۴۰۵/۰۶/۰۱) — صادقانه
>
> این بخش عمداً خوش‌بینانه نوشته نشده. بند ۸ می‌گوید «هیچ قابلیتی به‌خاطر تقویم حذف نمی‌شود»، و این یعنی تصویر واقعی از حجم باقی‌مانده باید در دسترس باشد تا تصمیم‌ها آگاهانه گرفته شوند — نه اینکه دامنه بی‌سروصدا آب برود.
>
> | فاز | وضعیت |
> |---|---|
> | ۰ پایه + اسپایک AR | ✅ |
> | ۱ بک‌اند کاتالوگ و سفارش | ✅ |
> | ۲ پایپ‌لاین AR + جای‌گذاری | ✅ — **یک کار باز:** اندازه‌گیری خطای مقیاس با متر روی هر دو گوشی. (Quick Look روی آیفون در ۱۴۰۵/۰۵/۲۰ آزموده شد و کار می‌کند.) |
> | ۳ فروشگاه ۰ تا ۱۰۰ | ✅ **(۱۴۰۵/۰۶/۰۱)** — با بسته شدن لایه‌ی حرکت. یک باقی‌ماندهٔ کوچک و نام‌دار: توالی ورودِ بلوک‌های زیرِ تای صفحه‌ی خانه. |
> | ۴ قابلیت‌های هوشمند | ✅ — هر چهار قابلیت ساخته و یکپارچه شدند |
> | ۴٫۵ کاتالوگ از آنِ خودمان | ✅ — ۴۰ فرش، ۱۶۴ تصویر، ۱۵۱ واریانت AR |
> | ۵ امنیت، تست، استقرار، ارزیابی | 🔶 **~۷۵٪** — ارزیابی، سخت‌سازی، E2E و CI ✅؛ مانده: استقرار واقعی، بیلد داکر، متر AR — **هر سه بیرون از کد** |
> | ۶ پایان‌نامه و دفاع | ⬜ **~۲۰٪** — ۷ فصل از ۱۰ نوشته نشده. **بزرگ‌ترین تکه‌ی باقی‌مانده** |
>
> **فاز ۵ سه قلم باقی دارد و هیچ‌کدام کدنویسی نیست:** استقرار Compose/Caddy روی VPS با دامنه (بند ۱۲)، یک بیلد لوکال ایمیج‌های داکر، و اندازه‌گیری خطای مقیاس AR با متر روی دو گوشی. هر سه منتظر کارفرمایند. بدون اولی ادعای «قابل نصب» و بدون سومی ادعای «مقیاس واقعی روی دستگاه» اثبات‌نشده می‌مانند.
>
> **جایی که وزن کار جابه‌جا شده:** تا امروز بدنه‌ی کد سنگین‌تر از متن بود؛ حالا برعکس است. بک‌اند، پایپ‌لاین AR، کل فروشگاه، کل پنل، هر چهار قابلیت هوشمند و اعداد ارزیابی وجود دارند — و **پایان‌نامه هنوز هفت فصل بدهکار است** در حالی که اعداد و تصمیم‌هایش همه ثبت‌شده‌اند. یعنی کار باقی‌مانده بیشترش نوشتن است تا ساختن، و به هیچ بلاک‌کننده‌ای هم وابسته نیست.
>
> **به‌علاوه‌ی بند ۱۴:** هفت باگ و کار که کارفرما در ۱۴۰۵/۰۶/۰۱ گزارش کرد و پیش از دفاع باید بسته شوند. دوتاشان (تسویه و قفل‌شدن پس از لاک) از جنسی‌اند که دمو را می‌کُشند.
>
> ### بدهی‌های باز (پیش از تیک خوردن فاز ۳)
>
> ۱. **فیلتر رنگ** — رنگ‌های غالب هگز دقیق‌اند و هر سطل یک فرش دارد؛ باید هنگام ingest به خانواده‌های نامی خوشه‌بندی شوند. شرح کامل پایین‌تر.
> ۲. ~~**API فقط یک اندازه‌ی تصویر می‌داد / `srcset` واقعی از `full_url` استفاده نمی‌کند.**~~ **بسته شد (۱۴۰۵/۰۵/۲۰).** گالری صفحه‌ی محصول از فایل `card` تغذیه می‌شد که لبه‌ی بلندش سقف ۸۰۰ دارد — یعنی فرش عمودی **۴۶۱ پیکسل** عرض داشت برای جایگاهی که اندازه‌گیری شد **۵۸۲ پیکسل CSS** (و ۱۱۶۴ روی رتینا). `next/image` جزئیاتی که به آن نداده‌اند نمی‌سازد: مرورگر از srcset نسخه‌ی ۷۵۰ را خواست و **۳۹۸ پیکسل واقعی** تحویل گرفت. `full_url` همان عکس است در **۷۳۰×۱۲۶۸** — ۵۸٪ عرض بیشتر روی دو نمونه‌ای که اندازه گرفته شد. بعد از تغییر: ۶۳۰ پیکسل تحویل، کسری صفر.
>   - **برای گوشی هزینه ندارد:** `sizes` همچنان تعیین می‌کند کدام نسخه دانلود شود، پس صفحه‌ی کوچک فایل کوچک می‌گیرد؛ منبع فقط **سقف** را تعیین می‌کند و سقف همان چیزی بود که کم می‌آورد. در ۳۷۵ با DPR ۲ نیاز ۶۶۶ پیکسل است و حالا ۷۳۰ می‌رسد — قبلاً ۴۶۱ می‌رسید، یعنی گوشی هم کم‌سرویس بود.
>   - **کارت‌های شبکه دست نخوردند:** کارت در ~۳۰۰ پیکسل CSS با DPR ۲ یعنی ۶۰۰، و فایل `card` همان حدود است. عوض کردن آن‌ها یعنی ۲۴ فایل بزرگ‌تر در یک صفحه‌ی فهرست، بی‌آنکه چیزی دیده شود.
> ۳. ~~**میزبانی خودمان از قلم Satoshi با subset عددها.**~~ **بسته شد (۱۴۰۵/۰۵/۲۰) — ولی نه آن‌طور که نوشته شده بود. خودِ بدهی غیرقانونی بود.**
>   - کار تا آخر پیش رفت: دو وزن لازم مشخص شد (۴۰۰ و ۷۰۰؛ وزن ۵۰۰ در هیچ‌کدام از هشت کاربرد `font-figure` استفاده نمی‌شد)، فایل‌ها گرفته شدند و subset شدند — **۲۵٬۵۱۶ و ۲۵٬۳۲۸ بایت به ۶٬۷۹۶ و ۶٬۷۳۶ رسید**، با پوشش تأییدشده‌ی همه‌ی کاراکترهای لازم.
>   - **بعد لایسنس خوانده شد و کار متوقف شد.** ITF Free Font Licence دو بند دارد که هر دو راه را می‌بندند: «you cannot modify, make changes or reverse engineer the original font software» — که subset کردن دقیقاً همان است — و «The Fonts may not … be distributed, duplicated … including … uploading» — که گذاشتن `woff2` در این مخزن دقیقاً همان است. لایسنس صراحتاً لینک API را **روش موردنظر خودش برای وب** معرفی می‌کند: «delivered directly from the servers used by Indian Type Foundry … without the Licensee having to download the Font Software».
>   - **یعنی فقط دو راه قانونی وجود داشت:** نگه داشتن همان درخواست شخص‌ثالث، یا برداشتن قلم. هشت کد رهگیری ارزش یک مبدأ خارجی روی مسیر بحرانی رندر را ندارند، پس قلم برداشته شد و `--font-figure` روی **Inter** نشست که از قبل با `next/font` خودمیزبان بود و از قبل هم fallback پشت Satoshi بود. اندازه‌گیری‌شده: **صفر درخواست خارجی**، و عنصرهای `font-figure` روی `Inter` می‌نشینند.
>   - **فایل‌های دانلودشده پاک شدند و هیچ‌وقت وارد مخزن نشدند.**
>   - **درس، و از جنسی که تکرار می‌شود:** «subset به ارقام» که در متن همین بدهی نوشته شده بود، حتی اگر قانونی بود هم **خراب می‌کرد** — کد رهگیری `FR-` است به‌علاوه‌ی هشت نویسه از `23456789ABCDEFGHJKMNPQRSTUVWXYZ`، و ایمیل و اینستاگرام حروف کوچک می‌خواهند. **پیش از subset، مجموعه‌ی نویسه‌ها را از کدی که رشته را می‌سازد دربیاورید، نه از اسم فیلد.**
> ۴. ~~**راه خروج کیبورد و صفحه‌خوان برای ورودی سینمایی.**~~ **بسته شد (۱۴۰۵/۰۵/۲۰)، هم‌زمان با ساخت خود ورودی.** نمونه فقط کلیدهای جهت داشت و **صفر ویژگی `aria`**. حالا یک `dialog` مودالِ برچسب‌خورده است؛ فوکوس روی دکمه‌ی خروج می‌نشیند و داخل نگه داشته می‌شود؛ `Escape` هر لحظه بیرون می‌برد؛ هر صحنه با ارقام فارسی و **توصیف تصویر** (نه شعارِ روی آن) اعلام می‌شود؛ و بوم `aria-hidden` است چون تزئین است نه محتوا.
>   - **اندازه‌گیری، تله‌ای را داخل خودِ همین اصلاح پیدا کرد:** دکمه‌ی خروج عمداً فوکوس می‌گیرد، و نگهبان کلید *همه‌ی* کلیدهای حرکت را وقتی آن فوکوس داشت می‌بلعید — یعنی کلیدهای جهت تا وقتی کاربر Tab نمی‌زد **اصلاً کار نمی‌کردند**. مسیر کیبوردی که با غیرفعال بودن شروع شود، مسیر کیبوردی نیست. استثنا حالا فقط `Enter` و `Space` است که به دکمه تعلق دارند.
> ۵. **اختلاف شمارش محصول** — پیش از نوشتن فصل داده باید روشن شود. **اندازه‌گیری تازه (۱۴۰۵/۰۵/۲۰): ۷۴ فرش (۷۱ فعال + ۳ غیرفعال) و ۲۵۹ سایز.** با شمارنده‌های `/admin/stats` و پیمایش کامل `/admin/carpets` مستقلاً جور درمی‌آید. یعنی عدد نه ثابت مانده و نه فقط سه‌تا اختلاف دارد: ۷۰ → ۷۴. **سرنخ محکم:** یکی از رکوردها اسمش `unluckiest` است (شناسه ۷۲) و آشکارا در حین آزمودنِ خودِ پنل ساخته شده. یعنی بخشی از اختلاف از فرش‌های آزمایشی می‌آید، نه از ingest. **پیش از فصل داده، رکوردهای آزمایشی باید شناسایی و پاک شوند و بعد شمارش نهایی ثبت شود** — حدس نزنید، همان موقع بشمارید.
> ۶. ~~**سرریز افقی هدر در ۳۷۵ پیکسل.**~~ **بسته شد (۱۴۰۵/۰۵/۱۵).** دوباره اندازه‌گیری شد و دیگر وجود ندارد: روی خانه، `/carpets`، صفحه‌ی محصول، `/checkout` و هدر با کشوی باز، `scrollWidth` برابر `clientWidth` است و صفر عنصر از لبه بیرون می‌زند. `overflow-x` هم روی `html` و `body` هر دو `visible` است، یعنی پنهان نشده. علتش کامیت `d534374` بود که نشان برند را روی گوشی دوطبقه کرد و ۱۶۷ پیکسل را به ۷۰ رساند.
>
> ۷. ~~**چهار تریگر منوی دسکتاپ ۳۶ پیکسل‌اند، نه ۴۴.**~~ **بسته شد (۱۴۰۵/۰۵/۲۰).** کامیت `26dea33` از یک شاخه‌ی کاریِ موقت بدون تعارض cherry-pick شد. در عمل **پنج** تریگر بودند نه چهار. اندازه‌گیری دوباره در ۱۰۲۴ و ۱۲۸۰: همه ۴۴.
>
> ۸. **راه‌های تماس هنوز خالی‌اند.** `lib/content/contact.ts` چهار ثابت خالی دارد (واتس‌اپ، اینستاگرام، تلفن، ایمیل) و صفحه‌ی `/contact` و فوتر هر دو از همان می‌خوانند. حالت خالی آبرومند است و بن‌بست نیست، ولی حالت پرشده تا وقتی مقدار واقعی نیاید روی سایت دیده نمی‌شود. پر کردنش ویرایش یک فایل است.
>
> ۹. ~~**نوار مقایسه روی گوشی صلیب حذف ندارد.**~~ **بسته شد (۱۴۰۵/۰۵/۲۰) — و راه‌حل، آن کشوی جمع‌شونده نبود.** یادداشت قبلی می‌گفت اگر لازم شد کشو بسازید نه اینکه ردیف را فشرده‌تر کنید، و اندازه‌گیری تازه نشان داد چرا کشو هم جواب نمی‌دهد: ردیف در ۳۷۵ دقیقاً ۳۳۵ از ۳۳۵ پیکسل را مصرف می‌کند، پس **جا برای هیچ کنترل پنجمی نیست**، حتی یک دستگیره‌ی باز کردن. راه درست عوض کردن جای هدف بود، نه اضافه کردن هدف: **خودِ عکس دکمه‌ی حذف شد.** عکس از قبل ۴۴ پیکسل بود و هیچ کاری نمی‌کرد؛ ضربدر حالا داخل گوشه‌اش کشیده می‌شود. صفر پیکسل هزینه‌ی عرض، و یک ساختار برای همه‌ی عرض‌ها به‌جای یک شکل گوشی و یک شکل دسکتاپ.
>    - جانبی: صلیبِ بیرون‌آویزان با باگ ثبت‌شده‌ی خودش (فرزند `absolute` که ۱۰ پیکسل بیرون می‌زد و در `scrollWidth` شمرده می‌شد) برداشته شد. اندازه‌گیری‌شده در ۱۲۸۰: نوار دیگر اسکرول بی‌خود ندارد.
>
> ۱۰. ~~**همه‌ی کنترل‌های لمسی باید یک‌بار با مرورگر اندازه‌گیری شوند، نه چشمی.**~~ **انجام شد (۱۴۰۵/۰۵/۲۰).** جارو روی هر صفحه‌ی فروشگاه و پنل در ۳۷۵ و ۱۰۲۴ و ۱۲۸۰، به‌علاوه‌ی حالت‌هایی که جارو‌ی ایستا نمی‌بیند: کشوی موبایل، برگه‌ی فیلتر، نوار مقایسه، و مگامنوی باز. شرح یافته‌ها پایین‌تر در «جاروی کنترل‌های لمسی».
>
> ### شروع یک جلسه‌ی تازه (هر چت جدید، هر حساب)
>
> کل حافظه‌ی این پروژه در مخزن است، نه در چت: همین سند، پیام‌های کامیت، و سرصفحه‌ی هر فایل که می‌گوید چرا آن‌طور نوشته شده. یک چت تازه با خواندن این سه، دقیقاً از جایی که کار مانده ادامه می‌دهد.
>
> پیام اول یک جلسه‌ی تازه همین کافی است:
>
> ```
> ROADMAP.md را بخوان و از «قدم بعدی» ادامه بده.
> ```
>
> **دقیقاً یعنی: فاز ۴ — قابلیت‌های هوشمند**، و از میان چهار قابلیتش **جست‌وجوی بصری (رابط کامل)** اول. دلیل ترتیب و آن‌چه عمداً باز مانده در بند «چرا فاز ۳ باز ماند» پایین‌تر. کار در نقطه‌ی تمیزی متوقف شده: آخرین کامیت روی `main` همان کامیتی است که این خط را آورده.
>
> **دو چیز پیش از شروعِ جست‌وجوی گفتگویی باید تصمیم‌گیری شوند** (و فقط جلوی همان یکی را می‌گیرند، نه بقیه‌ی فاز ۴): مدل زبانی ابری در برابر محلی، و کلید API.
>
> **پیش از شروع، ورودی را یک‌بار خودتان ببینید** (`http://localhost:3000/` — در حالت توسعه هر رفرش پخش می‌شود). دو چیزِ باقی‌مانده‌اش هم همان‌جا معلوم می‌شود: اندازه‌گیری در ۳۷۵ و ۱۰۲۴، و لمس روی گوشی.
>
> پیش از شروع، بند «سه سؤال بی‌جواب» را ببینید؛ هیچ‌کدام جلوی این قدم را نمی‌گیرند.
>
> شاخه‌های موقتی که کنار `main` در `git branch -a` دیده می‌شوند شاخه‌های کاری‌اند. تنها یکی‌شان چیزی دارد که مرج نشده، و آن بدهی ۷ است.
>
> **قاعده‌های ثابتی که هر جلسه باید رعایت شوند** (در حافظه‌ی ابزار هم ثبت‌اند، ولی اینجا هم می‌آیند تا مستقل باشد):
>
> - هر قطعه‌ی رابط از ۲۱st گرفته می‌شود و بعد تطبیق داده می‌شود؛ دست‌ساز فقط جایی که [components/ui/README.md](frontend/src/components/ui/README.md) گفته.
> - **اول همان README و پوشه‌ی `components/ui/` را بگردید، بعد سهمیه خرج کنید.** یک سهمیه بابت آکاردئونی خرج شد که از قبل در پروژه بود.
> - انتخاب کامپوننت با کارفراست: لینک پیش‌نمایش فرستاده می‌شود، نه نسخه‌ی ساخته‌شده.
> - هر تغییر رابط با مرورگر اندازه‌گیری می‌شود — **۳۷۵ و ۱۰۲۴ و ۱۲۸۰** — نه با حدس. ۱۰۲۴ جا افتاده بود و لازم است: نوار هدر فقط همان‌جا سرریز می‌کند، چون مگامنو از `lg` ظاهر می‌شود و باریک‌ترین عرضی که آن را نشان می‌دهد همین است.
> - فارسی مؤدبانه (دوم‌شخص جمع)، ارقام فارسی، بدون `font-figure` روی عدد فارسی.
> - هر کامیت با نام کارفرما و بدون هیچ اشاره‌ای به ابزار.
>
> **دو رفتار محیط که باگ نیستند و وقت نگیرند:**
>
> - **اسکرین‌شات وقتی پنل مرورگر بسته است کار نمی‌کند** («not compositing frames»). راهش این نیست که دنبال باگ بگردید: با JS اندازه بگیرید (`scrollWidth`/`clientWidth`، `getBoundingClientRect`، `getComputedStyle`) و برای تأیید ظاهر از کارفرما بخواهید پنل را باز کند.
> - **در پنل بسته `requestAnimationFrame` هیچ تیکی نمی‌زند — پس ورودی سینمایی آن‌جا اصلاً قابل آزمودن نیست (۱۴۰۵/۰۵/۲۰).** اندازه‌گیری‌شده: `visibilityState` برابر `hidden` و **صفر تیک در ۱۲۰۰ میلی‌ثانیه**. فیلم روی rAF می‌چرخد، پس پخش فصل روی صحنه‌ی دوم می‌ایستد و شبیه باگ به نظر می‌رسد. دنبال باگ نگردید: **دیدن فیلم کارِ کارفرماست، با پنل باز.**
> - **پنل مرورگر `effectiveType: "3g"` گزارش می‌دهد** (`downlink` حدود ۰٫۴۵). نگهبان اتصال کندِ ورودی درست شلیک می‌کند و به‌جای فیلم، تصویر ثابت نشان می‌دهد. کد سالم است؛ محیط درباره‌ی شبکه دروغ می‌گوید. `?force=play` دورش می‌زند.
> - **در پنل مرورگر `document.hasFocus()` نادرست است، و رویدادهای فوکوس شلیک نمی‌شوند (۱۴۰۵/۰۵/۲۰).** `element.focus()` مقدار `document.activeElement` را عوض می‌کند و حلقه‌ی فوکوس هم دیده می‌شود، ولی `focus`/`focusin` نمی‌آید — چون خودِ سند فوکوس ندارد. یعنی هر `onFocus` ری‌اکتی در اندازه‌گیری «کار نمی‌کند» و این باگ برنامه نیست. یک ساعت سر ذره‌بین همین را داد. **راهش:** `new FocusEvent('focusin', {bubbles:true})` را dispatch کنید؛ ری‌اکت به `focusin` روی ریشه گوش می‌دهد، پس همان مسیر واقعی اجرا می‌شود.
> - **فیبر ری‌اکت را برای خواندن state باور نکنید.** هر کامپوننت دو فیبر دارد (`current` و `alternate`) و آن که از روی گره‌ی DOM برمی‌دارید ممکن است درخت کهنه باشد؛ یک‌بار `frameSize` را `{0,0}` نشان داد در حالی که مقدار واقعی ۳۳۷ بود. از روی *خروجی* بسنجید (اینجا: `backgroundSize` محاسبه‌شده)، نه از روی حافظه‌ی داخلی ری‌اکت.
> - **پورت ۳۰۰۰ و ۸۰۰۰ ممکن است از یک چت دیگر بالا باشند.** `preview_start` آن‌وقت رد می‌کند و می‌گوید پورت مال چت دیگری است. اگر همان پوشه را سرو می‌کند (با `Get-CimInstance Win32_Process` بررسی کنید) مشکلی نیست، مستقیم به `http://localhost:3000` وصل شوید.
>   - **دو راهی که آن پیام پیشنهاد می‌دهد هر دو غلط‌اند (۱۴۰۵/۰۵/۲۰).** `autoPort: true` یعنی سرور دوم روی پورت تصادفی — دو Turbopack روی یک `.next` و پروکسی‌ای که به ۸۰۰۰ می‌رود بی‌فایده می‌شود؛ آزاد کردن پورت هم یعنی کشتن سرور سالمِ چت دیگر. **این پورت‌ها واقعاً لازم‌اند:** پروکسی `/api` و `/files` به `localhost:8000` در `next.config.ts`، و باز کردن سایت از گوشی روی `:3000`. راه درست وصل شدن است، نه بالا آوردن دوباره. در فایلِ محلیِ `launch.json` (پیکربندیِ سرورهای توسعه) هر دو ورودی `"autoPort": false` گرفتند تا پیام تکرار نشود — **ولی آن فایل در `.git/info/exclude` است و با مخزن جابه‌جا نمی‌شود**، پس روی هر ماشین تازه باید دوباره ست شود.
>
> ### پیش از هر جلسه
>
> ```
> .\scripts\dev-db.ps1                     # وگرنه دیتابیس بالا نیست — WSL خودش می‌خوابد
> uv run uvicorn app.main:app --reload      # از پوشه‌ی backend
> npm run dev --prefix frontend
> ```
>
> از گوشی: `http://<آی‌پی لپ‌تاپ>:3000` — API و فایل‌ها از همین مبدأ پروکسی می‌شوند، پس فقط پورت ۳۰۰۰ لازم است.
>
> **ورود به پنل ادمین.** نام کاربری `admin`، رمز در `backend/.env` (گیت‌ایگنور). اگر `.env` نبود، ورود **غیرفعال** است — `admin_password_hash` خالی یعنی همین — و ساختنش:
>
> ```powershell
> uv run python scripts/hash_password.py "رمز-دلخواه"
> ```
>
> و هش را در `ADMIN_PASSWORD_HASH` بگذارید، بعد بک‌اند را ری‌استارت کنید (تنظیمات per-process کش می‌شوند و `--reload` فقط `.py` را می‌پاید).
>
> **`SESSION_COOKIE_SECURE=false` فقط برای محلی.** کوکی سشن به‌طور پیش‌فرض `Secure` است و مرورگر روی `http` ذخیره‌اش نمی‌کند، پس ورود از گوشی روی آی‌پی شبکه بی‌صدا شکست می‌خورد. این تنظیم از `DEBUG` جدا شد چون `DEBUG=true` لاگ هر کوئری SQL را هم روشن می‌کند و امنیت کوکی نباید عارضه‌ی پرحرفی لاگ باشد. **در استقرار ست نشود.**
>
> **برای دیدن پنل بدون دانستن رمز** (مثلاً در جلسه‌ی توسعه) می‌شود با امضاکننده‌ی خود اپ یک نشست ساخت و کوکی را دستی گذاشت:
>
> ```powershell
> uv run python -c "from app.services.auth import issue_session; print(issue_session())"
> ```
>
> **پس از هر تغییر در مسیرها یا اسکیمای بک‌اند:** `uv run python scripts/export_openapi.py` و بعد `npm run gen:api`. وگرنه CI می‌افتد.
>
> ### سهمیه‌ی ۲۱st
>
> دو بار گرفتن کد در روز، فقط `get_component` از آن کم می‌کند (جست‌وجو رایگان است). **وقتی صفر شد، منتظر فردا نمانید** — به کارفرما بگویید حساب را عوض کند:
>
> ```powershell
> & "$env:USERPROFILE\...\set-21st-key.ps1"   # مسیرِ کاملش شخصی و بیرون از مخزن است
> ```
>
> **تصحیح (۱۴۰۵/۰۵/۱۴): «بدون ری‌استارت اثر می‌کند» درست نبود.** خودِ اسکریپت در توضیحاتش نوشته بعد از تعویض باید محیطِ توسعه ری‌استارت شود تا سرور MCP دوباره وصل شود و کلید تازه را بخواند. در این جلسه یک‌بار بدون ری‌استارت گرفت (احتمالاً چون همان موقع سرور خودش قطع و وصل شد) و بار دوم نگرفت. اگر بعد از تعویض `get_usage` هنوز صفر داد، اول ری‌استارت کنید؛ اگر باز هم صفر بود، سهمیه‌ی همان حساب دوم هم امروز خرج شده است.
>
> `-Show` چهار رقم آخر کلید فعلیِ **فایل پیکربندی** را می‌گوید (نه کلیدی که سرورِ در حال اجرا در حافظه دارد) و برای تشخیص دو حساب از هم کافی است:
>
> ```powershell
> & "$env:USERPROFILE\...\set-21st-key.ps1" -Show
> ```
>
> **تأیید شد: ری‌استارت جواب می‌دهد، VPN نه.** روشن کردن VPN اتصال HTTP موجود را نمی‌بندد، پس کلاینت همان اتصال با کلید قدیمی را نگه می‌دارد؛ نشانه‌اش این است که پیام قطع و وصل شدن سرور اصلاً نمی‌آید. بعد از ری‌استارتِ محیطِ توسعه، `get_usage` بلافاصله **۲ از ۲** داد.
>
> وضعیت جلسه‌ی ۱۴۰۵/۰۵/۱۴: حساب دوبار عوض شد و چهار کامپوننت گرفته شد — زوم گالری، سبد، فرم تسویه، صفحه‌بندی.
>
> وضعیت جلسه‌ی ۱۴۰۵/۰۵/۱۵: یک کامپوننت گرفته شد (آکاردئون سؤالات) و **یک سهمیه از همان حساب باقی مانده بود**. جلسه‌ی بعد با `get_usage` بررسی کنید؛ سهمیه روزانه صفر می‌شود.
>
> وضعیت جلسه‌ی ۱۴۰۵/۰۵/۱۶: **هیچ سهمیه‌ای خرج نشد** — مقایسه‌ی فرش‌ها جدول و نوار و انبار است و هر سه از الگوی موجود مخزن درآمدند. `get_usage` ابتدای جلسه ۱ از ۲ می‌داد و دست‌نخورده ماند.
>
> **بودجه‌ی کامپوننت فاز ۳.** سهمیه‌ی رایگان ۲۱st **دو بار گرفتن کد در روز** است و فقط `get_component` و `generate` از آن کم می‌کنند؛ جست‌وجو رایگان و نامحدود است. برآورد کل فاز: **۸ کامپوننت ضروری، ۱۳ با مرتبه‌ی دوم، و با احتساب مواردی که هنگام تطبیق به درد نمی‌خورند حدود ۱۲ تا ۱۸ بار گرفتن کد.** به ترتیب بازده:
>
> | # | کامپوننت | کجا به کار می‌رود |
> |---|---|---|
> | ۱ | کارت محصول و شبکه | ✅ گرفته شد — لیست، مشابه‌ها، علاقه‌مندی‌ها، خانه |
> | ۲ | هدر + منو + ناوبری موبایل | ✅ گرفته و کامل ساخته شد ([site-header.tsx](frontend/src/components/toranjan/site-header.tsx)) |
> | ۳ | صفحه‌ی محصول (گالری + انتخاب واریانت) | ✅ گرفته و کامل ساخته شد |
> | ۳ب | زوم گالری | ✅ `zoomable-image` گرفته و تطبیق داده شد |
> | ۴ | پنل فیلتر faceted | ✅ آکاردئون + اسلایدر بازه + چیپ‌ها گرفته و ساخته شد |
> | ۵ | سبد خرید | ✅ `interactive-checkout` گرفته و تطبیق داده شد |
> | ۶ | فرم چندمرحله‌ای ثبت سفارش | ✅ `multistep-form` گرفته و تطبیق داده شد |
> | ۷ | اسکلت پنل ادمین | داشبورد + سه صفحه‌ی دیگر |
> | ۸ | جدول داده | سفارش‌ها، مدیریت فرش‌ها |
> | ۹ | آکاردئون سؤالات | ✅ `interactive-accordion` گرفته شد — فقط ظاهرش استفاده شد، ماشینش از `ui/accordion.tsx` خودمان |
>
> مرتبه‌ی دوم: آپلودر تصویر با مرتب‌سازی کشیدنی، هیرو صفحه‌ی خانه، ~~آکاردئون سؤالات~~ (گرفته شد)، اسکلتون بارگذاری. فوتر دستی ساده‌تر است.
>
> **درسی که یک سهمیه خرج شد تا فهمیده شود (۱۴۰۵/۰۵/۱۵): اول مخزن را بگرد، بعد سهمیه را خرج کن.** برای صفحه‌ی سؤالات یک آکاردئون گرفته شد، در حالی که `ui/accordion.tsx` از قبل در پروژه بود — همان چیزی که برای پنل فیلتر گرفته شده بود، با هوک‌های بدون‌ظاهر `useAccordion` و `useAutoHeight` که فوکوس چرخشی با کلیدهای جهت، `aria-expanded`/`aria-controls` و `inert` را دارند. کامپوننت تازه دور ریخته نشد — ماشین از مال خودمان ماند و فقط ظاهرش (شماره، + که × می‌شود، خط زیرین کشیده‌شونده) از کامپوننت تازه آمد — ولی اگر اول جست‌وجو شده بود، این یک سهمیه برای پنل ادمین می‌ماند.
>
> بیرون از دسترس ۲۱st و حتماً دست‌ساز: لایه‌ی جای‌گذاری AR (ساخته شده)، ورودی سینمایی (نمونه‌اش هست)، بازبینی گوشه‌های AR در ادمین، راهنمای اندازه، مشاور چیدمان.
>
> **بدهی‌های ثبت‌شده‌ی همین فاز:** میزبانی خودمان از قلم Satoshi با subset عددها (فعلاً از Fontshare می‌آید)؛ پاک کردن نام فروشگاه مبدأ از عنوان محصول‌های کاتالوگ نمایشی؛ مسیر خروج کیبورد و صفحه‌خوان برای ورودی سینمایی.
>
> - ~~**بدهی افزوده‌شده (۱۴۰۵/۰۵/۱۳) — فیلتر رنگ عملاً کار نمی‌کند.**~~ **سمت بک‌اند بسته شد (۱۴۰۵/۰۵/۲۳)، سمت رابط باز است.** رنگ‌های غالب هگزِ دقیق بودند و تقریباً یکتا، پس فست رنگ ساخته نمی‌شد و فیلتر `color` تطابق دقیق هگز می‌کرد. حالا `ColorFamily` (سیزده خانواده‌ی نامی) به `app/models/enums.py` افزوده شده، `carpets.color_families` در مهاجرت `0003` ساخته شده و هنگام ingest از روی همان یک پاسِ رنگ پر می‌شود؛ `/carpets/facets` فست `colors` می‌دهد و فیلتر `color` تکرارشونده است و روی خانواده `overlap` می‌کند. هگز حذف نشد — سواچ را همان رنگ می‌کشد، خانواده فقط فیلتر می‌کند.
>   - **دو بدهی بودند و یک مسئله:** این و یافته‌ی «کوررنگی امبدینگ» در بند ۷ هر دو یک چیز می‌خواستند — توصیف پیکسل بر اساس جایش در فضای رنگ، نه سه‌تایی دقیقش. برای همین هر دو در یک ماژول و یک پاس روی پیکسل‌ها حل شدند: هیستوگرام شکل پیوسته‌ی همان توصیف است و خانواده شکل درشت و نام‌پذیرش.
>   - ~~**باقی‌مانده: بخش رنگ در پنل فیلتر ساخته نشده.**~~ **ساخته شد (۱۴۰۵/۰۵/۲۳).** سیزده چیپ در پنل فیلتر، هر کدام با یک سواچ رنگی، شمارشِ فست، و ارتفاع ۴۴ مثل بقیه‌ی چیپ‌ها. `ChipRow` موجود یک پراپ `swatch` گرفت به‌جای اینکه ردیف دومی نوشته شود. رنگ تنها فیلتری است که سواچ می‌گیرد، چون تنها فیلتری است که نامش جانشین خوبی برای خودش نیست — دو نفر با خواندن «طلایی» یک رنگ را تصور نمی‌کنند. سواچ‌ها (`COLOR_SWATCH`) نماینده‌ی خانواده‌اند نه رنگ هیچ فرشی، و از جایی که خانواده در فرش می‌نشیند انتخاب شده‌اند نه از چرخ رنگ.
>   - **`color` حالا در هر چهار جایی که فیلترها شمرده می‌شوند هست:** خواندن از URL، چیپ‌های اعمال‌شده، «پاک کردن همه»، و شمارنده‌ی دکمه‌ی موبایل. جا انداختن یکی از این‌ها فیلتری می‌سازد که پاک نمی‌شود.
> - ~~**بدهی افزوده‌شده (۱۴۰۵/۰۵/۱۳) — API فقط یک نسخه از هر عکس می‌دهد.**~~ **بسته شد (۱۴۰۵/۰۵/۱۴).** مهاجرت `0002` سه ستون `thumb_url`/`full_url`/`texture_url` را به `carpet_images` افزود؛ `url` همان مشتق ۸۰۰ پیکسلی می‌ماند تا هیچ فراخوانی موجود نشکند. `ImageOut` حالا `thumb_url` و `full_url` را هم می‌دهد (`texture` داخلی می‌ماند، مصرف‌کننده‌اش پایپ‌لاین AR است).
  - **چرا URL گم‌شده را نمی‌شد حدس زد:** ذخیره‌ساز محتوا-آدرس است و نام هر فایل از هش بایت‌های *همان مشتق* می‌آید، پس از روی مسیر `card` نمی‌شود مسیر `full` را ساخت. راه بازیابی، اجرای دوباره‌ی همان پایپ‌لاین روی عکس مبدأ است — قطعی است، پس هر چهار هش دوباره همان درمی‌آیند. `scripts/backfill_image_derivatives.py` همین کار را می‌کند و فقط می‌خواند: فایل‌ها از قبل روی دیسک‌اند. نتیجه: **۷۰ از ۷۰ ردیف تطبیق خورد، صفر بی‌جفت.**
- ~~**یافته‌ی جدید (۱۴۰۵/۰۵/۱۴) — هر فرش فقط یک عکس دارد.**~~ **رسیدگی شد (۱۴۰۵/۰۵/۱۴).** ۷۱ عکس `data/catalog-seed/covers/` با `scripts/attach_gallery_images.py` به فرش‌های موجود وصل شدند (فرش از روی نام فایل پیدا می‌شود: `dk-9814545-2.jpg` عکس دوم `dk-9814545` است). مجموع: **۱۴۱ عکس، ۷۰ عکس اصلی، ۷۰ امبدینگ.**
  - **این عکس‌ها عمداً امبدینگ نمی‌گیرند** (بند ۷). نماهای زاویه‌دار استودیویی‌اند و جست‌وجوی بصری الگو را از نمای تخت می‌سنجد؛ امبدینگ‌شان یعنی نتایج پر شود از همان فرش با زاویه‌ی دیگر، و ایندکس یاد بگیرد فرش ذوزنقه است. `search_by_embedding` ردیف با امبدینگ تهی را رد می‌کند، پس تهی گذاشتن خودِ مکانیزم است. تأیید شد: جست‌وجوی بصری ۱۲ نتیجه‌ی یکتا می‌دهد، بدون تکرار.
  - **هیچ‌کدام `is_primary` نمی‌شوند**، چون عکس اصلی همان است که پایپ‌لاین AR تصحیح پرسپکتیو می‌کند و شبکه نشان می‌دهد؛ هر دو نمای تخت می‌خواهند.
  - **باقی‌مانده‌ی صادقانه: ۴۳ فرش هنوز تک‌عکس‌اند.** توزیع فعلی عکس به‌ازای فرش: `{۱: ۴۳، ۲: ۸، ۳: ۶، ۴: ۶، ۵: ۳، ۶: ۳، ۷: ۱}`. فرش‌های دیتاست عمومی فقط یک عکس دارند و منبع دومی برایشان نیست؛ گالری‌دار شدن‌شان به عکس‌برداری از فرش‌های قهرمان (بند ۱۲) گره خورده است.

> پیش‌نیاز شروع: ~~نام برند~~ (ترنجان، انتخاب شد) + تأیید سیستم طراحی توسط کارفرما.
> نقطه‌ی شروع پژوهشی در [docs/design-starting-point.md](docs/design-starting-point.md) آماده است (پیشنهاد، نه تصمیم).
> **تصمیم‌های گرفته‌شده (۱۴۰۵/۰۵/۱۰):**
> - **دیتا:** پستگرس محلی در WSL؛ فروشگاه از روز اول به داده‌ی واقعی وصل است.
> - **حالت رنگی:** فقط روشن، اما توکن‌ها از ابتدا متغیر تعریف می‌شوند تا افزودن تیره بعداً بازنویسی نباشد.
> - **حس کلی:** فروشگاه لاکچری فرش. ورودی سینمایی و تاریک، فروشگاه روشن و آرام؛ گذار از تاریکی به روشنایی خودش یکی از لحظه‌های سایت است.
> - **ورودی سایت:** روایت «بافته شدن فرش» با الگوی تعامل **فصل‌بندی‌شده** — جزئیات، صحنه‌ها و بدهی فنی‌اش در [docs/brand-brief.md](docs/brand-brief.md) و نمونه‌های کارکرد در [docs/prototypes/intro/](docs/prototypes/intro/).
> - **ترتیب ساخت:** ورودی سینمایی **آخرین** کار فاز ۳ است، نه اولین — چون به شبکه‌ی محصولات تحویل می‌دهد و تا آن ساخته نشود معلق است.
> - **نام برند: ترنجان (Toranjan).** ترنج نقش مرکزی فرش ایرانی است و تنها گزینه‌ی بررسی‌شده‌ای بود که درون دنیای فرش معنا دارد؛ پسوند «ـان» آن را از واژه‌ی عام صنف جدا می‌کند و یکتا می‌سازد، و با نشان پیشنهادی لوگو (ترنج ساده‌شده) هم‌پوشان است. **واژه‌ای ساخته‌شده است، نه مدخل فرهنگ لغت** — پیش از سفارش لوگو و خرید دامنه باید در دهخدا و بازار جست‌وجو شود.
> - **دارایی‌های گرافیکی، آخرِ کار.** لوگو و تصاویر تزئینی در پایان فاز ساخته می‌شوند، نه ابتدای آن. تا آن زمان جای‌نگهدار می‌نشیند، اما با یک شرط: هر دارایی از **یک مسیر ثابت با نام ثابت** خوانده می‌شود و مشتق‌هایش (آیکون مرورگر، آیکون نصب روی گوشی، تصویر اشتراک‌گذاری، نسخه‌ی روشن و تیره) با اسکریپت از همان یک فایل ساخته می‌شوند. جایگزینی نهایی باید تعویض یک فایل و اجرای یک دستور باشد، نه گشتن در صفحات.
> - نام برند جای‌نگهدار نیست: «ترنجان» از روز اول با قلم واقعی نوشته می‌شود و تنها نشان تصویری موقت است.
> - **جهت بصری (۱۴۰۵/۰۵/۱۲): «گالری موزه‌ای».** زمینه‌ی گرمِ روشن، فضای خالی زیاد، خطوط مویی؛ فرش قاب می‌شود و رابط عقب می‌نشیند. از میان چهار جهتِ ساخته‌شده انتخاب شد ([docs/prototypes/storefront/](docs/prototypes/storefront/)) — از روی توضیح متنی تصمیم‌گرفتنی نبود، همان‌طور که در ورودی سینمایی هم نبود.
>   - **شرط پذیرش:** این چیدمان عمداً ساده است و اگر حرکت نداشته باشد خشک می‌شود. جبرانش با لایه‌ی حرکت است: توالی ورود صفحه، رانش پارالاکس داخل هر قاب، سرآیندی که با اسکرول جمع می‌شود، و ترنزیشن اشتراکیِ تصویر از شبکه به صفحه‌ی محصول.
>   - جهت «سینمایی تاریک» کنار گذاشته نشد؛ جای درستش ورودی و صفحه‌ی AR است، نه شبکه‌ی محصولات — چون برای تاریک ماندن باید رنگ فرش کم شود و این با ادعای اصلی محصول می‌جنگد.
> - **شیشه‌ای (Glassmorphism / Liquid Glass) — بررسی شد و برای فروشگاه رد شد (۱۴۰۵/۰۵/۱۳). این پرونده بسته است.** دیتابیس ابزار طراحی این سبک را برای «فروشگاه لاکچری» پیشنهاد می‌دهد و در چهار جهت اولیه اصلاً ساخته نشده بود، یعنی هیچ‌وقت واقعاً دیده نشده بود. دو نسخه ساخته شد — یکی با زمینه‌ی رنگی و یکی در پالت خودمان با هاله‌ای از رنگ‌های غالبِ خودِ فرش — و کارفرما هر دو را کنار «گالری موزه‌ای» دید و هر دو را رد کرد. تصمیم حالا بر پایه‌ی دیدن است نه استدلال، پس **در فروشگاه دوباره باز نمی‌شود.**
>   - نمونه‌ها در [docs/prototypes/storefront/](docs/prototypes/storefront/) می‌مانند (`glass.html`، `glass-light.html`) تا در **فاز ۲/۴ برای صفحه‌ی AR و ورودی سینمایی** امتحان شوند — روی تصویر زنده‌ی دوربین و صحنه‌ی تاریک، شیشه زبان درستی است و پس‌زمینه‌ی لازمش را دارد.
>   - چیزی که از این کار ماند: `shared.js` حالا دو رنگ غالب هر فرش را روی کارت منتشر می‌کند (`--c1`/`--c2`) — بی‌مصرف برای جهت انتخاب‌شده، اما داده‌ای که در فاز ۴ برای مشاور چیدمان به کار می‌آید.
> - **تایپوگرافی (۱۴۰۵/۰۵/۱۲):** فارسی **وزیرمتن**؛ لاتین نمایشی و برند **Playfair Display**؛ لاتین متن و برچسب **Inter**؛ **عدد لاتین Satoshi** (فقط گلیف عددها، subset‌شده تا هزینه‌ی چهارمین خانواده‌ی قلم ناچیز بماند).
> - **پالت (۱۴۰۵/۰۵/۱۲): «گچ و زغال»، با تأکید طلایی در جزئیات.** زمینه `#FAFAFA`، متن `#18181B`، متن ثانویه `#72727A`، تأکید اُخرایی `#A16207`. از میان پنج پالتِ ساخته‌شده روی همان صفحه انتخاب شد.
>   - **دو نقش جدا برای تأکید، نه یکی:** `--accent` (طلایی) فقط نشانه‌های ریز را رنگ می‌کند — شمار اندازه‌ها روی کارت، آیکون AR، نشان سبد، حلقه‌ی فوکوس. `--cta` سطح بزرگ اقدام است و در این پالت زغالی می‌ماند. دلیل تفکیک: یک بلوک بزرگ طلایی، طلایی را از «جزئیات» به «رنگ غالب» تبدیل می‌کند و حس خنثای انتخاب‌شده را می‌شکند.
>   - طلایی روی این زمینه ۴٫۷۲:۱ می‌دهد، یعنی به‌عنوان **متن** هم مجاز است نه صرفاً تزئین.
> - **حالت تأیید (۱۴۰۵/۰۵/۱۲):** طلایی کم‌رنگ `#F6EEDF` با متن `#8A5406`. توکن مستقل `--confirm-*` دارد، نه رنگ ثابت داخل کد. سبز آگاهانه کنار گذاشته شد: تنها سبز پالت می‌شد و رنگی که به هیچ‌چیز دیگری تعلق ندارد، حس «پیام سیستم» می‌دهد نه «فروشگاه تأیید کرد».
>   - **تأیید فقط تغییر رنگ نیست، یک ژست چهارضربی است:** پرده‌ی رنگ از لبه می‌آید؛ کلمه‌ی قبلی از پشت ماسک بیرون می‌رود و بعدی جایش می‌آید (هم‌زبان با ورود تیتر در جهت بصری)؛ تیک با انیمیشن `stroke` خودش کشیده می‌شود، نه اینکه ظاهر شود؛ و یک حلقه یک‌بار باز می‌شود و محو می‌شود. هر چهار زیر `prefers-reduced-motion` خاموش می‌شوند و تیک در آن حالت کشیده‌شده می‌ماند نه نامرئی.
>   - ریشه‌ی مسئله: کامپوننت واردشده در موفقیت فقط `disabled` می‌شد و کم‌رنگ. کاربر «خاموش شدن دکمه» می‌دید نه «تأیید فروشگاه».
>   - **اصلاح دسترس‌پذیری حین همین کار:** رنگ متن ثانویه (شهر، جنس، واحد پول) در همه‌ی کاندیدها حدود ۳٫۶:۱ بود — زیر استاندارد ۴٫۵:۱ بند ۳. در هر پنج پالت تیره‌تر شد (`#8A827C` → `#79726D` در پالت منتخب). کنتراست‌ها محاسبه شدند، نه تخمین زده.
>   - پالت‌ها به‌صورت متغیر CSS زیر `[data-palette]` تعریف شده‌اند، پس تعویض پالت — و افزودن حالت تیره در آینده — تغییر توکن است نه بازنویسی.

1. **سیستم طراحی اول** (جهت بصری، پالت، تایپوگرافی فارسی، شبکه، کامپوننت‌ها) — **با تأیید کارفرما پیش از ساخت صفحات**؛ از ابزارهای طراحی/تولید کامپوننت و ایده‌های کارفرما استفاده می‌شود.
2. اسکلت و ناوبری (هدر، منو، جست‌وجو، فوتر، ناوبری موبایل).
3. صفحات ۱ تا ۱۳ بخش ۶ (خانه تا PWA/SEO).
4. پنل ادمین (۱۴ تا ۱۷).
5. ریزتعامل‌ها، اسکلتون‌ها، حالت‌های خالی/خطا.
6. کنترل کیفیت: ۳۷۵/۷۶۸/۱۰۲۴/۱۴۴۰، دسترس‌پذیری، سرعت، RTL.
**معیار اتمام:** فهرست بسته‌ی بخش ۶ آیتم‌به‌آیتم تیک بخورد؛ چک‌لیست کیفیت بند ۵ بخش ۳ روی هر صفحه پاس شود.

### فاز ۴ — قابلیت‌های هوشمند در دل فروشگاه ✅ انجام شد (به‌جز عدد ارزیابی، که کار فاز ۵ است)
جست‌وجوی بصری (رابط کامل)؛ تطبیق اندازه (A4 دقیق + عمق تخمینی)؛ مشاور چیدمان (سگمنتیشن کف/دیوار + پالت + سبک + توضیح پیشنهاد)؛ جست‌وجوی گفتگویی (LLM → فیلتر ساخت‌یافته با اسکیمای امن).

> **۰. پاس رنگ — انجام شد (۱۴۰۵/۰۵/۲۳).** پیش‌نیازی که خودِ نقشه ثبت کرده بود: بند ۷ نوشته بود کوررنگی امبدینگ «نیازمند رسیدگی پیش از فاز ۴» است، و بند ۳ فیلتر رنگ را بدهی کرده بود. هر دو با یک ماژول (`app/services/color.py`) و یک مهاجرت (`0003`) بسته شدند — جزئیات و اعداد در همان دو بند. خروجی‌ها: `scripts/backfill_color.py` (۷۵ هیستوگرام، ۷۴ فرش خانواده‌دار)، `tests/test_color.py` و `tests/test_visual_ranking.py`.
>
> **۱. رابط جست‌وجوی بصری — ساخته شد (۱۴۰۵/۰۵/۲۳).** `/visual-search` دو حالت دارد و یک ژست بین‌شان: یک دراپ‌زون بزرگ، و بعد عکسِ پرس‌وجو که با `layoutId` کوچک می‌شود و به نوار بالا می‌رود تا نتایج جایش را بگیرند. عکس روی صفحه می‌ماند چون هر نتیجه ادعایی درباره‌ی همان عکس است. دراپ‌زون از [joyco/file-dropzone](https://21st.dev/@joyco/components/file-dropzone) آمد و هوک `use-file-upload` که در پاسخ رجیستری نبود نوشته شد؛ جزئیات تطبیق در `frontend/src/components/ui/README.md`.
>
> - ~~**🔴 بدهی افزوده‌شده (۱۴۰۵/۰۵/۲۳) — نصب torch ناقص است و جست‌وجوی بصری نویز برمی‌گرداند.**~~ **بسته شد (۱۴۰۵/۰۵/۲۳).** با `uv sync --group ml --reinstall-package torch` درست شد؛ کش uv سالم بود و فقط استخراج در venv نصفه مانده بود، پس نصب دوباره پنج ثانیه طول کشید نه یک دانلود کامل. **تأیید: همان عکسی که ۰٫۳۲۵ می‌گرفت حالا ۰٫۹۹۶ می‌گیرد.** `--group ml` حالا در `README.md` بخش Development صریح است (همراه با دستور یک‌خطی تشخیص)، چون آن فایلِ محلیِ `launch.json` گیت‌ایگنور است و با مخزن سفر نمی‌کند.
>   - **یافته‌ی جانبی — سرد ۱۲ ثانیه، گرم ۳۰۰ میلی‌ثانیه.** `DinoV2Backend` مدل را با تنبلی و در اولین درخواست لود می‌کند، پس نخستین جست‌وجو پس از هر بالا آمدن سرور ۱۲٫۴ ثانیه طول می‌کشد و بعدی‌ها ۲۹۰ تا ۴۳۰ میلی‌ثانیه. برای فاز ۵: مدل هنگام راه‌اندازی گرم شود. **برای روز دفاع (بند ۱۰): پیش از شروع دمو یک جست‌وجوی بصری زده شود** وگرنه اولین نمایش دوازده ثانیه سکوت است.
>   - شرح مسئله‌ی اصلی، برای پرونده: `torch 2.13.0` نصب بود ولی فایل‌های پایتونش نصفه بودند (`torch/amp/__init__.py` اصلاً وجود ندارد؛ `nn`، `optim`، `utils`، `fx` هم نیستند، در حالی که `torch_cpu.dll` سیصد مگابایتی سر جایش است) — نشانه‌ی یک دانلود قطع‌شده. نتیجه‌اش این بود که `get_embedding_backend` با `except ImportError` بی‌صدا می‌افتاد روی `HashEmbeddingBackend`، و **آن بک‌اند قلابی بردار هش می‌دهد که بر امبدینگ‌های DINOv2 ذخیره‌شده در دیتابیس عمود است.** یعنی هر پاسخ ۲۰۰ بود و هر رتبه‌بندی تصادفی.
>   - **چطور دیده شد:** با عکسِ خودِ یک فرش کاتالوگ جست‌وجو شد و امتیازش ۰٫۳۲۵ بود نه ~۱. تفکیکش دقیقاً درمی‌آید: `۰٫۷×۰٫۰۴ + ۰٫۳×۰٫۹۹`. یعنی آن فرش **تنها به خاطر هیستوگرام رنگ** اول آمده بود و سهم ساختار نویز محض بود.
>   - **کنترل:** `HashEmbeddingBackend` بین نسخه‌ی ۸۰۰ و ۱۶۰۰ پیکسلی *همان عکس* کسینوس ‎−۰٫۰۴ می‌دهد. داک‌استرینگش می‌گوید «بایت‌های شبیه پایدار می‌مانند» و این درست نیست: عملاً هش بایت است. برای تست قراردادی کافی است، برای هیچ چیز دیگری نه.
>   - **رسیدگی‌شده در همین کامیت:** فallback دیگر بی‌صدا نیست و در لاگ می‌گوید کدام حالت است و راه‌حل چیست. **رسیدگی‌نشده: خودِ نصب.** راه‌حل `uv sync --group ml` است (دانلود سنگین) و تصمیم کارفرماست.
>   - **این بدهی امبدینگ‌های ذخیره‌شده را زیر سؤال نمی‌برد.** آن‌ها DINOv2‌اند: شباهت جفتی میان فرش‌های مختلف ۰٫۸ تا ۰٫۹۴ است، در حالی که بک‌اند هش برای فایل‌های مختلف حدود صفر می‌دهد. پس مقایسه‌ی رتبه‌بندی دومرحله‌ای که بالا ثبت شد معتبر است — آن روی بردارهای ذخیره‌شده اجرا شد، نه از مسیر پرس‌وجو.
>
> **۲. راهنمای اندازه — ساخته شد (۱۴۰۵/۰۵/۲۳).** بند ۶-۷ کامل شد. `/size-guide`: عکس اتاق → کف آزاد اندازه‌گیری می‌شود → اندازه‌های موجودِ کاتالوگ که در آن جا می‌شوند، هر کدام لینک به همان اندازه در فروشگاه. `POST /api/v1/room/size-guide`، ماژول‌های `app/room/scale.py` و `app/room/sizing.py`، تست‌ها در `tests/test_room_sizing.py`.
>   - **اندازه‌گیری از بالا، نه از تصویر.** هر سلول ۵ سانتی روی صفحه‌ی کف نشانده می‌شود و می‌پرسد در عکس کجا می‌افتد و آنجا چه ایستاده — نه برعکس. نگاشت پیکسل→سلول اول امتحان شد و شکست: یک ردیف پیکسل نزدیک افق نیم متر کف را می‌پوشاند، پس بین سلول‌ها نوارهای نمونه‌نگرفته می‌ماند و کف آزاد تکه‌تکه درمی‌آید.
>   - **🔑 تصمیم دامنه‌ای که کل قابلیت را نجات داد: فرش زیر میز جلومبلی می‌رود.** نسخه‌ی اول «کف کاملاً بی‌مانع» را می‌سنجید و برای یک پذیرایی معمولی حلقه‌ای از راهروها دور مبلمان می‌داد و ۸۰×۱۲۰ پیشنهاد می‌کرد. چیزی که جلوی فرش را می‌گیرد مبل و بوفه و دیوار است — نه میز و پاف که *روی* فرش می‌ایستند. آستانه روی ۴۸ سانتی‌متر گذاشته شد (ارتفاع میز جلومبلی در برابر ارتفاع نشیمن). با همین یک تغییر، همان عکس‌ها از ۸۰×۱۲۰ به ۳۰۰×۴۰۰ رسیدند.
>   - **سؤال درست «کدام اندازه جا می‌شود» است، نه «بزرگ‌ترین مستطیل کدام است».** بیشینه‌سازی مساحت روی کف واقعی یک نوار می‌دهد: ۴۵×۲۲۰ بین دو مبل، چون ۹۹۰۰ سانتی‌متر مربع از ۳۰۰×۱۰۰ وسط اتاق بیشتر است. نوار مساحت دارد و فرش نیست. حالا هر اندازه‌ی واقعیِ کاتالوگ با جدول مجموع-ناحیه‌ای آزمایش می‌شود.
>   - **برگه‌ی A4 = تنها خط‌کشی که هر خانه‌ای دارد.** ابعادش با ISO 216 دقیق است، پس یک برگه روی کف، خطای مقیاسِ عمق تک‌تصویری را برمی‌دارد. فیلتر واقعی هندسی است نه ظاهری: گوشه‌های برگه روی صفحه‌ی کف تصویر می‌شوند و نسبتشان **باید** √۲ دربیاید — هیچ‌چیز دیگری در اتاق تصادفاً این نسبت را نمی‌دهد، پس تشخیص می‌تواند سخاوتمند باشد.
>     - **تست رفت‌وبرگشتی:** برگه‌ای با اندازه‌ی درست روی کف کشیده می‌شود و باید مقیاس ۱٫۰ برگرداند؛ با ۱٫۲۵ برابر باید دقیقاً ۰٫۸ بدهد. خطا اول **۴ تا ۱۳ درصد و همیشه یک‌طرفه** بود — `cv2.dilate` روی لبه‌ها باند را ضخیم می‌کرد و کانتور از داخلش رد می‌شد. با `MORPH_CLOSE` به‌جای dilate و `cornerSubPix`، خطا به **۰٫۱ تا ۲٫۷ درصد** رسید. زیر خطای خودِ مدل عمق، که تمام دلیل وجود این مرجع است.
>   - **حدهای صادقانه‌ای که در رابط نوشته شده‌اند:** فقط کفِ داخل کادر سنجیده می‌شود (پس «از درِ اتاق عکس بگیرید»)، و بدون A4 عددها تخمینی‌اند. اطمینان زیر ۰٫۴ هشدار می‌دهد.
>   - **هزینه:** حدود ۴ ثانیه در هر عکس پس از گرم شدن مدل، روی نخِ جدا (`anyio.to_thread`) تا یک آپلود کل فروشگاه را قفل نکند.
>
> **۳. جست‌وجوی گفتگویی — ساخته شد (۱۴۰۵/۰۵/۲۳).** بند ۶-۸ کامل شد. صفحه‌ی `/ask`: جمله‌ی فارسی → فیلتر ساخت‌یافته → همان نتایجی که کاتالوگ می‌دهد. ماژول `backend/app/nlq/`، تست‌ها در `tests/test_conversational.py`.
>
> - **🔑 تصمیم باز بند ۱۳ («LLM ابری یا محلی») حل شد — ولی نه با انتخاب یکی از آن دو.** معماری طوری است که **هیچ‌کدام لازم نیست**: پیش‌فرض یک پارسر قاعده‌محورِ بدون مدل، بدون شبکه و بدون کلید است (`app/nlq/rules.py`)، و ارائه‌دهنده‌ی ابری یک **ارتقا** است نه یک وابستگی. هر ارائه‌دهنده‌ای که اسکیمای چت OpenAI را بفهمد — DeepSeek، OpenRouter، یا یک llama.cpp محلی — با دو متغیر محیطی وصل می‌شود (`LLM_BASE_URL`، `LLM_API_KEY`). **پس خرید کلید دیگر جلوی هیچ‌چیز را نمی‌گیرد** و از فهرست پیش‌نیازهای بند ۱۲ درآمد.
>   - **چرا این ترتیب:** بند ۱۰ می‌گوید دمو نباید بمیرد. قابلیتی که تنها پیاده‌سازی‌اش به یک فراخوان شبکه، یک روش پرداخت خارجی و کلیدی که منقضی نشده باشد نیاز دارد، روی صحنه می‌میرد. حالا API قطع = جواب کمی کندذهن‌تر، نه خطا. تست `test_the_rules_answer_when_the_model_cannot` همین را روی یک آدرس مرده می‌سنجد، نه روی ماکی که قبول کرده شکست بخورد.
>   - **مدل محلی بررسی و رد شد**: کار موردنیاز استخراج ساخت‌یافته است نه استدلال، و همین حالا DINOv2 و مدل عمق روی همان ماشین‌اند؛ افزودن یک LLM استقرار را سنگین می‌کند تا کاری را انجام دهد که تطبیق واژگان انجامش می‌دهد.
> - **مدل هیچ‌وقت جواب نمی‌دهد، فقط ترجمه می‌کند.** مدلی که کاتالوگ را «بلد» باشد فرش و قیمت و موجودی از خودش درمی‌آورد — روان و باورپذیر، که از بد انجام دادن بدتر است چون کسی نمی‌گیردش. مدل فقط **واژگان** را می‌گیرد (۹ نقش، ۷ جنس، ۱۳ رنگ، ۶ اتاق، بازه‌ی قیمت واقعی) و خروجی‌اش یک `QueryPlan` است که مقابل همان enumهای کاتالوگ اعتبارسنجی می‌شود. رنگی که فروشگاه ندارد **اصلاً قابل بیان نیست**. شعاع انفجار یک توهم، فیلتری کمی غلط است نه محصولی که وجود ندارد.
> - **پرامپت از روی خود enumها ساخته می‌شود** (`app/nlq/prompt.py`)، نه دستی. پرامپت دستی یک نسخه‌ی دوم از تاکسونومی است که اولین بار که رنگی اضافه شود بی‌صدا از آن جدا می‌افتد — مدل همچنان جواب می‌دهد، فقط هیچ‌وقت با مقدار تازه.
> - **هر تصمیم به کاربر گفته می‌شود.** `understood` برای هر برداشت یک عبارت فارسی می‌دهد و صفحه آن‌ها را چیپ می‌کند، با پیوندی به `/carpets?…` با همان فیلترها. جست‌وجویی که بی‌صدا به چهار خانواده‌ی رنگی باریک شده، تصمیمی گرفته که کاربر نمی‌تواند با آن بحث کند؛ و این صفحه راهی **به داخل** کاتالوگ است، نه کاتالوگ دوم.
> - **دو باگی که مرورگر پیدا کرد و خواندن کد پیدایشان نمی‌کرد:**
>   - «ابریشمی» با «یشمی» تطبیق می‌خورد و جست‌وجوی ابریشم را **سبز** می‌کرد. تطبیق زیررشته‌ای در فارسی خطرناک است؛ حالا ابتدای واژه لنگر دارد ولی انتهایش باز است، چون فارسی پسوند می‌چسباند («ابریشمی»، «قرمزه»).
>   - جمله‌ی نامفهوم «چیزی نفهمیدم» می‌گفت و زیرش کل ۷۰ فرش را می‌ریخت. جواب دادن با همه‌چیز یعنی جواب ندادن.
>
> **۴. مشاور چیدمان — ساخته شد (۱۴۰۵/۰۵/۲۳).** بند ۶-۶ کامل شد و **فاز ۴ تمام شد.** `/room-adviser`: عکس اتاق → رنگ کف و رنگ دیوار/مبلمان جدا خوانده می‌شوند → فرش‌های مناسب، هر کدام با جمله‌ی دلیلش. `POST /api/v1/room/adviser`، ماژول‌های `app/room/palette.py` و `app/room/adviser.py`، تست‌ها در `tests/test_room_adviser.py`.
>
> - **دلیل‌ها بعد از رتبه‌بندی نوشته نمی‌شوند؛ خودِ رتبه‌بندی‌اند.** هر قاعده هم یک عدد می‌دهد و هم یک جمله‌ی فارسی، پس جای فرش و توضیحش نمی‌توانند با هم اختلاف داشته باشند — که شکست همیشگی هر «پیشنهاد هوشمند»ی است که اول فهرست می‌سازد و بعد توجیه. یعنی می‌شود با آن بحث کرد: کسی که «اتاق شما کم‌رنگ است» را قبول ندارد، مقدمه را دیده نه حکم را.
> - **هیچ‌چیز اینجا یادگرفته نشده، و عمدی است.** دیتاستی از «این فرش در این اتاق خوب شد» وجود ندارد، پس مدل چیز دیگری را برازش می‌کرد و اسمش را سلیقه می‌گذاشت.
> - **عکس در دو نیمه خوانده می‌شود، چون فرش با دو چیز طرف است:** روی **کف** می‌نشیند (آنجا باید دیده شود — فرش نخودی روی سرامیک نخودی فرشی است که کسی نمی‌بیندش) و با **دیوار و مبلمان** زندگی می‌کند (آنجا هم‌رنگی نکته‌ی مثبت است). ماسک کفی که راهنمای اندازه می‌سازد همین‌جا پیکسل‌ها را تقسیم می‌کند.
> - **🔑 دو باگی که فقط با نگاه کردن به عکس‌های واقعی اتاق پیدا شدند:**
>   - **کرم و قهوه‌ای «رنگِ اتاق» نیستند.** یک پذیرایی آرامِ بژ، ۰٫۷۵ رنگارنگ سنجیده می‌شد، «پُررنگ» دسته‌بندی می‌شد و به آن توصیه می‌شد با فرشی بژ آرام شود. دیوار کرم و چوب قهوه‌ای را تقریباً هر اتاقی دارد؛ رنگِ اتاق آن چیزی است که کسی انتخاب کرده گذاشته، پس هم‌رنگ شدن با قرنیز تصمیم نیست.
>   - **رتبه‌بندی رتبه نمی‌داد.** امتیاز فقط روی *وجود* خانواده‌ی رنگی حساب می‌شد، پس شش فرش با امتیاز دقیقاً یکسان (تا سه رقم اعشار) بالا می‌آمدند. حالا سهم واقعی آن رنگ در اتاق (و در کف) وزن می‌دهد.
> - **تأیید روی عکس‌های واقعی خانه‌ی کارفرما:** اتاقی با مبل‌های ارغوانی، رنگ مبل‌ها `pink` خوانده شد و اولین پیشنهاد «لچک‌ترنج مشهد — زمینه ارغوانی» بود با دلیل «رنگ صورتی را از خود اتاق برمی‌دارد». قاعده‌ی طراح، روی داده‌ی واقعی.
> - **هشدار جدا از دلیل نوشته می‌شود** و رنگ دیگری دارد. «با کف اتاق شما هم‌رنگ است» اگر لای سه تعریف بنشیند خوانده نمی‌شود، و تنها جمله‌ای است که ممکن است جلوی یک خرید اشتباه را بگیرد.
>
> **معیار اتمام فاز ۴ — وضعیت:** هر چهار قابلیت روی ورودی دیده‌نشده کار می‌کنند و در فروشگاه ادغام‌اند (هیچ‌کدام صفحه‌ی جزیره نیست: همه به کاتالوگ و صفحه‌ی محصول تحویل می‌دهند). **آنچه می‌ماند: عدد ارزیابی**، که بند ۵ خودش صریحاً به فاز ۵ سپرده است (Precision@k، خطای اندازه، خطای مقیاس AR).
>
> **قاعده‌ای که از فاز ۳ ارث می‌رسد:** هر صفحه‌ی جدید ترنزیشن اشتراکی کارت‌ها را رایگان می‌گیرد، به شرطی که هیچ فرشی در یک صفحه دو بار نام‌گذاری نشود — سرآیند `frontend/src/lib/view-transition.ts`.

**۵. نمای اتاق از روی عکس (افزوده‌شده ۱۴۰۵/۰۵/۰۷).** کاربر یک عکس از اتاقش می‌دهد؛ سامانه عمق تک‌تصویری را تخمین می‌زند، صفحه‌ی کف را برازش می‌کند، فرش را با پرسپکتیو درست روی آن می‌نشاند و با ماسک انسداد آن را **زیر میز و صندلی** می‌برد. چون تصویر ثابت است، بودجه‌ی پردازش از میلی‌ثانیه به ده‌ها ثانیه می‌رسد و لبه‌ها بسیار تمیزتر از انسداد بلادرنگ گوشی درمی‌آید.
- **جایگزین واقعیت افزوده نیست، مکمل آن است** و به‌صورت دکمه‌ای مستقل ارائه می‌شود. روی iOS که WebXR در دسترس نیست، این حالت کمبود ژست‌های اختصاصی را جبران می‌کند.
- همان نقشه‌ی عمق ورودی موارد ۲ و ۳ را هم تأمین می‌کند: یک پایپ‌لاین، سه قابلیت.
- **وضعیت (۱۴۰۵/۰۵/۰۷): اسپایک زده شد و کار می‌کند، ولی موقتاً پارک شده.** پایپ‌لاین کامل ساخته شد (تخمین عمق متری → سگمنتیشن کف → برازش صفحه → نگاشت پرسپکتیو → ماسک انسداد) و روی عکس‌های واقعی خانه‌ی کارفرما اجرا شد: اطمینان تشخیص کف ۱٫۰، ارتفاع دوربین بازیابی‌شده ۱٫۱۸ تا ۱٫۴۵ متر. صفحه‌ی تعاملی هم ساخته شد (کشیدن، چرخاندن، تعویض سایز روی عکس).
- **نقص باقی‌مانده و راه‌حلش:** در محل تماس پایه‌ی مبلمان با کف، تکه‌های کوچک کف به جسم می‌چسبند و روی فرش دیده می‌شوند. ریشه: ماسک **آستانه‌ای** است، در حالی که پیکسل‌های لبه ذاتاً *مخلوط* (بخشی چوب، بخشی کف) هستند. راه‌حل درست، افزودن یک مرحله‌ی **مَتینگ** (آلفای پیوسته به‌جای ماسک دودویی) است — همان تکنیک جداسازی مو از پس‌زمینه. تا آن زمان این قابلیت به سطح نمونه‌ی تجاری نمی‌رسد.
- **تصمیم کارفرما:** اولویت با فاز ۳ است؛ این قابلیت پس از اتمام فازهای ۳ و ۴ در صورت وجود زمان تکمیل می‌شود.

**معیار اتمام:** هر قابلیت روی ورودی دیده‌نشده کار کند + در فروشگاه ادغام شده باشد (نه صفحه‌ی جزیره) + عدد ارزیابی داشته باشد.

### فاز ۴٫۵ — کاتالوگ از آنِ خودمان ★ ✅ انجام شد (۱۴۰۵/۰۵/۳۰)

کاتالوگ فعلی از دو منبع عمومی آمده (دیتاست فرش ایرانی + یک فروشگاه اینترنتی) و کیفیت عکس‌هایش پایین است: هر فرش یک یا دو عکس، بیشترشان بدون نمای تخت مناسب AR. این فاز جایش را با کاتالوگی می‌گیرد که **تصویرهایش را خودمان سفارش می‌دهیم** — ۴۰ فرش، هر کدام ۵ تصویر تولیدشده با مدل تصویری.

> **این کار جدید نیست، کار معلق ثبت‌شده است.** بند ۷ از همان روز ingest نوشته بود که ۲۹ فرش دیجی‌کالایی پیشوند `dk-` می‌گیرند «تا هنگام جایگزینی با موجودی واقعی با یک پرس‌وجو جدا شوند». این همان جایگزینی است.
>
> **برای پایان‌نامه ارتقاست، نه دور زدن.** فصل داده از «از دو منبع عمومی برداشته شد» به «با یک رویه‌ی مستند تولید شد، و رویه‌اش این است» می‌رود. دومی هم قابل دفاع‌تر است و هم مالِ خودمان — ولی **باید صریح نوشته شود**، و اعداد ارزیابی فاز ۵ آن‌گاه «بازیابی روی داده‌ی مصنوعی» را می‌سنجند.

**۱. طرح پوشش، نه چهل ردیف تصادفی — انجام شد (۱۴۰۵/۰۵/۲۳).** `backend/scripts/catalog_profiles.py`. چهل فرش طوری چیده شدند که هر فیلتری که فروشگاه عرضه می‌کند ارزش فشردن داشته باشد: هر ۹ نقش، هر ۷ جنس، هر ۶ اتاق و **هر ۱۳ خانواده‌ی رنگی** حاضرند و هیچ‌کدام تک‌عضو نیست. چهل تصادفی، دو سه فیلتر را خالی می‌گذارد و هیچ‌چیز در کد متوجهش نمی‌شود.
- ۱۶ دستباف و ۲۴ ماشینی — نسبت واقعیِ یک فروشگاه آنلاین. ۱۵۱ سایز فروشی.
- **قیمت محاسبه می‌شود، تایپ نمی‌شود:** نرخ هر متر مربع بر حسب رده‌ی بافت × مساحت × ضریبی که از خودِ اسلاگ مشتق می‌شود (پس بین دو اجرا تکان نمی‌خورد). دامنه: ۴۰۰ هزار تومان (پادری ۷۰۰ شانه) تا ۴۰۳ میلیون (ابریشم قم ۲۰۰×۳۰۰). آنچه باید حفظ شود نسبت است نه عدد مطلق: ابریشم قمِ قالیچه‌ای حدود سی برابر شش‌متریِ ۱۲۰۰ شانه.
- **رنگ‌ها در پروفایل نیستند.** `colors` و `color_families` هنگام ingest از خودِ عکس خوانده می‌شوند، مثل هر فرش دیگر. آنچه اینجا هست «رنگی که از عکس خواستیم» است، و **اختلافش با آنچه خوانده شد، خودش اطلاعات است** — یعنی سازوکار گرفتن عکسِ اشتباه.

**۲. دویست پرامپت — انجام شد (۱۴۰۵/۰۵/۲۳).** `backend/scripts/catalog_prompts.py`، و خروجی خواندنی‌اش در [dataset/prompts.md](dataset/prompts.md). از روی پروفایل‌ها ساخته می‌شوند، نه کنارشان نوشته — وگرنه نسخه‌ی دومی از کاتالوگ‌اند که با اولین تغییر پروفایل بی‌صدا واگرا می‌شود.
- **🔑 ترتیب اختیاری نیست.** برای هر فرش اول `flat` تولید می‌شود و بعد **همان فایل به‌عنوان تصویر مرجع** ضمیمه‌ی چهار پرامپت بعدی می‌شود. مدل تصویری اگر دو بار مستقل از او «فرش کاشان قرمز» بخواهی، دو فرش متفاوت می‌دهد — و فروشگاهی که عکس کاورش با مدل AR‌اش یکی نیست، دقیقاً در همان جایی دروغ می‌گوید که کل محصول قرار است صادق باشد.
- **`flat` عکس قشنگ نیست، بافت است**، و چهار چیزش باربر است: تخت و کاملاً از روبه‌رو (پایپ‌لاین AR پرسپکتیو را تصحیح و در `.glb` پخت می‌کند)؛ پس‌زمینه‌ی سفید خالص (استخراج رنگ هر پیکسل مات را می‌خواند، پس کف چوبی می‌شود یکی از رنگ‌های فرش)؛ کل فرش بدون هیچ شیئی رویش؛ و نور یکنواخت (لکه‌ی نور روی مدل سه‌بعدی می‌شود لکه‌ی دائمی).
- چهار تصویر دیگر آزادند زیبا باشند، چون چیزی اندازه‌شان نمی‌گیرد. `room` هم‌زمان ورودی تست مشاور چیدمان است.

**۳. تولید تصویر — انجام شد با دست (۱۴۰۵/۰۵/۳۰).** کلید هرگز نرسید و منتظرش نماندیم: هر ۴۰ فرش **دستی در اپ Gemini** ساخته شد، یک گفتگو برای هر فرش، از روی همان پرامپت‌های `dataset/prompts.md`. ۱۶۴ تصویر تحویل شد.
- **`macro` کنار گذاشته شد.** تصویر نزدیکِ پرز در بیشتر فرش‌ها بد درمی‌آمد و کارفرما حذفش کرد؛ ۴ تا که سالم بودند ماندند. پس هر فرش `flat` و `cover` و `room` و `gallery` دارد و هیچ صفحه‌ای ناقص نیست. اگر روزی ساخته شوند، `ingest_catalog.py` بدون تغییر برشان می‌دارد.
- **مرتب‌سازی با چشم انجام شد، نه با نام فایل.** خروجی اپ نام تصادفی دارد (`Gemini_Generated_Image_...`) و اندازه‌ی تصویرها هم شات را لو نمی‌دهد — ۱۴۷ تا از ۱۶۴ تصویر دقیقاً ۸۴۸×۱۲۶۴ بودند. پس کانتکت‌شیت ساخته شد و هر ۱۶۴ تصویر نگاه شد. **صحتش را خودِ نتیجه ثابت می‌کند:** هر ۴۰ پوشه دقیقاً یک `flat` و یک `cover` و یک `room` و یک `gallery` داد، بدون تکراری و بدون جای خالی.
- **سه چیزی که روی `flat` اندازه‌گیری شد، چون بعد از ingest تعمیرشدنی نیست:** حاشیه‌ی یکنواخت (بیشینه ۱٫۳٪ از یک ضلع — یعنی فاجعه‌ی دور اول تکرار نشد)، نسبت ابعاد در برابر سایز شاخص پروفایل (خطای بیشینه ۱٪)، و **ریشه: هر ۴۰ فرش با آنچه پروفایل گفته می‌خواند** — ۳۰ ریشه‌دار، ۱۰ لبه‌دوزی‌شده.

**۳٫۵. پرامپت‌ها سه دور اصلاح شدند، هر سه بار با نگاه کردن به خروجی واقعی.** چهار فرش اول ساخته شد و هر دور یک اشکال داد که از روی متن پرامپت دیدنی نبود:
- **دور اول — پس‌زمینه‌ی سفید غلط بود.** استخراج رنگ فقط پیکسل‌های *شفاف* را رد می‌کند و JPEG تولیدشده آلفا ندارد، پس هرچه فرش نیست هم فرش حساب می‌شود. یک ابریشم قمِ سرمه‌ای با حاشیه‌ی سفید، `white=۳۳٪` خواند و **زیر «سفید» فایل شد**. سه فرش دیگر اتفاقی کادر را پر کرده بودند و درست خواندند. حالا پرامپت می‌گوید فرش کادر را لبه‌به‌لبه پر کند و هیچ پس‌زمینه‌ای نباشد — که برای AR هم بهتر است، چون گوشه‌های فرش می‌شوند گوشه‌های تصویر.
- **دور دوم — تصویر ۲ و ۵ خالی بودند.** فرشِ تنها روی سطح صاف، مثل تصویر بریده‌شده‌ای می‌ماند که روی پس‌زمینه چسبانده باشند؛ مشکل نورپردازی نبود، خالی بودن بود. کف **پارکت** شد (پنج نوع چرخشی)، گوشه‌ی اتاق و سایه‌ی قاب پنجره اضافه شد، گرید صریح نام‌گذاری شد (فیلم مدیوم‌فرمت)، فرش از پایین کادر بیرون می‌زند تا دوربین نزدیک حس شود، و گالری از جعبه‌ی سفید به اتاقی مبله شد (طاقچه با ظرف سفالی، مینیاتور قاب‌شده، پنجره‌ی ارسی).
- **دور سوم — دو چیز ریز که کارفرما دید.** اتاقِ تصویر ۴ نباید **شلخته** باشد (سیم شارژر و گوشیِ در حال شارژ روی کف، تصویر را از طبیعی به بی‌سلیقه می‌برد) و تصویر `flat` باید **ریشه** را نشان دهد. ریشه از خود پروفایل استنتاج می‌شود نه دستی: هر دستبافی ریشه دارد چون ریشه همان چله‌ی گره‌خورده است، و ماشینی فقط وقتی نقشش سنتی باشد — ۳۰ فرش ریشه‌دار، ۱۰ تا لبه‌دوزی‌شده.
- **درس تکرارشونده:** دو بار کلمه‌ی فارسی داخل پرامپت انگلیسی نوشتم («ارسی»، «ریشه») که برای مدل تصویری بی‌معنی است. هر دو به لاتین با توضیح بازنویسی شدند.

**۴. ingest — انجام شد (۱۴۰۵/۰۵/۳۰).** `backend/scripts/ingest_catalog.py`. برخلاف `ingest_folder.py` که واقعیت‌های فرش را از نام فایل حدس می‌زند، این یکی پروفایل را منبعِ حقیقت می‌گیرد و تصویر را با نام پیدا می‌کند — چون اینجا اول متن نوشته شد و بعد عکس سفارش داده شد.
- **فقط `flat` تصویر اصلی است و فقط `flat` امبدینگ می‌گیرد.** همان قاعده‌ی `attach_gallery_images.py` و به همان دلیل: تصویر اصلی همان است که پایپ‌لاین AR تصحیحش می‌کند و گرید نشانش می‌دهد، و هر دو فرش را از روبه‌رو می‌خواهند. ۱۶۴ تصویر نشست، دقیقاً ۴۰ امبدینگ.
- **`intended_colors` کار خودش را کرد.** مقایسه‌ی «رنگی که خواستیم» با «رنگی که خوانده شد» در همان اجرای اول دو فرش را علامت زد، و هر دو واقعاً غلط خوانده شده بودند.

**۵. بازتنظیم آستانه‌های رنگ — انجام شد (۱۴۰۵/۰۵/۳۰)، و باز هم با نگاه کردن.** پیش‌بینی درست بود که تصویر مصنوعی اشباع بالاتری دارد، ولی خطاها **مرزی** بودند نه اشباعی؛ پنج تا، هر پنج با کانتکت‌شیت دیده و بعد اصلاح شدند:
- **زیتونی سبز است، طلایی نیست.** یک کاشان ساده‌ی `h≈۶۰ s≈۰٫۴۶ v≈۰٫۴۲` زیر «طلایی» فایل شده بود. همان استدلالی که در فایل بود («قهوه‌ای رنگ نیست، نارنجیِ تیره است») یک بار دیگر: **زردِ تیره زیتون است**، و زیتون سبز است. بیرجندِ ترنجی سبز هم با همین یکی درست شد.
- **مشکیِ با ته‌رنگ.** یک فرش مدرن مشکی «آبی» خوانده می‌شد. اضافه شد که پیکسل *هم* تیره *و هم* کم‌اشباع مشکی است — و این حرفِ بالای فایل را («تاریکی نبودِ رنگ نیست») نقض نمی‌کند چون **هر دو** را می‌خواهد: سرمه‌ای واقعی `s≈۰٫۸۸` دارد، مشکیِ با ته‌رنگ `s≈۰٫۱۷`.
- **صورتیِ روشن، و آلبالوییِ تیره.** قرمزِ کم‌رنگ و پُرنور صورتی است (فرش کودک)، و مخالفش: بنفشِ ۳۰۰ تا ۳۴۵ درجه با ارزش پایین **ارغوانی** است نه صورتی. سومین باری که در این تابع «ارزش» و نه «فام» خانواده را تعیین می‌کند.
- **نیلی آبی است.** زمینه‌ی ابریشم قم `h≈۲۶۰` است در برابر `h≈۲۱۵` نایین، آن‌قدر بنفش که از فیلتر آبی بیفتد بیرون. مرز به ۲۷۰ رفت.
- **تست‌های قدیمی دست نخوردند و همه سبز ماندند.** دو ادعای `test_color.py` که از ۷۰ عکس *واقعی* آمده بودند اول شکستند و درست هم شکستند: قرمزِ کارمن کاشان (`h≈۳۴۰ s≈۰٫۸۰`) با ارغوانی مشهد (`h≈۳۳۸ s≈۰٫۵۵`) در فام دو درجه فاصله دارند و در **اشباع** جدا می‌شوند، نه در فام. هر ۱۵۰ تست بک‌اند سبز است.
- بعدش `backfill_color.py --force` روی هر ۱۱۴ فرش اجرا شد تا کاتالوگ قدیم و جدید با یک آستانه خوانده شوند.

**۶. مشتق‌ها و انتشار — انجام شد (۱۴۰۵/۰۵/۳۰).** `backend/scripts/build_ar_assets.py` نوشته شد، چون تا حالا دارایی AR فقط یکی‌یکی از پنل ادمین ساخته می‌شد و چهل فرش این شکل را برنمی‌دارد. همان `generate_for_carpet` پروداکشن را صدا می‌زند، نه پیاده‌سازی دوم.
- **۱۵۱ واریانت، هر ۱۵۱ تا `ready`، صفر شکست.** مقیاس واقعی با `inspect_glb.py` بررسی شد: واریانت ۲۰۰×۳۰۰ دقیقاً `X 2.0000 m × Z 3.0000 m` است.
- ۷۴ فرش قدیمی **غیرفعال** شدند نه حذف — سفارش‌های تستی به واریانت‌هایشان وصل‌اند، و برگرداندنشان یک کوئری است. ۴۰ فرش خودمان فعال شدند.

**۷. سه چیزی که تصمیمش گرفته شد (۱۴۰۵/۰۵/۳۱):**
- **فیلتر «پنبه» تک‌عضو می‌ماند، و اشکالی ندارد.** ادعای بند ۱ برای رنگ و نقش و اتاق درست است ولی برای **جنس** نبود: پنبه ۱ و کرک‌وابریشم ۱. نایین ۹ لا اصلاح شد (توضیحش «با گل ابریشم» بود و رده‌ی قیمتش `kork_silk`، فقط `material` جا مانده بود) و کرک‌وابریشم شد ۲. پنبه ۱ باقی می‌ماند: **کارفرما بعداً فرش‌های بیشتری اضافه می‌کند** و هویت یک فرش را برای پُر کردن یک چیپ عوض نمی‌کنیم. عدد پوشش در فصل داده باید همین را بگوید، نه چیز دیگر.
- **ریشه بریده می‌شود، و این حالا خواسته است نه عارضه.** تشخیص گوشه، گوشه‌های *زمینه‌ی بافته* را پیدا می‌کند نه نوک ریشه را. غیر از اینکه از نظر ابعادی درست‌تر است — اندازه‌ی فرش را با ریشه نمی‌گویند — دلیل اصلی‌اش تصویری است: ریشه‌ی تولیدشده پس‌زمینه‌ی سفیدِ خودش را همراه می‌آورد، و روی یک صفحه‌ی تخت در AR آن سفیدی **می‌افتد** و کف اتاق را کثیف می‌کند. یک فرش بی‌ریشه‌ی تمیز در AR بهتر از یک فرش ریشه‌دارِ حاشیه‌سفید است. دور سومِ پرامپت‌ها همچنان درست بود — ریشه در تصویر `flat` هست و در گالری فروشگاه دیده می‌شود؛ فقط به مدل سه‌بعدی نمی‌رسد.
- **عکس‌ها جای خاصی نگه داشته نمی‌شوند.** با بقیه‌ی محصولات فرقی ندارند: از همان `data/` مسیر می‌روند که کاتالوگ قبلی رفت، و پوشه‌های `dataset/NN_slug/` بعد از ingest فقط نسخه‌ی دوم همان پیکسل‌هایند. `.gitignore` حالا هم آن‌ها را می‌گیرد، تا `dataset/` همان چیزی بماند که بند ۲ گفته بود: متنِ قابل‌مرور، نه پیکسل.

**۸. آنچه واقعاً باز است، و کار فاز ۵ است:**
- **عدد بازیابی بصری روی عکس زاویه‌دار پایین است: ۳۸٪ رتبه‌ی اول، ۵۸٪ در سه‌تای اول** (پرسش = عکس `cover` هر فرش، تصویری که ایندکس ندیده است). با عکس `room` که فرش کمتر از یک‌سوم کادر است، ۱۰٪ و ۳۰٪. علتش سازوکاری است نه باگ: DINOv2 **کل کادر** را امبد می‌کند و در عکس اتاق بیشترِ کادر دیوار و پارکت و مبل است، و هیستوگرام رنگ هم رنگ پارکت را می‌شمارد. API هیچ برشی روی تصویر پرسش نمی‌زند. این همان عددی است که فاز ۵ باید ثبت و بهترش کند — راه روشن، بریدنِ فرش از تصویر پرسش پیش از امبد کردن.

**۹. کارت فرش با عکس چیده‌شده شروع می‌شود، نه با نمای تخت.** بعد از اینکه چهل فرش کنار هم نشستند معلوم شد گریدِ نماهای تخت، بیست‌وچهار مستطیلِ نقش است بدون هوا، و یک **کناره** با نسبت ۰٫۲۶ در قابِ ۰٫۷۵ نواری است رهاشده وسط کارت. حالا کارت روی عکس `cover` می‌نشیند و نمای تخت را زیر موس نشان می‌دهد.
- `cover_image` به خروجی لیست اضافه شد: «اولین عکسی که primary نیست». قرارداد تازه‌ای نیست — `ingest_catalog.py` تخت را در position صفر می‌گذارد و پنل ادمین اولین آپلود را primary می‌کند. برای فرشِ تک‌عکس `null` است و کارت همان یکی را نگه می‌دارد.
- **`is_primary` دست نخورد.** همچنان تخت است، چون همان است که AR تصحیحش می‌کند و جست‌وجوی بصری امبدش می‌کند. فقط گرید دیگر از آن نمی‌پرسد چه نشان دهد.
- **روی گوشی هیچ تعویضی نیست، و هیچ هزینه‌ای هم ندارد:** لایه‌ی دوم زیر `(hover: none)` مقدار `display: none` می‌گیرد، و تصویر lazy داخل جعبه‌ی مخفی هرگز به کادر دید نمی‌رسد که دانلود شود — روی ویوپورت موبایل، هر ۲۴ کارت لایه دارند و **صفر تا** دانلود شد. این گارد باید صریح نوشته می‌شد: Tailwind v4 بعضی یوتیلیتی‌های hover را در `@media (hover: hover)` می‌پیچد ولی `group-hover:opacity-100` را نه، و یک تپ روی صفحه‌ی لمسی `:hover` را بالا می‌آورد.
- جدول مقایسه، سینی مقایسه، سرچ هدر و پنل ادمین عمداً همان نمای تخت را نگه داشتند: آنجا فرش سنجیده می‌شود، نه عرضه.
- **صفحه‌ی محصول هم با همان عکس باز می‌شود و نمای تخت دومی است.** وگرنه انیمیشن مشترک، یک عکس را در عکسی *دیگر* محو می‌کرد. ترتیب از روی `is_primary` خوانده می‌شود نه از روی position، چون آن پرچم تنها چیزی است که تضمین‌شده یعنی «تخت» — چه فرش از ingest آمده باشد چه از آپلود پنل ادمین. هر ۴۰ فرش بررسی شد: کارت و صفحه‌ی محصول روی یک عکس می‌نشینند.

**۱۰. ورق خوردن گالری جهت پیدا کرد.** تعویض عکس یک محوشدنِ عمودی بود و «در هوا آب شدن» حس می‌شد. علتش ساختاری بود نه سلیقه‌ای: قاب و لایه‌ی متحرک **یک** المان بودند، پس تنها چیزی که می‌توانست تغییر کند شفافیت بود — هر حرکتی، خودِ قاب را با بوردر و گردی‌اش تکان می‌داد.
- حالا قاب ثابت است (و همان است که انیمیشن مشترک در آن رشد می‌کند) و عکس درونش سُر می‌خورد. راست‌به‌چپ: عکس دوم سمت *چپ* عکس اول است، پس ورق زدن به جلو یعنی نوار به راست می‌رود — تازه از لبه‌ی چپ می‌آید و قبلی از لبه‌ی راست بیرون می‌رود. عقب‌گرد آینه‌ی همین است. همان قرارداد علامتی که درگ و کلیدهای جهت از قبل داشتند.
- **جهت پیش از wrap حساب می‌شود.** رفتن از آخرین عکس به اولی، `index = imageCount` می‌خواهد که هنوز از فعلی بزرگ‌تر است، پس رو به جلو خوانده می‌شود هرچند روی صفر می‌نشیند. اگر مقدارِ wrap‌شده مقایسه شود، آخرین ورقِ یک دور برعکس بازی می‌کند.
- `initial={false}` روی `AnimatePresence` نشست نه روی فرزند: `useReducedMotion` روی سرور و مرورگر یک جواب نمی‌دهد، و همان دام قبلاً `opacity: 0` را در HTML نوشته بود. کم‌تحرکی حالا با **مدت‌زمان** بیان می‌شود نه با مقدارِ متفاوت، پس استایلی که رندر می‌شود به آن هوک بند نیست. خروجی سرور بررسی شد: `opacity:1;transform:none`.

**معیار اتمام:** ✅ کاتالوگ فقط از تصویرهای تولیدشده‌ی خودمان است (۴۰ فرش فعال، ۷۴ فرش قدیمی غیرفعال)؛ ✅ هر فرش نمای تختِ AR‌پذیر دارد و هر ۱۵۱ واریانت `glb`/`usdz` با مقیاس واقعی؛ ✅ هر ۱۳ خانواده‌ی رنگی و هر ۹ نقش و هر ۶ اتاق بیش از یک محصول برمی‌گردانند (جنسِ پنبه یکی است و بند ۷ می‌گوید چرا اشکالی ندارد)؛ ⬜ فصل داده‌ی پایان‌نامه رویه را ثبت کند — کار فاز ۶.

### فاز ۵ — امنیت، تست، استقرار، ارزیابی — **در جریان**
سخت‌سازی (اعتبارسنجی ورودی/فایل، rate limit، هدرهای امنیتی، مرور حفره‌ها)؛ تست‌های واحد و یکپارچه + **E2E اسموک** (جریان خرید و ورود ادمین با Playwright)؛ استقرار Compose + Caddy روی VPS + دامنه؛ پشتیبان‌گیری شبانه؛ نوت‌بوک‌های ارزیابی: Precision@k، خطای اندازه، خطای مقیاس AR در برابر baseline تجاری، نرخ موفقیت پایپ‌لاین AR روی N عکس.
**معیار اتمام:** محصول روی دامنه بالا، از گوشی غریبه قابل استفاده؛ CI سبز؛ اعداد ارزیابی ثبت‌شده.

> **وضعیت (۱۴۰۵/۰۵/۳۱):** ارزیابی، امنیت و E2E انجام شدند. استقرار **ساخته شد ولی اجرا نشد** — هیچ VPSای در کار نیست و Docker روی این ماشین بالا نیامد؛ جزئیاتش در بند ۵ پایین. اندازه‌گیری خطای AR با متر همچنان کار کارفرماست (بند ۱۲).
>
> **۱. ارزیابی بازیابی بصری — و بهبودی که از دلش درآمد.** بدهی‌ای که فاز ۴٫۵ بند ۸ ثبت کرده بود بسته شد. `backend/app/eval/retrieval.py` + `scripts/eval_retrieval.py`؛ مجموعه‌ی پرسش ۱۲۰ عکس است که ایندکس هیچ‌کدام را ندیده (فقط `flat` امبد می‌شود، پس `cover`/`room`/`gallery` هر ۴۰ فرش دیده‌نشده‌اند) و برچسبشان از نام فایل می‌آید.
>
> | | رتبه‌ی اول، قبل | رتبه‌ی اول، بعد |
> |---|---|---|
> | همه (۱۲۰ پرسش) | ۱۷٫۵٪ | **۴۵٫۸٪** |
> | `cover` | ۳۷٫۵٪ | **۶۷٫۵٪** |
> | `room` | ۱۰٪ | **۳۵٪** |
> | `gallery` | ۵٪ | **۳۵٪** |
>
> MRR از ۰٫۳۰۰ به ۰٫۵۶۰. اعداد «قبل» دقیقاً همان‌هایی درآمدند که فاز ۴٫۵ ثبت کرده بود (۳۸٪ و ۱۰٪) — یعنی هارنس همان چیزی را می‌سنجد که آن روز دستی سنجیده شده بود.
>   - **🔑 تشخیص‌دهنده‌ی فرش ساخته شد و دور انداخته شد.** راه‌حلی که خود نقشه پیشنهاد داده بود («بریدن فرش از تصویر پرسش») اول با همان تکنیک تشخیص گوشه‌ی AR پیاده شد. روی ۱۲۰ تصویر **سه بار** فعال شد و هر سه بار **قاب خطاطیِ روی دیوار** را برید: مستطیل روشنِ محدب روی زمینه‌ی ساده، دقیقاً همان چیزی که امتیازش به آن جایزه می‌دهد. کد در `app/eval/crop.py` مانده و در اجرای کامل به‌عنوان پیکربندی رد‌شده اندازه‌گیری می‌شود. **درس: تشخیص‌دهنده‌ای که وقتی مطمئن است اشتباه می‌کند، از نبودِ تشخیص‌دهنده بدتر است** — چون جواب را دور می‌ریزد نه اینکه رقیقش کند.
>   - **راه‌حلی که ماند: هیچ تشخیصی.** کادر به پنجره‌های هم‌پوشان بریده می‌شود، هر پنجره جست‌وجو می‌شود، و هر فرش بهترین امتیازِ هر پنجره را نگه می‌دارد (`app/services/query_windows.py`). هیچ‌چیز لازم نیست تصمیم بگیرد فرش کجاست. **کادر کامل خودش یکی از پنجره‌هاست**، پس نتیجه نمی‌تواند از حالت قبل بدتر دربیاید.
>   - **۷ پنجره از ۱۹ پنجره بهتر شد** (۴۵٫۸٪ در برابر ۴۵٫۰٪). شبکه‌ی ۱۹تایی عمداً ولخرجانه ساخته شد تا **بگوید کدام پنجره‌ها کار می‌کنند**: از ۱۲۰ جواب درست، ۶۳ تا از یک پنجره آمد — پایین‌وسط کادر — و ۹۱ تا از ردیف پایین. شش پنجره یک بار هم برنده نشدند. با max-pooling، پنجره‌ی بیشتر لزوماً بهتر نیست: هر پنجره یک شانس تازه برای فرشِ *غلط* است که روی یک تکه پارکت امتیاز خوبی بگیرد.
>   - **هزینه‌اش هفت برابر نیست.** `EmbeddingBackend.embed_batch` اضافه شد و هر هفت پنجره در یک forward pass می‌روند. کارِ CPU روی نخ جدا می‌رود (`anyio.to_thread`)، مثل اندپوینت‌های اتاق.
>
> **۲. ارزیابی پایپ‌لاین AR.** `app/eval/ar_assets.py` + `scripts/eval_ar.py`. روی هر ۴۰ فرش × هر ۱۵۱ سایز × سه نوع عکس = **۴۵۳ اجرا**: ۱۰۰٪ هر دو فایل را نوشت، ۱۰۰٪ اندازه‌ی درست، **بدترین خطای مقیاس ۰٫۰۰۰۰٪**. ابعاد از داخل خودِ `.glb` خوانده می‌شوند نه از ورودی سازنده، چون سازنده‌ای که اشتباه مقیاس بزند با خودش کاملاً موافق است.
>   - **🔑 دو چیز متفاوت را با یک عدد گزارش نکنید.** نسخه‌ی اول «اطمینان تشخیص گوشه» را داخل «موفقیت» تا کرده بود و نتیجه «۰٪ موفقیت» شد — درباره‌ی مجموعه‌ای از فایل‌ها که **همه‌شان درست بودند**. اطمینان، *تشخیص‌دهنده* را نمره می‌دهد؛ پایین که باشد به کل کادر برمی‌گردد، و برای عکس تختی که فرش لبه‌به‌لبه پرش کرده همان جواب درست است. حالا دو ستون جدا: **اندازه‌ی درست ۱۰۰٪، اطمینان تشخیص ۲۳٫۲٪** (روی `flat` ‏۶۹٫۵٪، روی عکس‌های سبک‌دار صفر).
>   - نصف دیگر بند ۳-۱ — خطا در برابر متر روی کف واقعی — کد ندارد و ندارد که داشته باشد. کاری که این عدد می‌کند تفسیرپذیر کردنِ آن است: اگر فایل تا میکرومتر درست باشد و گوشی ۴٪ بدهد، خطا در جلسه‌ی AR است نه در پایپ‌لاین.
>
> **۳. ارزیابی راهنمای اندازه.** `app/eval/sizing.py` + `scripts/eval_sizing.py`. ۲۱۶ صحنه‌ی مصنوعی (۸ مقیاس × ۳ ارتفاع دوربین × ۳ چرخش برگه × ۳ فاصله): **۹۹٫۵٪ برگه پیدا شد، خطای میانه ۱٫۰۹٪، بدترین ۳٫۶۹٪، فقط ۳ مورد از ۲۱۵ بالای ۳٪.**
>   - **مصنوعی، و این انتخاب دلیل دارد:** عکس اتاق واقعی حقیقت زمینی ندارد، پس خطا در برابرش خطا در برابر یک تخمین دیگر است. اینجا صفحه‌ی کف و دوربین و جای برگه ساخته می‌شوند.
>   - **🔑 آنچه دقت را تعیین می‌کند، بزرگیِ برگه در کادر است** — و همان تنها چیزی است که خریدار کنترلش می‌کند. ۱۰۰+ پیکسل: خطای میانه ۰٫۶۱٪. زیر ۵۵ پیکسل: ۱٫۷۶٪، و هر سه موردِ بالای ۳٪ همان‌جایند. تصحیح گوشه تا حدود نیم پیکسل کار می‌کند و بر طول ضلع تقسیم می‌شود؛ ریاضیاتش همین است.
>   - **دو بار صحنه‌ی اشتباه ساختم و هر دو بار عدد، تشخیص‌دهنده را متهم کرد.** با دوربین افقی، کفِ ۱٫۴ متر پایین‌تر کاملاً زیر کادر می‌افتد: ۷۲ صحنه بدون هیچ برگه‌ای، «۰٪ پیدا شد»، که مثل یک نتیجه‌ی فاجعه‌بار درباره‌ی کد به نظر می‌رسید و باگِ صحنه بود. با شیب ثابت ۲۵ درجه هم در ارتفاع چشم باز برگه از پایین کادر می‌رفت بیرون (۲۴ از ۷۲). هر دو یک اشتباه‌اند: **کسی که این عکس را می‌گیرد به برگه نگاه می‌کند**، پس شیب دوربین از روی جایی که برگه باید در کادر بیفتد مشتق می‌شود نه فرض.
>
> **۴. سخت‌سازی.** ماژول `app/core/security.py` و آزمون‌هایش در `tests/test_security.py` (۱۵ آزمون، همه روی کدِ پیش از فاز ۵ قرمز می‌شوند).
>   - **هدرهای امنیتی روی هر پاسخ**، به‌صورت میان‌افزار ASGI خالص نه `BaseHTTPMiddleware` — چون تنها مسیری که *واقعاً* لازمشان دارد `StaticFiles` است که بایت‌های آپلودشده‌ی یک غریبه را سرو می‌کند و از لایه‌ی روتینگ رد نمی‌شود. `nosniff`، `frame-ancestors 'none'`، `Referrer-Policy`، `Permissions-Policy`؛ و HSTS فقط بیرون از حالت توسعه (سرور توسعه‌ی HSTS‌دار مرورگر را به https://localhost سنجاق می‌کند).
>   - **سقف حجم بدنه، *پیش از* خوانده شدن.** هر چهار اندپوینت آپلود اول `await file.read()` می‌کردند و بعد اندازه را چک می‌کردند — چکی که وقتی خسارت خورده اجرا می‌شود. حالا `Content-Length` رد می‌شود و بایت‌ها هم حین رسیدن شمرده می‌شوند (چون آپلود chunked طول اعلام نمی‌کند و آپلود ناصادق هر طولی که بخواهد اعلام می‌کند)، و `read_upload` به‌محض عبور از سقف قطع می‌کند.
>   - **محدودیت نرخ روی اندپوینت‌های گران.** تنها چیزی که محدود بود، ورود ادمین بود — یعنی ارزان‌ترین چیز اینجا. حالا جست‌وجوی بصری (۲۰/دقیقه)، اندپوینت‌های اتاق (۱۰/دقیقه، هرکدام ~۴ ثانیه CPU) و گفتگویی (۳۰/دقیقه). کلید محدودیت `request.client.host` است و **نه** `X-Forwarded-For`: هدر را هرکسی می‌نویسد، و محدودکننده‌ای که روی مقدار انتخابیِ مهاجم کلید بخورد بی‌نهایت کلید دارد. آدرس Caddy به‌جایش به uvicorn داده می‌شود (`FORWARDED_ALLOW_IPS`، در compose ثابت است).
>   - **ورود ادمین حالا زمان‌ثابت است.** نام کاربری غلط در چند میکروثانیه رد می‌شد و رمز غلط در ~۲۰۰ میلی‌ثانیه‌ی bcrypt — یعنی **زمان پاسخ می‌گفت کدام نیمه غلط بوده**، دقیقاً همان چیزی که متن خطا با دقت نمی‌گوید. حالا bcrypt همیشه اجرا می‌شود، حتی وقتی اصلاً ادمینی تنظیم نشده.
>   - **پیکربندی پیش‌فرض توسعه بیرون از توسعه، سرور را بالا نمی‌آورد.** `SESSION_SECRET` پیش‌فرض یعنی هرکسی که این مخزن را خوانده می‌تواند کوکی ادمین جعل کند، و `ADMIN_PASSWORD_HASH` خالی یعنی پنلی که کسی نمی‌تواند واردش شود. هر دو بی‌صدا خراب می‌شوند؛ امتناع در startup بلند است.
>   - **مدل هنگام راه‌اندازی گرم می‌شود** (بدهی ثبت‌شده‌ی فاز ۴: اولین جست‌وجو پس از هر restart ‏۱۲٫۴ ثانیه بود). روی نخ جدا تا سرور فوراً درخواست بپذیرد. **این هم یک باگ داشت که به‌خاطر طراحیِ خودش بی‌صدا بود:** تصویر گرم‌کننده را به‌صورت hex نوشته بودم و hex خراب بود؛ گرم‌کردن خطایش را عمداً می‌بلعد، پس مدل هیچ‌وقت گرم نمی‌شد و لاگش را کسی نمی‌خواند. حالا تصویر با PIL کشیده می‌شود. **هرچه شکستش بی‌صداست باید با کدی ساخته شود که نتواند نامحسوس اشتباه کند.**
>
> **۵. تست E2E — و دو باگ محصولی که فقط همین‌جا پیدا می‌شدند.** Playwright، ۸ سناریو روی دسکتاپ و موبایل (۱۶ اجرا، همه سبز). خودش API و بیلد پروداکشن سایت را روی پورت‌ها و دیتابیس خودش (`farsh_e2e`) بالا می‌آورد، با کاتالوگ سه‌فرشیِ `scripts/seed_e2e.py` نه کاتالوگ دمو.
>   - **🔑 دکمه‌ی «افزودن به سبد» روی فرش ناموجود، خریدِ نشده را تأیید می‌کرد.** کد قدیم فقط داخل `onAdd` زودهنگام return می‌کرد، پس دکمه کاملاً فعال بود: اسپینر می‌چرخید، تیک کشیده می‌شد، «به سبد اضافه شد» می‌گفت، و سبد خالی می‌ماند. کامنت بالای همان کد نوشته بود «دکمه نباید وانمود کند». حالا `AddToCart` پراپ `disabled` دارد و «ناموجود» می‌گوید.
>   - **🔑 سفارش با کلیک روی «ادامه» ثبت می‌شد، نه با «ثبت سفارش».** دکمه‌ی پایین فرم شرطی است (`{last ? submit : next}`) و React هر دو شاخه را یک `Button` در یک جای درخت می‌بیند، پس **همان المان DOM** را نگه می‌دارد و فقط `type` و `onClick`ش را عوض می‌کند. مرورگر عمل پیش‌فرضِ یک کلیک را *بعد* از هندلر ارزیابی می‌کند، روی `type`ای که آن لحظه دارد — یعنی یک فشار، هم به گام آخر می‌رفت و هم سفارش را ثبت می‌کرد. **خریدار گام مرور را هرگز نمی‌دید و کد رهگیریِ خریدی را می‌گرفت که تأیید نکرده بود.** بک‌اند ۲۰۱ می‌داد و همه‌ی تست‌ها سبز بودند. با `key` جدا حل شد.
>   - **درسی برای نوشتن خودِ تست‌ها:** اولین انتظار «به سبد اضافه شد» بی‌ارزش بود — سه برچسب دکمه در یک سلول grid روی هم‌اند و با `opacity` عوض می‌شوند، و Playwright المان با `opacity: 0` را **visible** می‌شمارد. هر بار فوراً سبز می‌شد، از جمله وقتی هیچ‌چیز اضافه نشده بود.
>   - **یک نقص دسترس‌پذیری هم همین‌جا درآمد:** در `checkout-form.tsx` و `carpet-form.tsx` برچسب‌ها `<label>` بدون `htmlFor` و کنارِ فیلد بودند — یعنی اصلاً برچسب نبودند. صفحه‌خوان روی فیلد نام چیزی نمی‌گفت و تپ روی متن، فیلد را فوکوس نمی‌کرد. حالا کل فیلد داخل `<label>` است (بستنِ فیزیکی، نه id که باید در هر فراخوانی درست نوشته شود).
>
> **۶. استقرار — ساخته شد، اجرا نشد.** `backend/Dockerfile`، `frontend/Dockerfile`، `infra/compose.prod.yml`، `infra/Caddyfile`، `infra/backup.sh`، `infra/.env.example`. `output: "standalone"` به Next اضافه شد. **هیچ‌کدام روی Docker واقعی اجرا نشده‌اند** چون Docker Desktop روی این ماشین بالا نبود و VPSای هم در کار نیست (بند ۱۲). تا اجرا نشوند، هر ادعایی درباره‌شان ادعاست.
>   - **فقط Caddy پورت منتشر می‌کند.** API و فروشگاه و پستگرس روی شبکه‌ی داخلی‌اند و از بیرون در دسترس نیستند — پستگرسِ باز به اینترنت رایج‌ترین راهِ از دست رفتن یک استقرار کوچک است.
>   - **DINOv2 هنگام build دانلود می‌شود، نه در اولین درخواست.** `torch.hub.load` وگرنه یک فراخوان شبکه روی مسیر بحرانیِ اولین جست‌وجوی خریدار است، روی سروری که ممکن است پشت فیلتر باشد، و شکستش یک ۵۰۰ است مدت‌ها بعد از اینکه استقرار موفق به نظر رسیده. چرخ‌های CPU هم صریح انتخاب شده‌اند (پیش‌فرض، بیلد CUDA است).
>   - **پشتیبان دو نیمه دارد یا هیچ.** `pg_dump` تنهایی کاتالوگی را برمی‌گرداند که تک‌تک عکس‌ها و فایل‌های AR‌اش لینک شکسته‌اند، چون آن‌ها در یک volume‌اند نه در پستگرس. اسکریپت هر دو را با هم می‌گیرد، اول در پوشه‌ی `.partial-` می‌نویسد و آخرش rename می‌کند (پشتیبانِ نصفه نباید از پشتیبانِ کامل قابل تشخیص نباشد)، و **روش بازگردانی پایین خودش نوشته شده** — پشتیبانی که کسی بلد نیست برگرداندش وجود ندارد.
>   - **تصمیمی که با نقشه فرق دارد و باید تأیید شود: MinIO نیست.** بند ۴ برای استقرار MinIO گفته بود. اینجا یک volume نام‌دار است و Caddy مستقیم `/files` را سرو می‌کند. دلیل: در مقیاس یک فروشگاه، MinIO یک سرویس و یک جفت کلید و یک مسیر کد اضافه می‌کند که نوشته و آزموده نشده، در ازای سازگاری S3 که امروز هیچ‌کس نمی‌خواهد. قرارداد URL دست‌نخورده است، پس اگر لازم شد بعداً بدون تغییر کد اپلیکیشن عوض می‌شود — که کل دلیل وجود آن لایه بود.
>
> **۷. CI سبز شد — و دو باگ داد که ویندوز نمی‌توانست ببیند (۱۴۰۵/۰۶/۰۱).** برنچ `catalog-of-our-own` **هیچ‌وقت push نشده بود**: ۱۳۰ کامیت، یعنی تمام فازهای ۳ تا ۵، لوکال روی ویندوز نوشته و آزموده شده بودند و **یک بار هم روی لینوکس اجرا نشده بودند**. بند ۱۱ می‌گوید «CI سبز در هر کامیت» و تریگر روی `main` و PR بود — یعنی روی هیچ‌کدام از این ۱۳۰ کامیت. حالا روی هر برنچی اجرا می‌شود.
>   - **جاب E2E از همان بار اول سبز شد.** پستگرس و pgvector، seed، ساخت دارایی AR، بیلد پروداکشن Next (با فونت گوگل)، مرورگر Playwright و هر هشت سناریو — روی ماشینی که هیچ‌کدامشان تا آن لحظه رویش نرفته بودند.
>   - **🔑 گاردِ راه‌اندازی، حاضر نبود import شود.** `assert_production_ready` از داخل `create_app` صدا زده می‌شد و آن، زمان import اجرا می‌شود؛ ولی `scripts/export_openapi.py` اپ را فقط برای **خواندن اسکیمایش** import می‌کند. روی رانری که `.env` ندارد، بررسی قرارداد API با پیام «SESSION_SECRET پیش‌فرض است» می‌مرد — خطایی که هیچ ربطی به چیزی که شکسته بود نداشت. **امتناع از *سرو کردن* با رمز توسعه درست است؛ امتناع از *خوانده شدن* قاعده‌ای است که کسی نخواسته بود.** رفت داخل lifespan، جایی که «از راه‌اندازی امتناع کن» همان معنایی را می‌دهد که می‌گوید — با تستی که هر دو نیمه را نگه می‌دارد: ساختن اپ مجاز است، وارد شدن به lifespanش نه.
>   - **🔑 تایپ‌چک پیش از بیلدی اجرا می‌شد که تایپ‌هایش را می‌سازد.** `tsc` از `next-env.d.ts` می‌فهمد که `import hero from "…/hero-1.webp"` مجاز است، و آن فایل را **بیلد می‌سازد** و گیت‌ایگنورِ خودِ Next کنارش می‌گذارد — پس روی هر ماشین توسعه‌ای هست و روی هیچ رانری نیست. نتیجه: سه تصویرِ کامیت‌شده و موجود، «پیدا نشد» گزارش شدند. حالا بیلد اول است. تایپ‌چک می‌ماند، چون بیلد جایش را نمی‌گیرد: بیلد صفحه‌ها را چک می‌کند، نه فراخوان بدتایپِ داخل هوکی که به آن نمی‌رسد.
>
> **۸. آنچه از فاز ۵ باقی است:** ⬜ استقرار واقعی روی VPS + دامنه (منتظر بند ۱۲)؛ ⬜ خطای مقیاس AR با متر روی هر دو گوشی (منتظر بند ۱۲)؛ ⬜ بیلد لوکال ایمیج‌های داکر (منتظر بالا آمدن Docker Desktop).

### فاز ۶ — پایان‌نامه و دفاع
پیش‌نویس فصل‌ها از پایان هر فاز جمع شده؛ اینجا: نهایی‌سازی، **هم‌راستاسازی متن با معماری نهایی** (متن فعلی proposal قدیمی است)، اسلاید، دو تمرین کامل دمو.
**معیار اتمام:** سند کامل + اسلاید + دموی تمرین‌شده با برنامه‌ی پشتیبان.

## ۹. روش اجرا

- هر فاز با خروجی **قابل اجرا و کامیت‌شده** تمام می‌شود.
- قرارداد API و مدل داده پیش از UI؛ سیستم طراحی پیش از صفحات.
- مرز هر فاز: مرور کد، اجرای کامل تست‌ها، به‌روزرسانی همین سند.
- ریسک‌های بزرگ زودتر تست می‌شوند (نمونه: اسپایک AR روز اول).
- تصمیم‌های بزرگ (سیستم طراحی، انتخاب LLM، هر انحراف از این سند) با تأیید کارفرما.

## ۱۰. برنامه‌ی روز دفاع (دمو نباید بمیرد)

- مسیر ۱: نسخه‌ی مستقر (VPS + دامنه).
- مسیر ۲: صفحه‌ی AR روی GitHub Pages (از قبل کش‌شده روی گوشی).
- مسیر ۳: اجرای کامل لوکال روی لپ‌تاپ + شبکه‌ی محلی + QR.
- دو گوشی تست‌شده (اندروید WebXR + آیفون Quick Look) با فرش‌های قهرمان؛ دو تمرین کامل.

## ۱۱. اصول ثابت

- 🔒 هیچ رمز/کلیدی در گیت. 📦 داده‌ی سنگین خارج از گیت. ✅ CI سبز در هر کامیت. 🧱 کد ماژولار (نوت‌بوک فقط ارزیابی). 🛡️ روی ورودی جدید نشکند.

## ۱۲. کارهای کارفرما

- [ ] عکس/اسکن ۴–۵ فرش واقعی + ابعاد مترشده.
- [ ] تصمیم Docker Desktop (پیش از کارهای دیتابیسی فاز ۱).
- [ ] فاز ۳: نظر روی سیستم طراحی + ایده‌ها و منابع خودش.
- [x] ~~فاز ۴ (در صورت LLM ابری): کلید API.~~ **دیگر پیش‌نیاز نیست (۱۴۰۵/۰۵/۲۳).** جست‌وجوی گفتگویی روی پارسر قاعده‌محور کار می‌کند و کلید فقط ترجمه را بهتر می‌کند. اگر کلیدی تهیه شد، `LLM_BASE_URL` و `LLM_API_KEY` در `backend/.env` کافی است.
- [ ] بررسی «ترنجان» در دهخدا و جست‌وجوی بازار، و آزاد بودن دامنه — پیش از سفارش لوگو.
- [ ] فاز ۵: VPS (ترجیحاً خارج از ایران) + دامنه. **حالا مسدودکننده است** — فایل‌های استقرار نوشته شده‌اند و تا سروری نباشد اجرا نمی‌شوند.
- [ ] فاز ۵: **اندازه‌گیری خطای مقیاس AR با متر**، روی اندروید و آیفون، روی یک فرش با ابعاد معلوم. تنها نصفِ خودکارنشدنیِ بند ۳-۱؛ نصف دیگرش اندازه‌گیری شد و خطای فایل صفر است، پس هرچه متر نشان دهد مربوط به جلسه‌ی AR است.
- [ ] فاز ۵: **Docker Desktop روی ماشین توسعه بالا بیاید** تا ایمیج‌های استقرار حداقل یک بار لوکال build و اجرا شوند، پیش از اینکه روی VPS امتحان شوند.

## ۱۳. تصمیم‌های باز

- [ ] جهت بصری و سیستم طراحی — ابتدای فاز ۳.
- [x] ~~رفتار تغییر اندازه در AR~~ — حل شد: حالت پیش‌فرض فقط بین سایزهای واقعی انبار جابه‌جا می‌شود؛ تغییر اندازه‌ی پیوسته پشت یک حالت مستقل «اندازه‌ی دلخواه» قرار گرفت که نقش ابزار اندازه‌گیری دارد و نسبت ابعاد در آن قفل است.
- [x] ~~LLM گفتگویی: ابری یا محلی — فاز ۴.~~ **حل شد (۱۴۰۵/۰۵/۲۳): هیچ‌کدام، اجباری.** پارسر قاعده‌محور پیش‌فرض است و هر ارائه‌دهنده‌ی سازگار با OpenAI پشت دو متغیر محیطی وصل می‌شود. دلیلش در بلوک «۳. جست‌وجوی گفتگویی» فاز ۴ آمده.
- [ ] منبع تقویتی دیتا اگر دیتاست‌ها کافی نبود — فاز ۱.
- [x] ~~**ذخیره‌ساز استقرار: MinIO یا volume ساده؟**~~ **حل شد (۱۴۰۵/۰۵/۳۱): volume ساده، و بند ۴ اصلاح می‌شود.** سه دلیل، که سومی تازه است و تعیین‌کننده بود:
  1. **صفر خط کد جدید.** `app/services/storage.py` همین حالا دقیقاً همین است؛ MinIO یک بک‌اند دوم می‌خواهد که نوشته و آزموده نشده.
  2. **یک سرویس کمتر روی صحنه.** بند ۱۰ می‌گوید دمو نباید بمیرد.
  3. **🔑 MinIO مسیر میزبانی رایگان را می‌بندد.** کارفرما تا روز دفاع VPS نخواهد داشت و می‌خواهد پروژه یک لینک عمومی داشته باشد که به سرمایه‌گذار یا فروشنده نشان دهد. هیچ لایه‌ی رایگانی یک سرویس ذخیره‌سازِ همیشه‌روشن نمی‌دهد، ولی فایل‌های روی دیسک را می‌شود مستقیم داخل ایمیج پخت — و کاتالوگ اینجا خواندنی است، پس این کافی است.
  - **در را نمی‌بندد:** قرارداد URL دست‌نخورده است و آن لایه از روز اول برای همین ساخته شد. هر وقت فروشگاه از یک سرور بزرگ‌تر شد، تعویضش کد اپلیکیشن را تغییر نمی‌دهد.

## ۱۴. باگ‌ها و کارهای گزارش‌شده‌ی کارفرما (۱۴۰۵/۰۶/۰۱)

هفت مورد که کارفرما پس از استفاده‌ی واقعی از محصول گزارش کرد. **ترتیب زیر ترتیب اجراست، نه ترتیب گفته شدن** — معیارش این است: اول چیزی که به پول یا اعتماد خریدار دست می‌زند، بعد چیزی که دمو را می‌کُشد، بعد صحت خودِ AR که تمایز محصول است، و آخر قابلیت‌های تازه.

سه مورد اول از جنسی‌اند که **تست سبز نگهشان داشته** — یعنی بستنشان باید با آزمونی همراه شود که روی کدِ امروز قرمز شود، وگرنه دوباره برمی‌گردند.

| # | کار | چرا این‌جای صف | وضعیت |
|---|---|---|---|
| ۱ | ~~**مرحله‌ی تأیید نهایی تسویه رد می‌شود**~~ | خریدار بدون دیدن فاکتور، خرید تأییدنشده را ثبت می‌کند | ✅ **بسته شد** |
| ۲ | ~~**پس از لاک شدن گوشی، صفحه قفل می‌ماند**~~ | کاربر مجبور است اپ را ببندد و باز کند؛ روی صحنه‌ی دفاع یعنی دموی مرده | ✅ **بسته شد** — نیاز به تأیید روی گوشی |
| ۳ | ~~**بازسازی دارایی AR پس از ویرایش ابعاد، تکه‌ای از فرش را کل فرش نشان می‌دهد**~~ | محصولِ غلط در AR؛ به صحت داده دست می‌زند | ✅ **کد بسته شد** — بازسازی داده با مورد ۴ |
| ۴ | ~~**بافت AR تاریک و کم‌جزئیات است**~~ | نقش فرش روی زمین باید روشن و باجزئیات باشد؛ این تمایز اصلی محصول است | ✅ **بسته شد** — روشنایی نیاز به تأیید روی گوشی |
| ۵ | ~~**ذره‌بین ویرایشگر گوشه حرکت‌های ریز را نمی‌فهمد**~~ | ابزار داخلی، ولی همان جایی است که مورد ۳ اصلاح می‌شود | ✅ **بسته شد** |
| ۶ | ~~**نوار ناوبری پایین برای گوشی و تبلت**~~ | قابلیت تازه‌ی رو به کاربر | ✅ **بسته شد** |
| ۷ | ~~**اپ جدا برای پنل ادمین**~~ | قابلیت تازه، کوچک‌ترین دامنه | ✅ **بسته شد** |

### شرح هر مورد، با آنچه هنگام گزارش معلوم بود

**۱. ~~تسویه: صفحه‌ی «خرید موفق» پیش از فاکتور نهایی می‌آید.~~ ✅ بسته شد (۱۴۰۵/۰۶/۰۱).** کارفرما: «نشانی را وارد می‌کردم، مرحله‌ی بعدی می‌زد، موفق می‌آمد» — و مرحله‌ی مرور **فقط وقتی دیده می‌شد که موجودی کافی نباشد**، یعنی خریدار فاکتورش را دقیقاً روی مسیری می‌دید که خرید شکست خورده بود.

- **این از خانواده‌ی همان باگی بود که فاز ۵ بند ۵ بسته اعلامش کرد** — دکمه‌ی شرطی که React همان المان DOM را برایش نگه می‌داشت. آن اصلاح (دو `key` جدا) سر جایش است و درست است؛ **کافی نبود.**
- **بازتولید نشد، و همین راه را نشان داد.** دو فرضیه ساخته و هر دو رد شدند: لمس به‌جای کلیک (تست تازه‌ی `tap` روی پروژه‌ی موبایل سبز است) و ارسال ضمنی فرم. ولی گشتن دنبالشان یک حفره‌ی واقعی‌تر را پیدا کرد.
- **🔑 `onSubmit` هیچ‌وقت نمی‌پرسید روی کدام مرحله است.** تنها چیزی که جلوی ثبتِ زودهنگام را می‌گرفت، صفتِ `type` روی دکمه‌ی فوتر بود — و **فرم ارسال‌کننده‌هایی دارد که هیچ صفتی بر آن‌ها حکم نمی‌کند**: ارسال ضمنیِ مرورگر با Enter، و هر کلیکی که مرورگر *پس از* تغییر DOM می‌سازد. بلند کردن مستقیم رویداد submit از مرحله‌ی تماس، سفارش را ثبت می‌کرد؛ با همه‌ی فیلدها معتبر و فاکتوری که هرگز رندر نشده بود.
- **دلیل اینکه «رفتن» کیبورد گوشی همین حالا این کار را نمی‌کند، یک تصادف در استاندارد HTML است:** ارسال ضمنی روی فرمی که بیش از یک فیلد تک‌خطی و هیچ دکمه‌ی submit دارد سرکوب می‌شود، و مرحله‌ی تماس دقیقاً دو فیلد دارد. **یک فیلد کمتر، و این یک باگ زنده بود.**
- **علاج جایی رفت که تصمیم گرفته می‌شود، نه روی دکمه:** سفارش از مرحله‌ی مرور ثبت می‌شود یا اصلاً ثبت نمی‌شود. در برابر همان `step`ای سنجیده می‌شود که این رندر از رویش ساخته شده — و همین است که حالتِ خطرناک را می‌گیرد: submitای که در **همان tickِ** جلو رفتن می‌رسد، مرحله‌ی *قدیم* را می‌خواند و رد می‌شود. این دقیقاً همان چیزی است که کارفرما دیده بود.
- **🔑 درسی درباره‌ی خودِ آزمون: ادعا روی صفحه بی‌ارزش بود.** `requestSubmit()` بلافاصله برمی‌گردد و POST هنوز در راه است، پس «سفارش ثبت شد» در هر دو حالت چند صد میلی‌ثانیه غایب است — آزمون **با گاردِ برداشته‌شده هم سبز می‌شد**. حالا روی خودِ **درخواست** ادعا می‌کند و بدون گارد قرمز می‌شود. سه آزمون تازه در [shop.spec.ts](frontend/e2e/shop.spec.ts).
- **تأیید کارفرما:** روی دسکتاپ و اندروید دوباره آزموده شد و درست است.

**۲. ~~قفل‌شدن صفحه پس از برگشتن از لاک گوشی.~~ ✅ بسته شد (۱۴۰۵/۰۶/۰۱).** تاچ و همه‌چیز می‌ایستد و فقط بستن و باز کردن اپ درستش می‌کند.

- **ریشه، و از جنسی که سرنخ اولیه اشتباه نشانش می‌داد.** حدس اول rAF بود (سند نمایش‌داده‌نشده تیک نمی‌زند). ولی موتور ورودی از این جان سالم به‌در می‌برد: با برگشتن، `t - last` بزرگ می‌شود و فصل به انتهایش می‌پرد. **مشکل جای دیگری بود.**
- **🔑 ورودی صفحه را نگه می‌دارد و فقط وقتی رهایش می‌کند که `armed` شود** — و سه `await` بدون هیچ مهلتی سر راهِ `armed` شدن ایستاده بودند: پروبِ AVIF که جوابش از یک `onload` می‌آید و مرورگر در صفحه‌ی مخفی حق دارد به تعویقش بیندازد؛ `fetch` نخستین فریم، که وقتی رادیوی خوابیده دیگر جواب نمی‌دهد **reject نمی‌شود**؛ و `createImageBitmap` همان پاسخ. `READY_BUDGET_MS` فقط یک گامِ میانی را می‌پوشاند، نه این‌ها را.
- تا `armed` نشود `data-intro="play"` می‌ماند، یعنی `overflow: hidden` روی بدنه و یک مودال `fixed` روی کل فروشگاه: **صفحه‌ای که هیچ‌جا اسکرول نمی‌شود و هیچ لمسی را جواب نمی‌دهد.** دقیقاً همان گزارش.
- **علاج، `visibilitychange` نیست.** پرسیدن «کدام‌یک از آن سه گیر کرد» پرسش غلط است؛ یک تایمر هر سه را با هم جواب می‌دهد، و تایمرِ throttleشده یا فریزشده **هنگام برگشتن صفحه شلیک می‌کند** — یعنی دقیقاً همان لحظه‌ای که بازدیدکننده لازمش دارد. `ARM_DEADLINE_MS = 12000`؛ پس از آن موتور static می‌شود: فریم پایانی به‌عنوان تصویر ثابت، armed، یک ژست تا فروشگاه.
- **یک راه فرار دوم که مالِ خودِ بازدیدکننده است.** ژست اول همچنان نگه داشته می‌شود (معمولاً یک لحظه پیش از قابل‌پخش شدن فصل اول می‌رسد و همین بازپخشش «پاسخگو» بودن ورودی را می‌سازد). ولی نگه داشتنِ ژست **دوم** همان چیزی است که حالت بارگذاری را به در قفل تبدیل می‌کند — پس دومی وارد فروشگاه می‌شود.
- **🔑 درسی از خودِ آزمون: شکل اولش غلط بود و ادعا می‌کرد صفحه خودش را رها می‌کند.** نمی‌کند و نباید بکند — ورودی‌ای که وسط نبودنِ بازدیدکننده خودش را کنار بکشد، تنها چیزی را که برایش وجود دارد با خودش می‌برد. آنچه باید درست باشد **پاسخگو بودن** است: یک ژست، یک فروشگاه. دو آزمون در [intro.spec.ts](frontend/e2e/intro.spec.ts)، هر دو بدون اصلاح قرمز؛ دومی پیش از فشردن هیچ کلیدی از مهلت رد می‌شود تا مهلت را بسنجد نه راه فرار را.
- ⬜ **باقی‌مانده: تأیید روی گوشی واقعی.** سناریوی لاک با شبیه‌سازی «فریم‌ها نمی‌رسند» بازتولید شد، نه با لاک واقعی. کارفرما باید یک بار روی گوشی خودش بیازماید.

**۳. ~~بازسازی دارایی AR پس از ویرایش ابعاد در پنل.~~ ✅ کد بسته شد (۱۴۰۵/۰۶/۰۱).** فایل تازه ساخته می‌شود و **بخش کوچکی از فرش به‌عنوان کل فرش** نشان داده می‌شود.

- **🔑 ریشه: منبع به `rectified_url or url` حل می‌شد، و rectify خودتوان (idempotent) نیست.** هیچ‌چیز `rectified_url` را تا پایان نخستین ساخت نمی‌نویسد، پس اجرای اول عکس را warp می‌کرد و **هر اجرای بعدی، نتیجه‌ی همان را**. تشخیص گوشه روی فرشِ از قبل rectify‌شده یک مستطیل *درونی* پیدا می‌کند — حاشیه‌ی داخلی یا ترنج — و به آن می‌بُرد. دو بار یعنی یک تکه؛ سه بار یعنی تکه‌ی کوچک‌تر.
- **نیمه‌ی بدترش، پنل بود.** `suggest_corners` روی `url` اندازه می‌گیرد، یعنی هر دستگیره‌ای که فروشنده می‌کشد در فضای پیکسلی *عکس* است، در حالی که سازنده همان اعداد را روی تصویری با ابعاد کاملاً متفاوت اعمال می‌کرد. **اصلاح یک برشِ بد، آن را از فرش دورتر می‌کرد.**
- **به‌صورت یک ثابت بیان شد، نه به‌صورت نشانه:** بازسازی چیزی که کسی عوضش نکرده باید همان چیزی را بدهد که قبلاً داده بود. ذخیره‌ساز محتوا-آدرس است، پس این ادعا فقط یک تساوی روی URL است و هیچ فایلی لازم نیست باز شود.
- **آن پیش‌نمایشی که این بلوک برایش وجود داشت، هیچ‌وقت خوانده نمی‌شد:** هیچ اسکیمایی `rectified_url` را منتشر نمی‌کند و هیچ صفحه‌ای درخواستش نمی‌کند. یعنی تنها اثرش امروز همین باگ بود. ماند، چون نیتش خوب است، ولی حالا **هر اجرا تازه می‌شود** — نوشتنِ یک‌باره یعنی آن تک‌تصویری که قرار بود اصلاحِ فروشنده را نشان دهد، تصویرِ *پیش از* اصلاح بود.

**۳٫۱. حسابرسی کاتالوگ زنده — و چیزی که فقط با نگاه کردن معلوم شد.** پس از اصلاح، هر ۴۰ فرش فعال بدون نوشتن چیزی حسابرسی شدند (ساخت در پوشه‌ی موقت و مقایسه‌ی هش با URL ذخیره‌شده).
- **۲۰ از ۴۰ با بازسازیِ تمیز فرق داشتند.** آزمون تفکیک‌کننده نشان داد هیچ‌کدام از مسیرِ «rectify روی rectify» نیامده‌اند — پس عدد به‌تنهایی می‌گفت «دلیلش گوشه‌ی دستی است» و **این نتیجه‌گیری غلط بود.**
- **🔑 با نگاه کردن معلوم شد که هر ۲۰ تا واقعاً خراب‌اند.** بافتِ ذخیره‌شده‌ی `qom-silk-lachak-sormei` فقط گوشه‌ی بالا-چپ فرش است و `kashan-kodak-firoozei` یک تکه از متن. یعنی **۲۰ فرش کاتالوگ زنده همین حالا در AR تکه‌ای از خودشان را کل خودشان نشان می‌دهند.** ← [[verify-visual-outputs]]
- ✅ **بازسازی داده انجام شد** پس از مورد ۴ (هر ۱۵۱ واریانت، صفر شکست).

**۳٫۳. حاله‌ی سیاه دور فرش — ✅ بسته شد (۱۴۰۵/۰۶/۰۲).** کارفرما یک فرش واقعی را روی پارچه‌ی تیره عکس گرفت و از پنل ساخت؛ فایل AR با یک نوار تیره‌ی پس‌زمینه دور فرش درآمد.
- **🔑 تشخیص، کلِ کادر را برگردانده بود — با اطمینان ۰٫۸۸۹.** پس `needs_review` غلط بود، پنل هیچ اخطاری نداد و فروشنده هرگز نفهمید باید نگاه کند.
- **امتیازدهی این کار را می‌کند:** `regularity × (0.5 + 0.5 × coverage)` هم بابت مستطیل بودن پول می‌دهد هم بابت بزرگ بودن، و **مرزِ خودِ تصویر** یک مستطیل بی‌نقص است که همه‌چیز را می‌پوشاند: یک ۱٫۰ تضمینی که هیچ فرش واقعی نمی‌تواند ببرد. هر وقت پس‌زمینه تا لبه‌ها برسد — کف، پارچه، دیوار — Canny آن مرز را می‌بندد و می‌برد.
- حذف ترمِ coverage کمکی نمی‌کند؛ قاب با regularity تنها هم ۱٫۰ می‌گیرد. **قاب باید حذف شود، نه شکست بخورد.**
- **و حذفش هیچ هزینه‌ای ندارد، چون خودش fallback است.** چهارضلعی به آن اندازه دقیقاً همان چیزی را می‌گوید که «فرشی پیدا نشد» می‌گوید. جایی که فرش واقعاً کادر را پر می‌کند — هر تصویر کاتالوگ تولیدی — همان چهار گوشه با **صفر** برمی‌گردد، که راست می‌گوید.
- **🔑 نزدیک بود بدترش کنم.** جاروب آستانه‌های Canny روی همان عکس کاندیدایی با پوشش **۰٫۷۵۶** پیدا کرد — تقریباً دقیقاً سهم واقعی فرش از کادر، عددی کاملاً قانع‌کننده. کشیدنش روی تصویر نشان داد چهارضلعیِ کجی است که هیچ لبه‌ای را دنبال نمی‌کند، و امتیازش ۰٫۵۱۶ بود؛ آن‌قدر نزدیک آستانه که ممکن بود بی‌پرچم رد شود. **عددی که با فرضیه‌ات جور دربیاید، شاهد نیست.** ← [[verify-visual-outputs]]
- با گوشه‌های دستی، بافت **بدون حاله و بدون ریشه** درمی‌آید — یعنی پایین‌دست پایپ‌لاین سالم بود.
- **آزمون هم درس دارد:** بافتِ پارچه به‌تنهایی این را بازتولید نمی‌کند، چون کانتوری که از هر چهار طرف از تصویر بیرون بزند چهارضلعی بسته نیست. آنچه یکی می‌بندد **یک پله‌ی تُنی درست داخل کادر** است — لبه‌ی عادیِ یک عکس عادی. هشت پیکسلش کافی است و تشخیص با ۰٫۹۹۹ برش می‌دارد.
- ⬜ **فرش تست خود کارفرما هنوز فایل قدیمی را دارد:** حالا در `‎/admin/ar` پرچم «بازبینی لازم است» می‌خورد؛ چهار گوشه را در ویرایشگر بکشید و دوباره بسازید.

**۳٫۲. دو چیزی که همین حسابرسی پیدا کرد و کار مورد ۴ است:**
- **🔑 بافت AR از `‎/files/card/` ساخته می‌شود — ۵۳۷×۸۰۰** — و بعد تا `max_texture_px = 2400` بزرگ‌نمایی می‌شود. یعنی نقش فرش روی زمین از تصویری می‌آید که لبه‌ی بلندش ۸۰۰ پیکسل است. این مستقیماً همان «تاریک و کم‌جزئیات» مورد ۴ است.
- **تشخیص گوشه روی `kashan-kodak-firoozei` با اطمینان ۰٫۵۸۷ (بالای آستانه‌ی ۰٫۵۵) وسطِ خودِ فرش می‌بُرد** — عکس لبه‌به‌لبه است و کل کادر جواب درست بود. ۱۱ فرش با اطمینان صفر به کل کادر برمی‌گردند و درست‌اند؛ این یکی دقیقاً در میانه می‌افتد. **باز هم همان درس فاز ۵: تشخیص‌دهنده‌ای که با اطمینان اشتباه می‌کند از نبودش بدتر است.**

**۴. ~~کیفیت و روشنایی بافت AR.~~ ✅ بسته شد (۱۴۰۵/۰۶/۰۲).** نقش و نگار فرش روی زمین باکیفیت، روشن و باجزئیات نیست. دو نیمه بود و هر نیمه جای دیگری.

- **🔑 نیمه‌ی تیزی: پایپ‌لاین از `‎/files/card/` می‌خواند — ۸۰۰ پیکسل، همان مشتقی که برای بندانگشتیِ گرید ساخته شده.** در حالی که `texture_url` (مشتق ۲۰۴۸) در خودِ مدل کامنت دارد: «ورودی پایپ‌لاین AR». هر فرش فروشگاه روی بافت ۸۰۰ پیکسلی بافته و بعد تا ۲۴۰۰ **بزرگ‌نمایی** می‌شد.
- **و `max_texture_px` هم هدف بود نه سقف.** مستقیم در لبه‌ی بلند نوشته می‌شد، پس چهارضلعیِ ۶۰۰ پیکسلی تا ۲۴۰۰ کشیده می‌شد و آن مگابایت‌های اضافه **درون‌یابی** بودند — یعنی تاری، که گوشی پیش از دیدن فرش دانلودش می‌کند. حالا بلندترین ضلعِ خودِ چهارضلعی سقف صادقانه است: نتیجه ۱۲۶۴ پیکسل عکسِ واقعی در فایلی **کوچک‌تر**.
- **منبع حالا از یک تابع مشترک (`ar_source_url`) خوانده می‌شود** که هم سازنده و هم ویرایشگر گوشه صدایش می‌زنند. دو خواننده که این را متفاوت بفهمند، همان باگ مورد ۳ است؛ پس خواننده‌ی دوم حذف شد.
- **این تغییر رزولوشن، یک شکستِ تشخیص را مجانی درست کرد.** `kashan-kodak-firoozei` روی کارت با اطمینان ۰٫۵۸۷ وسط فرش می‌بُرید؛ روی تصویر کامل‌تر کل کادر با ۰٫۹۶ برمی‌گردد. در کل کاتالوگ، کوچک‌ترین برش از ۷۴٪ به **۸۹٫۵٪** کادر رسید.
- **🔑 نیمه‌ی روشنایی، در بافت نبود — اندازه‌گیری شد** (تصویر rectify‌شده چند سطح با منبعش فاصله دارد، نه بیشتر). در لایه‌ی جای‌گذاری بود، و اشکالش **مفهومی** است: تخمین نور، نورِ *مطلق* اتاق را می‌سنجد و کلمپ‌ها اجازه می‌دادند ambient از ۱٫۰ خنثی تا **۰٫۲۵** پایین برود — فرشی با یک‌چهارم روشنایی در اتاق معمولی.
- **ولی کفی که فرش رویش است این‌طور نورپردازی نشده:** آن تصویرِ دوربین است و اکسپوژر خودکار گوشی از قبل روشنش کرده تا عادی دیده شود. پس تاریک کردنِ فرش برای تطبیق با یک اندازه‌گیریِ فیزیکی، آن را در برابر پس‌زمینه‌ای تاریک می‌کند که حاضر نشده تاریک شود — و **تنها چیز کم‌نورِ کادر، خودِ محصول می‌شود.** اکسپوژر دوربین خواندنی نیست، پس تخمین دیگر مقیاس مطلق فرض نمی‌شود: **جهت** می‌ماند (واقعی است و سایه را سمت درست می‌اندازد) و **شدت** یک تلنگر حول خنثی می‌شود.
- هر ۱۵۱ واریانت بازسازی شد؛ ۲۰ فرشِ خراب دوباره کامل‌اند. ⬜ **روشنایی باید روی گوشی واقعی تأیید شود** — نور AR را فقط دستگاه واقعی نشان می‌دهد.
- **🔑 درسی که این بند دو بار داد: هش می‌گوید «فرق دارد»، و این با «غلط است» یکی نیست.** حسابرسیِ عددی نتیجه گرفت «پس گوشه‌ها دستی بوده‌اند» و آن نتیجه‌گیری غلط بود؛ فقط نگاه کردن به تصویرها نشان داد هر ۲۰ تا واقعاً خراب‌اند. ← [[verify-visual-outputs]]

**۵. ~~ذره‌بین ویرایشگر گوشه روان نیست.~~ ✅ بسته شد (۱۴۰۵/۰۶/۰۲).** با انگشت آرام‌آرام حرکت داده می‌شود ولی یک‌دفعه جهشی می‌پرد.

- **🔑 جهش مالِ *فشار* بود، نه کشیدن.** `pointerdown` گوشه را به همان‌جایی می‌برد که فشار فرود آمده، و ناحیه‌ی لمس ۴۴ پیکسل است حول نشانِ ۱۸ پیکسلی — پس نوک انگشت، که هرجای آن ۴۴ پیکسل می‌نشیند، **پیش از آنکه کشیدنی شروع شود** گوشه را تا بیست پیکسل صفحه پرت می‌کرد.
- **و بیست پیکسل صفحه، بیست پیکسل فرش نیست:** عکس کمی بیش از ۳۰۰ پیکسل کشیده می‌شود و بیش از ۸۰۰ پیکسل است، پس هر پیکسل صفحه **دو و نیم** پیکسلِ عکس است. اندازه‌گیری روی پنل واقعی: فشاری که اصلاً حرکت نکرد، گوشه را **۳۶ پیکسلِ عکس** جابه‌جا کرد.
- **چرا هیچ‌وقت گرفته نشده بود:** ماوس دقیقاً وسطِ نشان کلیک می‌شود، پس جهش صفر است. این باگ فقط با انگشت وجود دارد.
- **علاج: کشیدن نسبی.** فاصله‌ی انگشت تا گوشه در لحظه‌ی گرفتن ثبت و در طول ژست نگه داشته می‌شود — تا انگشت تکان نخورد هیچ‌چیز تکان نمی‌خورد، و بعد دقیقاً به اندازه‌ی انگشت. ضمناً حالا انگشت می‌تواند **کنارِ** نقطه بنشیند نه رویش، که از اول کارِ ذره‌بین بود.
- **🔑 سه شکلِ غلطِ آزمون، که هر سه ارزش ثبت دارند:** (۱) سنجش جعبه‌ی ویوپورتِ دستگیره، گوشه‌ای را که تکان نخورده بود «۲ پیکسل جابه‌جا» گزارش کرد، چون فشردن دستگیره فوکوسش می‌کند و فوکوس صفحه را اسکرول می‌کند؛ حالا درصدِ خودِ گوشه خوانده می‌شود. (۲) فشار روی مختصات خامِ داخلِ *جعبه‌ی* دستگیره به **عکس** می‌خورد نه دستگیره — دستگیره دایره‌ای به شعاع ۲۲ است و hit-test گردی را رعایت می‌کند، پس گوشه‌ی جعبه رویش نیست؛ آزمون بی‌صدا سبز می‌شد. (۳) نخستین شکست، یک overshoot چهاربرابری، **اصلاً باگ اپ نبود** — آزمون پیش از نشستن چیدمان اندازه گرفته بود.
- **آنچه نبود:** نه رویداد گم می‌شد، نه حرکت ریز نادیده گرفته می‌شد. کشیدن با ماوس در پنج اجرای پیاپی دقیقاً ۱:۱ بود و چیدمان در حالت سکون کاملاً پایدار است. ← تنها حدِ باقی‌مانده، **مقیاس نمایش** است: ریزترین حرکتِ ممکن یک پیکسل صفحه = ۲٫۵ پیکسل عکس. اگر دقتِ بیشتر لازم شد، ضریبِ ریزکاری (حرکت انگشت تقسیم بر بزرگ‌نماییِ ذره‌بین) کارِ بعدی است و **تصمیمش با کارفرماست**، چون حسِ کنترل را عوض می‌کند.

**۶. ~~نوار ناوبری پایین برای گوشی و تبلت.~~ ✅ بسته شد (۱۴۰۵/۰۶/۰۲).** چون کاربر موبایل کمتر سراغ منوی همبرگری می‌رود.

- **چرا واقعاً لازم بود:** مگامنو و کشو هر دو **پشت همبرگرند**، و کسی همبرگر را باز نمی‌کند تا کشف کند فروشگاه با عکس جست‌وجو می‌کند. سه چیزی که کل محصول برایشان وجود دارد — جست‌وجوی بصری، مشاور چیدمان، راهنمای اندازه — روی همان دستگاهی که بیشتر مردم دستشان می‌گیرند **سه تپ عمق** داشتند.
- **پنج مقصد:** فرش‌ها · جست‌وجوی بصری · مشاور چیدمان · راهنمای اندازه · سبد خرید. خانه و فرش‌ها یکی شدند (کارفرما: «خروجی‌شان یکی است»). **علاقه‌مندی‌ها عمداً بیرون ماند** و سبد ماند: هر دو در سرآیندند، ولی سرآیند با اسکرول جمع می‌شود و علاقه‌مندی‌ها یک قلب روی تک‌تک کارت‌ها هم دارد؛ سبد تنها جایی است که فاصله‌اش روی «خرید انجام می‌شود یا نه» اثر دارد.
- کامپوننت `arunachalam/bottom-nav-bar` از 21st، انتخابِ کارفرما از میان ۱۲ لینک ([[21st-component-picking-workflow]]، [[frontend-build-rules]]).
- **لینک و `usePathname`، نه دکمه و `useState`.** نسخه‌ی اصلی ایندکس فعال را خودش نگه می‌داشت، که نوار را از **نقشه** به **کنترل** تبدیل می‌کند: بعد از دکمه‌ی برگشت تبِ غلط را روشن می‌کرد و هیچ مقصدی URL نبود. `/carpets` مالک `/carpets/{slug}` هم هست، وگرنه دیدنِ یک فرش، تبی را که تو را آورده خاموش می‌کند.
- **بدون انیمیشن ورود.** نوار ماندگاری که موقع mount فنر می‌خورد، در هر ناوبری دوباره اجرا می‌شود — و `initial` تنها جایی است که باید روی `useReducedMotion` شاخه بزند، یعنی همان ناهماهنگی hydration که یک بار هزینه‌اش داده شده.
- **🔑 رنگ حالت فعال اندازه‌گیری شد، انتخاب نشد.** `--accent` روی چیپِ ته‌رنگ‌دار **۴٫۳۲:۱** است و برچسبِ کنار آیکون متنِ ۱۲٫۵ پیکسلی است که ۴٫۵ بدهکار است. آیکون به‌تنهایی قبول می‌شد، ولی «آیکون قبول است و کلمه‌ی کنارش نه» حالتی نیست که این طراحی داشته باشد. `--accent-strong` روی همان چیپ **۵٫۴۹:۱** است. آیکون‌های خاموش ۴٫۷۷:۱.
- **تداخل با سینی مقایسه حل شد:** هر دو کف صفحه‌اند و سینی `sticky` در لایه‌ی بالاتر است، پس ماندنِ نوار آن را پنهان نمی‌کند بلکه **نیمه‌پوشیده** می‌کند که مثل باگ دیده می‌شود. سینی کاری است که خریدار شروع کرده و نوار محیطی است؛ کار برنده است.
- **فوتر spacer دارد و اینجا بی‌خطر است، به همان دلیلی که برای سینی بی‌خطر نبود:** ارتفاع این نوار **ثابت** است و با متن داخلش تکان نمی‌خورد. هم نوار و هم spacer `env(safe-area-inset-bottom)` را حمل می‌کنند، وگرنه آخرین خط فوتر دقیقاً روی همان گوشی‌هایی که این نوار برایشان اضافه شد زیر نوار سیستم می‌ماند.
- زیر `lg` فقط؛ دسکتاپ سرآیند کامل دارد. پنج آزمون در [bottom-nav.spec.ts](frontend/e2e/bottom-nav.spec.ts).

**۶٫۱. عرض برچسب — اصلاح پس از دیدن (۱۴۰۵/۰۶/۰۲).** کارفرما: «جست‌وجوی بصری کامل جا نمی‌شود و زشت شده».
- **🔑 و فقط همان یکی، که خودش نشانه بود.** جعبه‌ی برچسب یک عدد ثابت بود — ۸۴ پیکسل — با کامنتی که می‌گفت «از روی بلندترین برچسب اندازه‌گیری شده». اندازه‌گیری واقعی: آن کلمه در وزیرمتنِ ۱۲٫۵ پیکسلی **۹۰٫۳** پیکسل است. چهار برچسب کوتاه‌تر جا می‌شدند و **آن یکی که کل دلیل وجود این نوار است** وسط کلمه بریده می‌شد.
- حالا `auto` است. عدد ثابت، شرط‌بندیِ بی‌صدا روی هر برچسبی است که بعداً نوشته شود، و برچسب فارسی کوتاه نیست.
- **عرضی که می‌خواست هنوز وجود نداشت:** چهار آیتم خاموش روی کف ۴۴ پیکسلی‌شان ۱۷۶ پیکسل خرج می‌کنند و آیتم باز با آن کلمه ۱۴۳ می‌خواهد. روی ۳۷۵ فقط ۵ پیکسل جا بود و روی **۳۶۰ (اندرویدِ رایج)** پیل به سقف `max-w-[95vw]` می‌خورد و همان کلمه با مکانیزمی دیگر بریده می‌شد. `gap-0.5` و `p-1.5` دوازده پیکسل پس می‌دهند و سقف شد «عرض دید منهای ۶ پیکسل از هر طرف» — ۳۴۸ در برابر ۳۳۹ لازم، سنجیده روی همان گوشه‌ای که شکست می‌خورد.
- **زیر ۳۶۰ هیچ حسابی جواب نمی‌دهد** (آیکون‌ها به‌تنهایی ۱۷۶ و آیتم باز ۱۴۳)، پس آن‌جا کلمه **اصلاً کشیده نمی‌شود** به‌جای اینکه نصفه کشیده شود. ته‌رنگ همچنان می‌گوید کدام فعال است و `aria-label` همچنان نامش را می‌گوید.
- **آزمون روی «متن از جعبه‌اش پهن‌تر نیست» ادعا می‌کند، نه روی یک عدد** — عددی که هرکس متن را عوض کند باید بازنویسی‌اش کند، و برای برچسبی که به یک ثابتِ تازه بریده شده هم سبز می‌شود.

**۷. ~~اپ جدا برای پنل ادمین.~~ ✅ بسته شد (۱۴۰۵/۰۶/۰۲).** لوگوی متفاوت، تم متفاوت، و ورود مستقیم به پنل.

- **دامنه با کارفرما تأیید شد: منیفست دوم روی همین پروژه، نه کدبیس دوم.** و لوگو «همین لوگو با یک تغییر — تمش تیره شود»، نه هویت دوم.
- **مشکلی که حل شد:** نصب از `/admin` **فروشگاه** را می‌داد — نام فروشگاه، آیکون روشن فروشگاه، و `start_url` روی `/`؛ یعنی اپی که روی همان کاتالوگی باز می‌شود که فروشنده می‌خواست از آن دور شود.
- **روت‌هندلر، نه `manifest.ts`:** قرارداد متادیتای Next برای منیفست **فقط ریشه** است و کپی‌اش زیر `app/admin/` اصلاً برداشته نمی‌شود — از آن هیچ‌هایی که یک ساعت شبیه مشکل کش به نظر می‌رسد.
- **🔑 `scope` باربر است.** بدون آن، پنلِ نصب‌شده کل مبدأ را از آنِ خود می‌داند، پس دنبال کردن یک لینک به فروشگاه، فروشنده را **داخل پنجره‌ی پنل و با رنگ تم پنل** نگه می‌دارد. `id` هم از همین خانواده: دو منیفست روی یک مبدأ با `id` از هم تشخیص داده می‌شوند و بازگشت به `start_url` یعنی تغییر بعدیِ `start_url` به‌عنوان **اپ دیگری** خوانده می‌شود و نسخه‌ی دومی کنار اولی نصب می‌کند.
- **آیکون همان نشان است، و پالت هم دست‌ساز نیست:** همان سه توکنِ `[data-surface="admin"]` که خودِ سطح پنل استفاده می‌کند، پس ویرایش آن بلوک نمی‌تواند از کاشیِ لانچر واگرا شود. نتیجه: لانچری که هر دو را دارد، کاشی روشن = فروشگاه، کاشی تیره = پنل.
- `assets/brand/mark-dark.png` سیم‌کشی شده ولی لازم نیست: لوگوی واقعی معمولاً آرتِ تیره روی شفاف است و آرت تیره روی زمینه‌ی `#14131A` یک مربع سیاه است — پس روزی که لوگوی واقعی آمد، نسخه‌ی روشنش آنجا می‌رود.
- آزمون لینک، `content-type`، `start_url`، `scope`، `id` و **۲۰۰ بودن هر آیکونی که منیفست وعده داده** را می‌سنجد؛ منیفستی که ۴۰۴ فهرست کند باز هم نصب می‌شود، با کاشی خالی.

## ۱۵. فهرست دوم کارفرما (۱۴۰۵/۰۶/۰۲) — ✅ هر چهار مورد بسته شد

**۱. ~~مشاور چیدمان و راهنمای اندازه «عکس فرش» می‌خواستند، نه «عکس اتاق».~~ ✅**
- دراپ‌زون از **درون خودش** می‌گفت «عکس فرش». برای جست‌وجوی بصری درست بود و برای دو صفحه‌ای که *اتاق* می‌خوانند غلط: مشاور، کف را برای جای فرش و دیوار و مبلمان را برای رنگ‌ها می‌خواهد، و راهنمای اندازه کف را با یک برگ A4. هر دو فرش می‌خواستند و بعد اتاق را در هرچه رسیده بود اندازه می‌گرفتند.
- **🔑 متنِ راهنمای زیرِ همان دکمه از اول درست بود** («عکسی بگیرید که هم کف اتاق و هم دیوارها…») — یعنی تیتر و راهنما مستقیماً هم را نقض می‌کردند و کسی ندیده بود.
- حالا `subject` یک پراپ **اجباری بدون پیش‌فرض** است. پیش‌فرض همان چیزی بود که این را ممکن کرد: کامپوننت درباره‌ی چیزی که جمع می‌کند نظر داشت و دو فراخوان بی‌آنکه بنویسندش آن را به ارث بردند.

**۲. ~~آیکون فرش برای نوار پایین.~~ ✅**
- آیتم اول `LayoutGrid` بود — «شبکه‌ای از چیزها»، که درباره‌ی هر صفحه‌ی فهرستی در دنیا صادق است. در فروشگاهی که یک نوع شیء می‌فروشد، آیتمی که یعنی «فرش‌ها» باید فرش باشد.
- **هیچ ست آیکونی فرش ندارد** — نه lucide که بقیه‌ی پروژه از آن است، نه Phosphor؛ پس دپندنسی دوم هیچ نمی‌خرید. با گرامر lucide کشیده شد ([rug-icon.tsx](frontend/src/components/toranjan/rug-icon.tsx)).
- **🔑 سه تلاش برد و فقط نگاه کردن مشکل را نشان داد.** دو نسخه‌ی اول حاشیه‌ی فرش را مستطیل تودرتوی دوم می‌کشیدند: در ۱۴۰ پیکسل فرش است و در **۲۱ پیکسلی که واقعاً رندر می‌شود** دو خطِ ۱٫۷۵ با سه واحد فاصله به هم می‌چسبند و کل نشان توپُر می‌شود. بازکردن فاصله فقط اندازه‌ای را که این اتفاق می‌افتد جابه‌جا کرد.

**۳. ~~حاله‌ی سیاه دور فرش در فایل AR.~~ ✅** — شرحش در بند ۱۴ مورد ۳٫۳ پایین.

**۴. ~~حرکت بین تب‌ها + جابه‌جایی با کشیدن انگشت.~~ ✅**
- **جهت از چیدمان مشتق شد، انتخاب نشد.** ایندکس ۰ راست‌ترین تب است، پس ایندکس بالاتر سمت *چپ* می‌نشیند. پنج‌تا را یک نوار ببینید: `[۴][۳][۲][۱][۰]`. رسیدن به ایندکس بالاتر یعنی نوار به **راست** می‌رود، پس صفحه‌ی رفتنی از راست بیرون می‌رود، آینده از چپ می‌آید، و انگشتی که این کار را می‌کند به راست می‌کشد. **صفحه دنبال انگشت می‌آید.**
- **🔑 هیچ wrapperای سُر نمی‌خورد، عمداً.** المان ترنسفورم‌شده containing block برای `fixed` می‌شود و `sticky` را به هم می‌زند — و این فروشگاه سرآیند چسبان، سینی چسبان و نوار پایینِ `fixed` دارد؛ هر سه می‌شکستند. ویوترنزیشن **اسنپ‌شات** را انیمیت می‌کند، پس هیچ‌چیز در درخت زنده ترنسفورم نمی‌شود.
- **سه چیز باید اندازه‌گیری می‌شد و هر سه اول غلط بودند:**
  1. **`startViewTransition` اصلاً صدا زده نمی‌شد** (`calls: 0`). React فقط وقتی یکی شروع می‌کند که یک `<ViewTransition>` در به‌روزرسانی شرکت داشته باشد، و تنها مرزها دو عکس فرش بودند — یعنی قواعد CSS درست بودند و روی ترنزیشنی اعمال می‌شدند که وجود نداشت.
  2. **نیمه‌ی غلط حرکت می‌کرد.** بی‌نام، React برای wrapper نام می‌سازد و بدنه‌ی صفحه گروه خودش می‌شود، که آن را از `root` **بیرون** می‌کشد؛ `root` می‌ماند کروم. سُر دادن `root` یعنی سُر دادن سرآیند و محو کردن صفحه — دقیقاً وارونه، و فقط `getAnimations()` نشانش داد.
  3. **نام‌گذاری یک عدد تنظیم‌شده را ناخواسته عوض کرد:** بدنه دیگر `--dur-page-fade` را از `root` به ارث نمی‌برد و به پیش‌فرض ۲۵۰ms مرورگر افتاد در حالی که کروم دورش هنوز ۳۸۰ می‌گرفت.
- **مورف فرش دست‌نخورده است — بررسی شد، فرض نشد:** باز کردن یک فرش همچنان `::view-transition-group(carpet-photo-…)` را با ۵۲۰ms اجرا می‌کند.
- **ژست موقعیتش را از `location.pathname` می‌خواند نه از React.** ref همگام‌شده با افکت یک رندر عقب بود و دو سوایپ پشت‌سرهم، دومی را در برابر تبی می‌سنجید که اولی ترکش کرده بود. همان درس `hasPointerCapture`.
- **نگهبان‌ها، هرکدام با آزمون:** کشیدن عمودی اسکرول است، کشیدن با ماوس انتخاب متن، کشیدن کوتاه شستِ سرگردان، و کشیدنی که داخل هر چیزِ افقی‌اسکرول شروع شود مال همان است. هیچ‌جا `preventDefault` صدا نمی‌شود، پس تا وقتی ژست مبهم است اسکرول کاملاً دست‌نخورده می‌ماند. چهار آزمون در [tab-swipe.spec.ts](frontend/e2e/tab-swipe.spec.ts).

### وضعیت بند ۱۴: هر هفت مورد بسته شد (۱۴۰۵/۰۶/۰۲)

دو کار باقی است و هر دو **دست کارفرماست**، نه کد:
- ⬜ **تأیید روی گوشی واقعی** برای مورد ۲ (قفل‌شدن پس از لاک) و نیمه‌ی روشناییِ مورد ۴. هر دو با شبیه‌سازی بسته شدند؛ نور AR و رفتار تعلیقِ صفحه را فقط دستگاه واقعی نشان می‌دهد.
- ⬜ **تصمیم اختیاری مورد ۵:** ضریب ریزکاری برای ذره‌بین. امروز یک پیکسل صفحه = ۲٫۵ پیکسل عکس، که حدِ مقیاس نمایش است نه باگ.

---

## ۱۶. فهرست سوم کارفرما (۱۴۰۵/۰۶/۰۳) — هفت مورد، و دو پی‌گیری

هفت مورد، پس از کار واقعی با محصول و با پنل. **ترتیب اجرا از روی ریسک بسته شد، نه از روی ترتیب گفته‌شدن:** اول چیزی که داده را غلط نشان می‌دهد، بعد چیزی که به تمایز محصول (AR) دست می‌زند، بعد قابلیت تازه، و آخر آرایش.

| # | کار | جنس | وضعیت |
|---|---|---|---|
| ۱ | ~~**کل کارت فرش باید باز کند، نه فقط عکس و نام**~~ | هدف لمسی | ✅ بسته شد |
| ۲ | ~~**نور AR هنوز کم است**~~ | تمایز محصول | ✅ بسته شد — نیاز به تأیید روی گوشی |
| ۳ | ~~**ویرایشگر گوشه، مرزبندی قبلی را نشان می‌دهد نه آخرین مرزبندی ادمین**~~ | صحت داده | ✅ بسته شد + آزمون |
| ۴ | ~~**هیچ‌جای سایت جست‌وجوی نام نیست**~~ | قابلیت تازه | ✅ بسته شد |
| ۵ | ~~**اقلام سفارش در پنل، عکس و لینک ندارند**~~ | کار روزمره‌ی فروشنده | ✅ بسته شد + آزمون |
| ۶ | ~~**«۴۱۱ از ۴۱۱» بین رقم‌های عدد اول فاصله دارد**~~ | تایپوگرافی | ✅ بسته شد |
| ۷ | ~~**باکس قیمت، سه‌رقم‌سه‌رقم جدا نمی‌شود**~~ | ورود داده | ✅ بسته شد |
| ۸ | ~~**دو نوار نارنجی موقع جست‌وجو**~~ | آرایش | ✅ بسته شد |
| ۹ | ~~**سایت اصلی اپ نمی‌شود، شورت‌کات می‌شود**~~ | PWA | ✅ بسته شد |

### شرح هر مورد

**۱. ~~هرجای کارت فرش را بزنی باید باز کند.~~ ✅** فقط عکس و نام لینک بودند؛ قیمت، تعداد سایز و نوار رنگ‌ها ناحیه‌ی مرده بودند.

- **راه‌حل، لینکِ کشیده است نه دیو شنونده‌ی کلیک.** کارت را در یک `<a>` پیچیدن یعنی دکمه‌ی علاقه‌مندی، کلید مقایسه و فلش‌های گالری **داخل** یک لینک قرار بگیرند — که هم نامعتبر است و هم هرکدام را برای صفحه‌کلید مبهم می‌کند. دیوی که `onClick` دارد هم به اشاره‌گر هدف می‌دهد و به صفحه‌کلید و «باز کردن در تب جدید» هیچ.
- پس `::after` روی **لینکِ نام** تمام قاب را می‌پوشاند: یک لینک، همان که صفحه‌خوان می‌خواند و کلیک وسط باید روی آن کار کند. هرچه کار خودش را دارد با `z-10` بالای پوشش می‌آید — لینک عکس (و فلش‌ها و نقطه‌ها و دو کلید گوشه‌اش) و دکمه‌ی «در خانه‌ی من ببین» که مقصد دیگری دارد.
- **اندازه‌گیری شد، فرض نشد:** `elementFromPoint` روی سه نقطه‌ی مرده‌ی قبلی حالا همان `<a>` فرش را برمی‌گرداند، و هر چهار کنترل هنوز خودشان را می‌گیرند. **مورفِ اشتراکی عکس هم بررسی شد:** ناوبری از لینکِ کشیده هم `startViewTransition` را یک بار صدا می‌زند، دقیقاً مثل لینک عکس.

**۲. ~~نور AR هنوز کم است.~~ ✅** بند ۱۴ مورد ۴ نیمه‌ی روشنایی را «منتظر تأیید روی گوشی» گذاشته بود. تأیید آمد و منفی بود.

- **🔑 نیمه‌ی اولِ آن اصلاح درست بود و کافی نبود، چون کفِ تخمین زیر یک «خنثی»ی تاریک بود.** three.js از r155 واحد نور را فیزیکی کرده و این پروژه روی r185 است. در آن واحدها یک سطح تختِ رو به بالا به اندازه‌ی `irradiance / π` از بافت خودش روشن می‌شود، و خنثیِ قبلی — همیسفر ۱٫۰ و خورشید ۰٫۶ — یعنی:

      (۱٫۰ + ۰٫۶ × ۰٫۸۷) / π ≈ ۰٫۴۹

  یعنی فرش **همیشه** با نصفِ روشناییِ عکسی که از آن بافته شده روی زمین می‌نشست، پیش از آنکه اتاق اصلاً اندازه‌گیری شود.
- خنثیِ تازه (۲٫۴ و ۰٫۸) جمعشان حدود π می‌شود و نتیجه ≈ ۰٫۹۹ — یعنی نقش، به روشناییِ عکس خودش. **بیشتر از این نه،** و دلیلش همان استدلالی است که تخمین را محدود کرد: تصویر دوربینِ پشتِ فرش را اکسپوژر خودکار عادی کرده، پس فرشِ روشن‌تر از عکس خودش تنها چیزِ سوخته‌ی کادر می‌شود.
- **عددهای تخمین حالا ضریب‌اند، نه شدت.** وقتی شدت بودند بی‌صدا یک تصمیم دوم را هم حمل می‌کردند — اینکه «خنثی» چیست — و همان تصمیم دو برابر غلط بود.
- ⬜ **این هم باید روی گوشی واقعی دیده شود.** حساب، بافت را روی روشناییِ عکس می‌نشاند؛ اینکه در اتاق واقعی چه حسی دارد را فقط دستگاه می‌گوید. (Quick Look آیفون از این تغییر بهره‌ای نمی‌برد: نورش مال خودِ سیستم است و ما جز بافت چیزی به آن نمی‌دهیم.)

**۳. ~~ویرایشگر گوشه، آخرین مرزبندیِ ادمین را نشان نمی‌دهد.~~ ✅** ادمین گوشه‌ها را اصلاح می‌کرد، فایل می‌ساخت، از فرش بیرون می‌رفت و برمی‌گشت — و همان برشِ خودکارِ قبلی سر جایش بود.

- **هیچ‌چیز ثبت نمی‌شد که ساخت با چه چیزی انجام شده،** پس `suggest_corners` یک جواب بیشتر نداشت و همان را هر بار می‌داد: «الان تشخیص بده». برای فرشی که کسی اصلاحش نکرده بود درست به نظر می‌رسید، چون تشخیص قطعی است. برای فرشِ اصلاح‌شده، اصلاح **روی صفحه** دور ریخته می‌شد در حالی که در فایل‌های روی دیسک زنده بود — و زدنِ «بازسازی» از همان صفحه، اصلاح را واقعاً هم دور می‌ریخت.
- `carpet_images.ar_corners` (+ `ar_corners_manual`) در مهاجرت ۰۰۰۴. **روی عکس، نه روی فرش:** مختصات فقط در برابر یک عکس معنا دارند، پس عوض‌کردن عکس اصلی خودبه‌خود به تشخیص برمی‌گردد، که درست است.
- **🔑 ثبت‌شونده `preview.corners` است نه آرگومان `corners`.** وقتی چیزی پاس نشده باشد این دو یکی نیستند: آن‌وقت خودِ rectify تشخیص داده و **همان** چیزی است که فایل با آن ساخته شده. ثبتِ آرگومان یعنی «خودکار» را به شکل یک غیاب ثبت کردن و صفحه دوباره حدس می‌زد.
- API حالا **دو دسته** می‌دهد: `corners` (چیزی که ویرایشگر روی آن باز می‌شود) و `detected` (تشخیصِ همین حالا، که دکمه‌ی «بازگرداندن گوشه‌های تشخیص‌داده‌شده» برمی‌گرداند). یک فیلد بودن، دلیلِ خودِ باگ بود.
- صفحه حالا **می‌گوید** مرزبندی از کجاست، و اخطارِ «تشخیص مطمئن نبود» روی برشی که آدم گذاشته دیگر نشان داده نمی‌شود.
- **دو آزمون، و هر دو روی کدِ اصلاح‌نشده قرمز شدند** ([test_ar_integration.py](backend/tests/test_ar_integration.py)) — یکی برای دستی، یکی برای خودکار.

**۴. ~~جست‌وجوی نام، با پیشنهاد زنده.~~ ✅** ذره‌بینِ هدر یک **لینک به `/carpets`** بود و کامنت بالای فایل هم همین را توجیه می‌کرد: «فروشگاه با نقش گشته می‌شود نه با نام».

- **🔑 آن توجیه درباره‌ی بازدید اول درست بود و درباره‌ی هیچ بازدید دیگری.** ضمناً `?q=` را همان اول لیست جواب می‌داد و **فیلدش هم در پنل فیلترها بود** — یعنی مشکل، نبودِ قابلیت نبود، **دیده‌نشدنش** بود.
- کامپوننت `moumensoliman/expanding-search-dock-shadcnui` از 21st، انتخابِ کارفرما از میان چهار شکل.
- **🔑 نسخه‌ی کاتالوگ فیلد را *در جریان چیدمان* باز می‌کند و این هدر جا ندارد.** اندازه‌گیری روی ۴۴۰ پیکسل: بین ذره‌بین و لبه‌ی صفحه ۳۲۴ پیکسل مانده و فیلدِ ۳۵۲ پیکسلی **۲۸ پیکسل بیرون می‌زد** — و بی‌صدا، چون سرریز به سمت شروعِ خطِ RTL مثل حالت چپ‌به‌راست `scrollWidth` را بلند نمی‌کند. حالا فیلد **بیرون از جریان** است: زیر `sm` تمام عرض نوار (چون wrapper آن‌جا `static` است و مرجع، خودِ هدرِ `sticky` می‌شود) و بالای آن، ۲۲rem که از دلِ آیکون باز می‌شود.
- ورودی یک `combobox` واقعی است: `aria-activedescendant`، جهت‌نماها، Enter روی سطرِ روشن، Escape. **`-۱` یک موقعیتِ واقعی در حلقه است** نه غیاب — گذشتن از آخرین سطر به «هیچ‌کدام» می‌رسد، که همان‌جایی است که Enter یعنی «همه‌ی نتیجه‌ها»؛ چرخیدنِ مستقیم به سطر اول، آن گزینه را از دسترس صفحه‌کلید خارج می‌کرد.
- حالت خالی هرگز «۰ نتیجه» نیست: دو راهِ ادامه می‌دهد (همه‌ی فرش‌ها، جست‌وجوی بصری).

**۵. ~~اقلام سفارش در پنل: عکس، و راه رسیدن به فرش.~~ ✅** سفارش، فهرستی از نام‌ها بود.

- نام همان چیزی است که سطر **ذخیره** می‌کند، و درست هم هست: ویرایش بعدیِ کاتالوگ نباید تاریخِ یک سفارش را بازنویسی کند. ولی کسی فرش را با نام نمی‌شناسد.
- `AdminOrderItemOut` **زیرکلاس** `OrderItemOut` است، نه سه فیلد اضافه روی آن. هیچ‌کدامشان سرّی نیستند — کاتالوگ‌اند — ولی جواب «سفارش من کجاست» نیستند.
- **همه‌شان اختیاری‌اند و این خودِ مدل داده است:** حذف یک سایز `variant_id` را `NULL` می‌کند تا سفارش خوانا بماند، و آن‌وقت واقعاً فرشی برای باز کردن نیست. چنین سطری قاب خالی می‌گیرد، نه لینکی که به جایی نمی‌رسد.
- **یک کوئری برای کل صفحه، نه یکی به‌ازای هر سطر.** و همان غنی‌سازی روی جوابِ تغییرِ وضعیت هم اجرا می‌شود، چون پنل آن جواب را مستقیم در کش می‌نویسد — بدونش، تأیید یک سفارش همه‌ی عکس‌های همان صفحه را پاک می‌کرد.
- مقصدِ لینک، **صفحه‌ی ادمینِ همان فرش** است نه صفحه‌ی فروشگاهی‌اش (انتخاب کارفرما): بقیه‌ی پنل هم آدم را در پنل نگه می‌دارد، و سایز و موجودی و عکس‌ها همان‌جا هستند.
- دو آزمون در [test_admin_panel.py](backend/tests/test_admin_panel.py)؛ اولی روی کدِ اصلاح‌نشده با `assert None == 1` قرمز شد.

**۶. ~~«۴۱۱ از ۴۱۱»: فاصله بین رقم‌های عدد اول.~~ ✅**

- **🔑 فاصله کنارِ عدد نبود، *درونش* بود: `tabular-nums`.** اندازه‌گیری در مرورگر روی Vazirmatn در ۱۶ پیکسل:

  | متن | بدون `tabular-nums` | با `tabular-nums` |
  |---|---|---|
  | `۴۱۱` | ۱۸٫۴px | ۳۱٫۶px |
  | `411` | ۲۷٫۰px | ۲۷٫۰px |

  رقم‌های لاتینِ این قلم **از قبل** جدولی‌اند، پس این ویژگی روی آن‌ها هیچ نمی‌کند و فقط رقم‌های فارسی را پُر می‌کند — حدود ۴٫۴ پیکسل به‌ازای هر گلیف. کنارِ یک `formatNumber` ساده که این تورم را نداشت، دو نیمه‌ی یک جمله از هم می‌افتادند.
- **کامنتِ قبلی در `cart-view.tsx` عکسِ این را ادعا می‌کرد** («Vazirmatn دارَدشان و عرض را ثابت نگه می‌دارند»). داشتنش درست بود، برای این رقم‌ها بودنش نه.
- برداشته شد و **چیزی جایش نیامد، چون لازم نبود:** سه‌تا از چهار شمارنده تمام سطرند و متنی که تنها در جعبه‌ی خودش است می‌تواند عرض عوض کند بی‌آنکه چیزی تکان بخورد. چهارمی «شمار از کل» بود روی یک خط و حالا عدد است با کلش در زیرنویس — **دقیقاً شکلِ کارتِ «فروش تأییدشده» کنارش،** که از اول همین کار را می‌کرد.
- **رقم‌های فارسیِ Vazirmatn واقعاً پروپورشنال‌اند** (اندازه‌گیری: `۱` = ۲۸٫۹ و `۳` = ۶۵٫۹ در ۱۰۰px)، پس این تصمیم آگاهانه است نه صرفِ حذف. سه جای دیگرِ فروشگاه که همین ویژگی را روی قیمت‌های فارسی داشتند هم پاک شدند؛ آن‌جاها متن ثابت است و ویژگی هیچ نمی‌خرید.

**۷. ~~باکس قیمت به تومان، زنده سه‌رقم‌سه‌رقم.~~ ✅** ۶۷۴۰۰۰۰۰ در یک باکس خالی، عددی است که جز با شمردنِ صفرها بررسی نمی‌شود.

- `MoneyInput` **نقطه‌گذاری را نگه می‌دارد و رقم پس می‌دهد**: صفحه همان چیزی را نشان می‌دهد که فروشگاه چاپ می‌کند (رقم فارسی، جداکننده‌ی «٬» از همان `formatNumber`)، و چیزی که به API می‌رود هنوز فقط رقمِ ASCII است.
- **مکان‌نما با «تعدادِ رقم» بازیابی می‌شود نه با اندیسِ نویسه.** تایپِ رقمِ هفتمِ یک قیمت، یک «٬» را جایی سمت چپِ مکان‌نما اضافه می‌کند؛ مکان‌نمایی که به اندیس قبلی برگردد سرِ هر مرزِ گروه یک خانه عقب می‌افتد.
- **دو دسته رقمِ غیرASCII پذیرفته می‌شود** (فارسی و عربی-هندی) چون هر دو واقعاً می‌رسند.
- **🔑 و یک باگِ نهفته این‌جا لو رفت:** مقدار اولیه‌ی باکسِ ویرایش `String(variant.price)` بود، در حالی که Postgres پانصد هزار تومان را «5.0E+5» پس می‌دهد — یعنی باکس همین را نشان می‌داد، و «فقط رقم‌ها را نگه دار» رویش می‌شد `505`. حالا از راهِ عدد خوانده می‌شود (`tomanDigits`).

### دو پی‌گیریِ همان روز

**۸. ~~دو نوار نارنجی موقع جست‌وجو.~~ ✅**

- **🔑 یک حلقه‌ی فوکوس بود که دو سرش بریده شده بود.** `globals.css` به هر چیزی که فوکوس می‌گیرد یک `outline: 2px solid var(--accent)` با آفستِ ۳ می‌دهد. آن حلقه روی `<input>` می‌نشست، و اینپوت داخل قرصی است که `overflow-hidden` دارد — پس بالا و پایینِ حلقه بریده می‌شد و **دو ضلع عمودی‌اش** وسط فیلد باقی می‌ماند.
- **قرص باید کلیپ کند** وگرنه موقع باز شدنِ عرض، اینپوت و دکمه‌ی بستن از دلش بیرون می‌زنند. پس حلقه از اینپوت برداشته شد و **خودِ قرص مرزش را تیره می‌کند** — همان کاری که پریمیتیوِ `Input` فروشگاه می‌کند. `has-[input:focus-visible]` نه `focus-within`: کلیک با ماوس هم اینپوت را فوکوس می‌کند و فیلدی که با هر کلیک دور خودش خط می‌کشد نویز است.
- **🔑 و درسی که این‌جا سفت شد: `focus-visible:outline-none` این‌جا کار نمی‌کند.** این استایل‌شیت **بی‌لایه** است و یوتیلیتی‌های Tailwind داخل `@layer`اند؛ قانونِ بی‌لایه بدون توجه به specificity برنده است. specificity فقط بین دو قانونِ بی‌لایه داوری می‌کند — برای همین اصلاح به‌صورت یک قانونِ همسایه در همان فایل نوشته شد (`[data-focus-ring="shell"]`).
- دکمه‌ی بستن حلقه‌اش را **نگه داشت** و فقط آفستش صفر شد (`[data-focus-ring="inset"]`): دیسکِ ۳۶ پیکسلی با آفست ۳ به ۴۸ پیکسل جا نیاز دارد و قرص ۴۶ است. حالا حلقه‌ی طلاییِ کامل دور دکمه می‌افتد.
- **حذف کردنِ نشانِ فوکوس گزینه نبود** — بند ۳-۵ و اولین ضدالگوی دسترس‌پذیری. نشان عوض شد، برداشته نشد.
- **پیامدِ جانبی که ثبت می‌شود چون تله است:** `focus-visible:outline-none` روی `ui/input.tsx` هم بی‌اثر است (اندازه‌گیری شد: حلقه‌ی طلایی هنوز می‌آید). آن‌جا ضرری ندارد — چیزی کلیپ نمی‌کند و حلقه‌ی دیدنی درست است — ولی کلاسی است که ادعای کاری را می‌کند که نمی‌تواند بکند.

**۹. ~~سایت اصلی به‌جای اپ، شورت‌کات می‌سازد.~~ ✅** پنل درست نصب می‌شد و فروشگاه نه.

- **آن‌چه سالم بود و بررسی شد:** منیفست با `application/manifest+json` سرو می‌شود، هر شش آیکون ۲۰۰ می‌دهند و ابعادشان واقعاً ۱۹۲ و ۵۱۲ است، `display: standalone` هست، `<link rel="manifest">` روی هر دو درست است، و `viewport` هم.
- **🔑 تنها تفاوتِ واقعی یک خط بود:** `app/admin/layout.tsx` از وقتی پنل نصب‌شدنی شد `appleWebApp: { capable: true, … }` داشت و **`app/layout.tsx` هرگز نداشت**. روی iOS منیفست تنها کافی نیست — سافاری `display: standalone` را فقط از ۱۶٫۴ و ناپایدار می‌خواند — و بدون آن متا، آیکونِ افزوده‌شده سایت را **داخل خودِ سافاری** باز می‌کند با نوار آدرس و تب‌هایش. یعنی دقیقاً «شورت‌کات».
- `statusBarStyle` فروشگاه `default` است نه `black-translucent`ِ پنل: آن مقدار از صفحه می‌خواهد **زیرِ** ساعت کشیده شود، که برای پنلِ تیره درست است و سرآیندِ فروشگاه را پشتِ نوار وضعیت می‌برد.
- **`id` و `scope` هم به منیفست فروشگاه اضافه شد** با همان استدلالی که منیفست پنل ثبت کرده و این یکی نداشت: هر دو از `start_url` مشتق می‌شوند، پس روزی که `start_url` جابه‌جا شود مرورگر آن را **اپی دیگر** می‌خواند و کنارِ نصب‌شده یکی دیگر نصب می‌کند.
- ⬜ **اگر روی اندروید یا ویندوز هم همین را دیدی بگو:** آن‌جا متای اپل بی‌اثر است و باقی شرط‌ها سالم‌اند، پس علت چیز دیگری است — محتمل‌ترینش این‌که سایت را از یک آدرسِ **غیرHTTPS** (مثلاً `http://192.168.x.x:3000`) باز کرده باشی، که کروم هرگز برایش «نصب» پیشنهاد نمی‌دهد.

---

## ۱۷. فهرست چهارم کارفرما (۱۴۰۵/۰۶/۰۴) — ویترین، هیرو، و اتاق‌ها

روز با قطعی برق شروع شد: سیستم ناگهانی خاموش شد و محیطِ کار باید از صفر دوباره برپا می‌شد. **پیش از هر کد، یک بازبینی کامل انجام شد** و نتیجه‌اش این است که تقریباً هیچ‌چیز از دست نرفت — ولی یک چیز رفته بود و تا وقتی کسی سراغش نمی‌رفت معلوم نمی‌شد.

| # | کار | جنس | وضعیت |
|---|---|---|---|
| ۰ | ~~**بازیابی محیط پس از قطعی برق**~~ | محیط | ✅ — یک قربانی: مرورگرهای Playwright |
| ۱ | ~~**ویترین یک لحظه فرشِ بعدی را نشان می‌دهد، بعد موشن اجرا می‌شود**~~ | موشن | ✅ + آزمون |
| ۲ | ~~**جای دکمه‌های ورود، و بازگشت هیروی بادبزنی**~~ | ترتیب صفحه | ✅ |
| ۳ | ~~**ویترین بعضی وقت‌ها قفل می‌شود**~~ | رفتار | ✅ + سه آزمون |
| ۴ | ~~**نوار پایین ویترین روی گوشی از لبه بیرون می‌زند**~~ | چیدمان | ✅ + آزمون |
| ۵ | ~~**۶۰٪ بالای عکس هیرو بریده می‌شود؛ فقط فرش پیداست**~~ | عکس | ✅ |
| ۶ | ~~**«برای کدام اتاق؟» پنج باکس متنیِ خالی بود**~~ | آرایش | ✅ — جلسه‌ی دوم |

### ۰. آنچه از قطعی برق سالم ماند، و آنچه نماند

- **سالم:** ریپو (بدون تغییر گم‌شده، `git fsck` بی‌خطا)، هر ۳۲ فایل حافظه، `settings.local.json` با ۶۲۳ اجازه، هوک‌های صرفه‌جویی توکن، کلید 21st، ۴۶ ترنسکریپت جلسه‌های قبل، و دیتابیس با ۱۱۶ فرش / ۴۱۱ واریانت / ۳۱۱ عکس / ۹ سفارش روی مهاجرت `0004`.
- **آخرین جلسه‌ی پیش از قطعی با «کامیت کن» تمام شده بود و کامیت هم انجام شده بود** — یعنی درست سر یک نقطه‌ی تمیز ایستاده بودیم.
- **🔑 تنها چیزی که واقعاً رفته بود، در هیچ `git status`ی دیده نمی‌شد:** پوشه‌ی `%LOCALAPPDATA%\ms-playwright` خالی بود و سوییت مرورگری اصلاً بالا نمی‌آمد. نصبِ وابستگی‌ها هم برنمی‌گرداندش، چون این باینری‌ها بیرون از `node_modules` می‌نشینند. دانلودشان از ایران **۴۰۳** می‌دهد («this service is not available in your location») و با VPN درست شد.
- درسِ کنارش: تا وقتی مرورگر نبود، برای *دیدنِ* صفحه از Edgeِ خودِ ویندوز استفاده شد (`chromium.launch({ channel: "msedge" })`) — بدون دانلود. سوییت روی کرومیومِ باندل‌شده ماند تا CI و این ماشین یک مرورگر را اجرا کنند.

### ۱. ~~پرشِ فرشِ بعدی پیش از موشن.~~ ✅

- **🔑 محو شدن و عوض شدنِ اسلاید یک آپدیت بودند.** `setFading(true)` و `setIndex(target)` در یک کامیت می‌نشستند، پس فرشِ **تازه** با opacity کامل کشیده می‌شد، بعد محو می‌شد، بعد برمی‌گشت. آنچه چشم می‌گرفت، اسلایدِ در حال ورود بود پیش از آنکه هیچ موشنی اجرا شود.
- ضمناً آن دو `setState` **داخل updaterِ `setIndex`** صدا زده می‌شدند؛ updater باید خالص باشد و React اجازه دارد دوباره اجرایش کند.
- حالا چرخش دو نیم است و تعویض در وسطِ آن: بیرون، تعویض، درون. طول کل عوض نشده. اندازه‌گیری فریم‌به‌فریم:

  | زمان | شمارنده | opacity |
  |---|---|---|
  | ۲۰۷ → ۴۸۶ms | «۱ از ۴» | ۱ → ۰ |
  | ۵۲۲ms | **«۲ از ۴»** | ۰ |
  | ۵۲۵ → ۸۰۳ms | «۲ از ۴» | ۰ → ۱ |

### ۲. ~~جای دکمه‌ها، و هیرویی که برگشت.~~ ✅

- دکمه‌های «ورود به فروشگاه» و «پیگیری سفارش» **بالای** عکس بودند و دلیلش فولد بود: عکسِ تمام‌عرض آن‌قدر بلند است که هرچه زیرش باشد از صفحه بیرون می‌رود. **با جعبه‌ای شدنِ عکس (بند ۵) این هزینه دیگر لازم نبود** و رفتند پایینِ ویترین — همان‌جا که پیش از این همه ماجرا بودند.
- `Hero10` (همان «هیرو بادبزنی» که با آمدن ویترین حذف شده بود) برگشت، **زیر ویترین**، با هر دو لینک داخل خودش.
- **دو تصمیم که در کامپوننت ثبت شد:** عنوانش `h2` است نه `h1` (یک صفحه یک سرآیندِ اول دارد؛ `h1` دوم یعنی سندی که دو بار شروع می‌شود) و اندازه‌اش `compact` است (تیتری بزرگ‌تر از تیترِ بالای صفحه، یعنی صفحه دارد نظرش را درباره‌ی نامِ خودش عوض می‌کند). برای این کار `titleAs` به کامپوننت اضافه شد.
- متنش هم عوض شد: «فرش ایرانی، با اندازه‌ی واقعی» همان ادعای `h1`ِ بالای صفحه بود. حالا دعوت است نه تکرار.

### ۳. ~~قفل‌شدن ویترین.~~ ✅ سه علت، نه یکی

- **🔑 علتِ اصلی، مکثِ hover بود — و دقیقاً برای کسی کار نمی‌کرد که داشت نگاه می‌کرد.** نشانگر که روی بخش می‌ماند (یعنی همان‌جا که موقع خواندن می‌ماند) ویترین می‌ایستاد؛ اسکرول هم راهش نمی‌انداخت، فقط بخش را از زیر نشانگر بیرون می‌برد. گزارشِ کارفرما همین را می‌گفت: «وقتی می‌بیندش pause می‌شود، وقتی پایین‌تر اسکرول می‌کند نرمال حرکت می‌کند». **برداشته شد.**
- **روی گوشی همین اشتباه از سرِ دیگر:** کروم اندروید به یک لمس، کلِ رویدادهای سازگاریِ ماوس را می‌فرستد — `mouseenter` هم بینشان — و تا چیز دیگری لمس نشود `mouseleave` نمی‌دهد. یعنی **اولین لمسِ ویترین، آخرین حرکتش بود.**
- **کلیک روی «بعدی» خودِ دکمه را فوکوس می‌کرد** و مکث «تا وقتی چیزی فوکوس است» نگه داشته می‌شد. حالا فوکوس یعنی `:focus-visible` — کِرسرِ صفحه‌کلید، نه کلیک.
- **🔑 و حلقه‌ی تایمر خودش را می‌کشت:** موقع چرخش `return` می‌کرد و منتظر می‌ماند که *عوض شدنِ اسلاید* دوباره روشنش کند — یعنی چرخشی که به هر دلیلی اتفاق نمی‌افتاد، ویترینی بود که دیگر هرگز تکان نمی‌خورد. حالا حلقه زنده می‌ماند و یک بازه بعد دوباره تلاش می‌کند.
- **نوار پیشرفت از state بیرون آمد** و شد یک custom property روی خودِ بخش: ۶۰ بار در ثانیه رندرِ دوباره‌ی عکس و متنِ کل ویترین، برای خطی به ضخامت دو پیکسل. مکث هم دیگر نوار را به ۱۰۰٪ نمی‌پراند؛ همان‌جا که بود می‌ایستد و از همان‌جا ادامه می‌دهد.
- آنچه باقی ماند: دکمه‌ی مکث، `prefers-reduced-motion`، و ویترینِ تک‌اسلایدی.

### ۴. ~~نوار پایین، روی گوشی.~~ ✅

- **🔑 `min-width: auto`.** حداقلِ خودکارِ یک آیتم فلکس، عرضِ محتوای خودش است و برچسب‌ها `nowrap` بودند — پس `flex-1` هرگز نمی‌توانست چهار دکمه را کوچک کند و **نوبت به `truncate` نمی‌رسید**. اندازه‌گیری روی ۳۷۵ پیکسل: ردیف **۴۴۳** پیکسل داخل **۳۷۵**، دکمه‌ی چهارم از **۶۸ پیکسل بیرونِ لبه** شروع می‌شد، و `overflow-hidden`ِ بخش همان‌جا وسطِ کلمه می‌بریدش. با `min-w-0` شد ۳۷۵ در ۳۷۵.
- زیرِ `sm` هر دکمه فقط **نقش** را می‌نویسد. سه فقره در ۷۸ پیکسل می‌شود «قم · ابری…»؛ و شهر هم انتخاب خوبی نبود، چون ویترین «گران‌ترین ابریشم و پشم» را می‌گیرد و در این کاتالوگ دو تا از چهارتا از قم‌اند. چیدمان پهن هر سه را نگه می‌دارد.

### ۵. ~~عکس هیرو: اتاقی که فقط کفش پیدا بود.~~ ✅

- **🔑 تمام‌عرض بودن، ارتفاع را تعیین می‌کند و ارتفاع جا نداشت.** عرضِ پنجره تقسیم بر نسبتِ عکس یعنی ۹۱۶ پیکسل روی ۱۳۶۶، در برابر ۳۶۸ پیکسلی که صفحه بالای فولد داشت. `object-cover` این فاصله را با دور ریختنِ **۵۴۸ از ۹۱۶ پیکسل از بالا** پر می‌کرد — مبل، پنجره، گلدان، هرچه ورودِ فرش را معنادار می‌کند. آنچه می‌ماند یک کف بود.
- حالا **عکسِ قاب‌شده** است در همان ستون و با همان گاترهای بقیه‌ی صفحه، با گوشه‌ی خمیده و سایه (خواسته‌ی کارفرما: «یه باکس بشه، لوکس‌تر بشه»). محدود به آن ستون، ارتفاع از نسبت درمی‌آید و عددی است که این صفحه توانش را دارد:
  - دسکتاپ ۱۳۶۶: **۱۲۱۶×۴۵۰**، نوارِ ۲٫۷:۱ از بالای مبل تا ریشه‌ی فرش، **۲۶ پیکسل بالای فولد** تمام می‌شود.
  - گوشی ۳۷۵: **۳۳۵×۴۱۶**، **صفر پیکسل برش** — کل عکسِ عمودی.
- **`object-position` درصدی است و این عمدی است:** هر دو طرفِ نسبت با ستون بزرگ و کوچک می‌شوند، پس نوارِ دیده‌شده در هر عرضِ دسکتاپی دقیقاً همان نوار می‌ماند.
- عکس تیزتر هم شد: قبلاً ۱۲۶۴ پیکسل روی ۱۳۶۶ کش می‌آمد، حالا در ۱۲۱۶ کمی جمع می‌شود.

### ۶. ~~پنج اتاق، به‌جای پنج باکسِ متنی.~~ ✅ (جلسه‌ی دوم، `dc0ae3a`)

- بخش «برای کدام اتاق؟» پنج جعبه‌ی خالی با یک کلمه وسطش بود — روی صفحه‌ای که کل استدلالش «فرش را روی کف خانه‌ی خودت ببین» است.
- شد یک **آکاردئون عکسی**؛ عکس‌ها از `data/catalog-gen` **انتخاب** شدند نه تولیدِ تازه (سهمیه‌ی تصویر خرج نشد)، و عددِ هر اتاق از `‎/facets` خوانده می‌شود نه از یک ثابتِ نوشته‌شده.

### آزمون‌ها

`frontend/e2e/showcase.spec.ts` — پنج آزمون، و **هر پنج‌تا اول روی کدِ اصلاح‌نشده قرمز شدند**: چرخشِ درست، دوام بعد از فشردنِ دکمه، **دوام زیر نشانگر**، دوام بعد از لمس، و نوارِ برچسب‌ها.

- **🔑 دو تا اولش برای دلیلِ اشتباه قرمز/سبز شدند و همین ارزشِ اثباتِ قرمزی است:** تپ روی عکسی که زیر Ken Burnsِ چهارده‌ثانیه‌ای است هرگز شرطِ «element is stable» را نمی‌دهد و آزمون با تایم‌اوتِ اکشن می‌افتاد نه با باگ؛ و فیکسچرِ سه‌فرشی روی دستگاهِ ۴۱۲پیکسلیِ سوییت جا می‌شود، پس ردیف حالا روی **۳۲۰** اندازه گرفته می‌شود.
- کلِ سوییت پس از هر دو کامیت: **۴۷ پاس، ۹ اسکیپ، خروجی صفر.**

### پی‌گیریِ همان روز: «چرا فقط بعد از لمس شروع می‌کند؟»

تأیید روی گوشی واقعی، لمس را بسته دید و **گزارش تازه‌ای آورد**: ویترین تا اولین لمس ورق نمی‌خورد.

- **روی گوشیِ شبیه‌سازی‌شده بازتولید نشد.** با اینترو و بی‌اینترو، بدون هیچ لمسی، سرِ ۷ ثانیه می‌چرخید. پس علتِ دقیقِ آن دستگاه معلوم نشد و ادعایی هم درباره‌اش ثبت نمی‌شود.
- **🔑 ولی همان گشتن یک ایرادِ واقعی با همین نشانه پیدا کرد: تایمر از لحظه‌ی mount می‌شمرد.** ویترین دو صفحه پایین‌تر از فولد است و پشتِ فیلمِ ورودی هم mount می‌شود، پس هفت‌ثانیه‌هایش را روی اتاق خالی خرج می‌کرد. اندازه‌گیری روی کدِ اصلاح‌نشده: صفحه باز شد، **هیچ اسکرول و لمسی نشد**، ۹ ثانیه بعد شمارنده از «۱ از ۳» رفته بود به «۲ از ۳».
- کسی که بعد اسکرول می‌کند پایین، **وسطِ یک بازه** می‌رسد: چند ثانیه عکسِ ثابت، و بعد چرخشی که با لمسِ بی‌حوصلگی‌اش هم‌زمان می‌شود. از دید او، لمس بود که راهش انداخت.
- حالا فقط وقتی می‌شمرد که **۲۰٪ بخش روی صفحه‌ی کاربر باشد**، و بیرون که رفت نوار به صفر برمی‌گردد — تنها جایی که عقب‌گرد درست است، چون آنجا کسی نیست. مکث‌های دیگر همچنان نوار را همان‌جا نگه می‌دارند.
- قلابِ دیدِ `compare-reveal` که دقیقاً همین کار را می‌کرد، به `lib/use-visibility-pause.ts` منتقل شد و حالا هر دو از یکی استفاده می‌کنند.
- آزمون از **هر دو سر** می‌سنجد و فقط جفتش معنا دارد: تا دیده نشده نچرخد، و تا دیده شد بی‌آنکه کسی دستش بزند بچرخد. روی کدِ اصلاح‌نشده قرمز شد. سوییتِ ویترین: **۹ پاس، ۳ اسکیپ.**

### دو چیز که این روز درباره‌ی کار کردن یاد داد

- **~~⬜ لمس روی گوشیِ واقعی~~ ✅ تأیید شد** — و یک ایراد دیگر را هم لو داد؛ پی‌گیریِ زیر.
- **🔑 دو خطِ کارِ موازی روی یک working tree.** هر دو نیمه‌ی این روز هم‌زمان روی یک پوشه نوشته شد و هر دو `page.tsx` را دست زدند. دو درس: (۱) `git stash` برای اثباتِ قرمزیِ آزمون، وقتی جلسه‌ی دیگری هم می‌نویسد، **فایل‌های او را هم چند دقیقه به عقب برمی‌گرداند** — بعد از آن، قرمزی با نوشتنِ آزمون *پیش از* اصلاح ثابت شد، نه با برگرداندن کد. (۲) وقتی تغییرهای دو نفر در یک هانک قاطی می‌شود، `git add` جوابگو نیست؛ نسخه‌ی «HEAD + سهم من» ساخته و مستقیم در ایندکس نشانده شد، بی‌آنکه فایلِ روی دیسک تکان بخورد — و هیچ rebase و merge لازم نشد.
