# نقشه‌ی راه پروژه — سامانه‌ی هوشمند خرید فرش

> سند برنامه‌ریزی و مرجع اصلی ادامه‌ی کار.
> آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۰۶ (۲۰۲۶-۰۷-۲۸) — بازتعریف فازها؛ طراحی و ساخت فروشگاه به یک فاز مستقل ارتقا یافت.

---

## ۱. هدف و ماهیت

یک **فروشگاه اینترنتی واقعی و حرفه‌ای فرش** که تجربه‌ی خرید در آن با واقعیت افزوده و بینایی ماشین تقویت شده است. کاربر باید حس کند وارد یک فروشگاه کامل و آماده شده — نه یک دموی دانشگاهی.

- سایت فروش فرش با ظاهر و تجربه‌ی کاربری در سطح محصولات تجاری روز.
- قابلیت‌های هوشمند (AR، جست‌وجوی بصری، مشاور چیدمان، تطبیق اندازه) **به‌عنوان امکانات خود فروشگاه** ارائه می‌شوند: کاربر هر فرشی را که انتخاب می‌کند می‌تواند در خانه‌اش ببیند.
- کلاینت سبک (PWA) + سرور و دیتابیس مرکزی + پنل مدیریت فروشنده.
- ددلاین: **۲۸ مرداد ۱۴۰۵ / ۲۰۲۶-۰۸-۱۹**.

## ۲. دو ستون پروژه

این پروژه روی **دو** ستون هم‌وزن می‌ایستد؛ ضعف هرکدام کل کار را پایین می‌آورد:

**ستون الف — تجربه و ظاهر فروشگاه:** طراحی بصری، معماری اطلاعات، جریان خرید، RTL فارسی، موبایل، سرعت، دسترس‌پذیری.

**ستون ب — عمق فنی و هوش:** مقیاس واقعی AR، تولید خودکار دارایی از یک عکس، جست‌وجوی بصری، درک صحنه و اندازه.

## ۳. استاندارد کیفیت

1. **مقیاس واقعی AR:** خطای ابعاد کمتر از ~۳٪ (تأیید با متر). baseline مقایسه در پایان‌نامه: نمونه‌ی تجاری بررسی‌شده که فرش «۳×۴» را ۰٫۲×۰٫۲ متر رندر می‌کند.
2. **تولید تمام‌خودکار دارایی AR از یک عکس معمولی** — بدون هیچ ابزار دستی.
3. بافت AR با وضوح بالا (حداقل 2K).
4. جست‌وجوی بصری **مقاوم روی ورودی جدید** (خط قرمز: دمو نباید بشکند) + ارزیابی کمّی.
5. **فروشگاه در سطح تجاری:** RTL کامل، موبایل‌اول، کنتراست ≥۴٫۵:۱، هدف لمسی ≥۴۴px، انیمیشن ۱۵۰–۳۰۰ms با احترام به `prefers-reduced-motion`، تصاویر WebP/AVIF با lazy-load و بدون پرش چیدمان، حالت‌های خالی/در‌حال‌بارگذاری/خطا برای همه‌ی صفحات.
6. محصول مستقر: HTTPS، پنل ادمین، احراز هویت، تست، CI.

## ۴. استک نهایی (قفل‌شده)

| لایه | فناوری |
|------|--------|
| کلاینت | React + Next.js + TypeScript + Tailwind، به‌صورت PWA |
| واقعیت افزوده | `<model-viewer>` — اندروید: WebXR و Scene Viewer (هر دو در اختیار کاربر) · iOS: Quick Look با `.usdz` |
| بک‌اند / API | Python 3.12 + FastAPI |
| دیتابیس | PostgreSQL + pgvector |
| ذخیره‌ساز فایل | عکس فرش‌ها و فایل‌های AR — پیش‌فرض: MinIO روی همان VPS |
| استقرار | Docker + دامنه + HTTPS روی VPS |
| CI | GitHub Actions (لینت + تست در هر کامیت) |

## ۵. معماری

```
┌───────────────────────────────────────────────┐
│  فروشگاه — React PWA                             │
│  خانه · لیست و فیلتر · صفحه‌ی محصول ·             │
│  جست‌وجوی بصری · مشاور چیدمان · راهنمای اندازه ·  │
│  جست‌وجوی گفتگویی · سبد · «در خانه‌ی من ببین» (AR)│
└───────────────────────┬───────────────────────┘
                        │ REST API
┌───────────────────────┴───────────────────────┐
│  بک‌اند — FastAPI                                │
│  • جست‌وجوی بصری: امبدینگ (DINOv2/CLIP) + pgvector│
│  • درک صحنه: تشخیص کف/دیوار + رنگ و سبک          │
│  • تطبیق سایز: مرجع A4 (دقیق) + تخمین عمق (سریع) │
│  • جست‌وجوی گفتگویی: مدل زبانی → فیلتر            │
│  • پردازش فرش جدید: تصحیح پرسپکتیو → glb/usdz     │
│  • سفارش‌ها و احراز هویت پنل مدیریت                │
└──────────┬──────────────────────┬───────────────┘
    ┌──────┴──────┐        ┌──────┴──────┐
    │ PostgreSQL  │        │ ذخیره‌ساز    │
    │ +pgvector   │        │ عکس/AR فایل  │
    └─────────────┘        └─────────────┘
           ▲
    ┌──────┴───────────────┐
    │ پنل مدیریت فروشنده     │
    │ افزودن/ویرایش فرش      │
    └──────────────────────┘
```

## ۶. دامنه‌ی فروشگاه

**صفحات اصلی:**
1. **خانه** — معرفی، دسته‌بندی‌ها، فرش‌های منتخب، ورودی جست‌وجوی بصری، معرفی قابلیت «در خانه‌ی من ببین».
2. **لیست محصولات** — فیلتر (اندازه، رنگ، طرح، جنس، بازه‌ی قیمت، نوع اتاق) + مرتب‌سازی + صفحه‌بندی.
3. **صفحه‌ی محصول** — گالری با زوم، مشخصات کامل، انتخاب اندازه، قیمت، **دکمه‌ی برجسته‌ی AR**، فرش‌های مشابه.
4. **جست‌وجوی بصری** — آپلود عکس یا دوربین → نتایج مشابه.
5. **مشاور چیدمان** — عکس اتاق → پیشنهاد فرش هماهنگ.
6. **راهنمای اندازه** — عکس اتاق (+ مرجع A4) → اندازه‌ی پیشنهادی.
7. **جست‌وجوی گفتگویی** — درخواست به زبان طبیعی.
8. **سبد خرید و ثبت سفارش** + علاقه‌مندی‌ها + مقایسه‌ی فرش‌ها.
9. **پنل مدیریت فروشنده** — افزودن/ویرایش فرش، مشاهده‌ی سفارش‌ها.

**پرداخت:** درگاه بانکی واقعی برای یک پروژه‌ی دانشجویی امکان‌پذیر نیست (نیاز به شخصیت حقوقی و مجوز). بنابراین جریان خرید تا **ثبت سفارش** کامل پیاده می‌شود و مرحله‌ی پرداخت شبیه‌سازی می‌گردد؛ این موضوع در پایان‌نامه شفاف ذکر می‌شود.

## ۷. برنامه‌ی داده (کاتالوگ)

- **لایه‌ی قهرمان (۵–۱۰ فرش):** فرش‌های واقعی موجود، با عکس‌برداری اختصاصی و ابعاد اندازه‌گیری‌شده. دموی زنده‌ی AR دفاع با این‌هاست.
- **لایه‌ی حجم (۵۰–۱۰۰ فرش):** دیتاست‌های عمومی فرش ایرانی (Kaggle / HuggingFace) برای پر کردن کاتالوگ و ارزیابی جست‌وجوی بصری. متادیتای اندازه/قیمت نمایشی ولی واقع‌گرایانه تولید می‌شود و در پایان‌نامه ذکر می‌گردد.

## ۸. فازها (۷ تا ۲۸ مرداد)

### فاز ۰ — پایه + اسپایک AR (۶ مرداد) ✅ انجام شد
- Python 3.12 با `uv`؛ مخزن گیت + monorepo + `.gitignore`؛ اسکلت FastAPI و Next.js؛ CI؛ docker-compose دیتابیس.
- ساخت GLB با مقیاس واقعی از تصویر + انتشار روی GitHub Pages + **تست موفق روی گوشی واقعی** (Galaxy S23 FE / Android 16).

### فاز ۱ — بک‌اند، کاتالوگ، جست‌وجوی بصری (۷–۱۰ مرداد)
- مدل داده‌ی فرش + مهاجرت‌ها + دیتابیس.
- قرارداد API کامل (کاتالوگ، فیلتر، محصول، سفارش، ادمین).
- آپلود عکس + ذخیره‌ساز.
- پایپ‌لاین عکس → امبدینگ → pgvector + **API جست‌وجوی بصری**.
- ingest دیتاست‌ها و پر کردن کاتالوگ.
- خروجی: API کامل و تست‌شده (بدون رابط زیبا).

### فاز ۲ — پایپ‌لاین خودکار AR (۱۱–۱۳ مرداد)
- تصحیح پرسپکتیو: تشخیص خودکار گوشه‌ها + امکان اصلاح دستی گوشه‌ها در ادمین (تور ایمنی).
- تولید خودکار `.glb` (بافت 2K+، ابعاد واقعی از متادیتا) و `.usdz` برای iOS.
- اتصال به فرایند افزودن محصول: فروشنده یک عکس می‌دهد، دارایی AR خودکار ساخته می‌شود.
- **تست مقیاس با متر واقعی** روی هر دو گوشی + جدول خطا برای پایان‌نامه.

### فاز ۳ — طراحی و ساخت فروشگاه ۰ تا ۱۰۰ (۱۴–۱۹ مرداد) ★ فاز اصلی فرانت
بزرگ‌ترین بلوک زمانی پروژه. ترتیب کار:

1. **سیستم طراحی (روز اول):** جهت بصری، پالت رنگ، تایپوگرافی فارسی، شبکه و فاصله‌ها، کتابخانه‌ی کامپوننت، حالت روشن/تیره. **پیش از ساخت صفحات تأیید می‌شود** تا دوباره‌کاری پیش نیاید.
2. **اسکلت و ناوبری:** هدر، منو، جست‌وجو، فوتر، ناوبری موبایل.
3. **صفحات فروشگاه:** خانه → لیست و فیلتر → صفحه‌ی محصول (با دکمه‌ی AR) → سبد و ثبت سفارش → علاقه‌مندی و مقایسه.
4. **پنل مدیریت:** رابط کاربردی‌محور برای افزودن/ویرایش فرش و مشاهده‌ی سفارش‌ها.
5. **پرداخت جزئیات:** انیمیشن‌های هدفمند، حالت‌های خالی/بارگذاری/خطا، اسکلتون، ریزتعامل‌ها.
6. **کنترل کیفیت:** ۳۷۵/۷۶۸/۱۰۲۴/۱۴۴۰ پیکسل، دسترس‌پذیری، سرعت بارگذاری، RTL بی‌عیب.

> نکته‌ی اجرایی: در این فاز از منابع طراحی و ابزارهای تولید کامپوننت و ایده‌های خود کارفرما استفاده می‌شود. تحقیق اولیه انجام شده؛ نقطه‌ی شروع پیشنهادی: پس‌زمینه‌ی خنثای گرم با تأکید طلایی (رنگ‌های خود فرش را برجسته می‌کند)، فونت فارسی حرفه‌ای (گزینه‌ها: وزیرمتن / استعداد / Noto Sans Arabic)، و استفاده‌ی محدود از افکت شیشه‌ای فقط در نوار بالا و پوشش AR (به‌دلیل هزینه‌ی کارایی روی موبایل).

### فاز ۴ — قابلیت‌های هوشمند در دل فروشگاه (۲۰–۲۳ مرداد)
به ترتیب اولویت (اگر زمان فشرده شد، از پایین حذف می‌شود):
1. جست‌وجوی بصری (رابط کامل: آپلود/دوربین → نتایج).
2. تطبیق اندازه (مرجع A4 + تخمین عمق).
3. مشاور چیدمان (سگمنتیشن کف/دیوار + پالت رنگ + سبک).
4. جست‌وجوی گفتگویی (مدل زبانی → فیلتر ساخت‌یافته).

### فاز ۵ — امنیت، تست، استقرار، ارزیابی (۲۴–۲۶ مرداد)
- احراز هویت، اعتبارسنجی ورودی، rate limit، HTTPS، بازبینی حفره‌ها.
- تست‌های واحد و یکپارچه.
- استقرار روی VPS + دامنه.
- نوت‌بوک‌های ارزیابی: Precision@k جست‌وجوی بصری، جدول خطای سایز، جدول خطای مقیاس AR در برابر baseline تجاری.

### فاز ۶ — پایان‌نامه و دفاع (۲۷–۲۸ مرداد)
- پیش‌نویس هر فصل از پایان همان فاز نوشته می‌شود؛ اینجا فقط نهایی‌سازی.
- اسلاید دفاع + دو تمرین کامل دمو.

## ۹. روش اجرا

- هر فاز با یک خروجی **قابل اجرا و کامیت‌شده** تمام می‌شود؛ کار نیمه‌کاره به فاز بعد منتقل نمی‌شود.
- قرارداد API و مدل داده پیش از ساخت رابط کاربری نوشته می‌شود.
- سیستم طراحی پیش از ساخت صفحات تأیید می‌شود.
- در مرز هر فاز: مرور کد + اجرای کامل تست‌ها + به‌روزرسانی همین سند.
- ریسک‌های بزرگ زودتر از موعد تست می‌شوند (نمونه: اسپایک AR در روز اول).

## ۱۰. برنامه‌ی روز دفاع (افزونگی — دمو نباید بمیرد)

- مسیر ۱: نسخه‌ی مستقر روی VPS + دامنه (اصلی).
- مسیر ۲: صفحه‌ی AR روی GitHub Pages (مستقل از سرور، از قبل کش‌شده روی گوشی).
- مسیر ۳: اجرای کامل لوکال روی لپ‌تاپ + دسترسی گوشی از شبکه‌ی محلی + QR.
- دو گوشی از قبل تست‌شده (اندروید WebXR + آیفون Quick Look) با فرش‌های قهرمان.
- تمرین کامل دمو حداقل ۲ بار قبل از دفاع.

## ۱۱. اصول کیفیت (در همه‌ی فازها)

- 🔒 هیچ رمز/کلیدی در گیت (`.env` مخفی).
- 📦 داده‌ی سنگین (عکس، glb/usdz، وزن مدل) خارج از گیت.
- ✅ تست خودکار در هر کامیت (CI).
- 🧱 کد ماژولار و تمیز (نوت‌بوک فقط برای آزمایش و نمودار گزارش).
- 🛡️ سیستم روی ورودی جدید نشکند (خط قرمز اصلی).

## ۱۲. کارهای فیزیکی و زیرساخت

- [ ] عکس/فایل اسکن ۴–۵ فرش + ابعاد واقعی هر فرش با متر.
- [ ] تصمیم نصب Docker Desktop (پیش از فاز ۱).
- [ ] فاز ۵: تهیه‌ی VPS (ترجیحاً خارج از ایران برای دسترسی به API مدل زبانی) + دامنه.
- [ ] فاز ۴ (در صورت مدل زبانی ابری): کلید API.

## ۱۳. کارهای باز

- [ ] جهت بصری و سیستم طراحی — تصمیم در ابتدای فاز ۳.
- [ ] تجربه‌ی تعامل AR: نگه‌داشتن `<model-viewer>` در برابر لایه‌ی جای‌گذاری اختصاصی — بازبینی در پایان فاز ۲.
- [ ] انتخاب منبع تقویتی دیتا در صورت نیاز (فاز ۱).
- [ ] مدل زبانی جست‌وجوی گفتگویی: ابری یا محلی (فاز ۴).
