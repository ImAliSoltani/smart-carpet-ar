# نقشه‌ی راه پروژه — سامانه‌ی هوشمند خرید فرش

> سند برنامه‌ریزی و مرجع اصلی ادامه‌ی کار (حتی در چت جدید).
> آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۰۶ (۲۰۲۶-۰۷-۲۸) — بازبینی کامل پس از تحلیل نمونه‌ی تجاری موجود.

---

## ۱. هدف و ماهیت

یک **محصول واقعی و حرفه‌ای** (نه دموی لوکال): سامانه‌ی خرید فرش با واقعیت افزوده + بینایی ماشین، که یک فروشگاه واقعی بتواند مستقیماً به مشتریانش بدهد.
- کلاینت سبک (PWA) + سرور و دیتابیس مرکزی + پنل مدیریت فروشنده.
- ددلاین: **۲۸ مرداد ۱۴۰۵ / ۲۰۲۶-۰۸-۱۹**.

## ۲. استاندارد کیفیت — تعریف «بهترین کیفیت ممکن»

بررسی فنی نمونه‌ی تجاری موجود (vajaar.ir، با اجازه‌ی صاحبش) در ۶ مرداد نشان داد: دارایی‌های AR آن دستی (Blender/Adobe/three.js) ساخته شده‌اند و مقیاس واقعی ندارند (فرشِ «۳×۴» به‌صورت ۰٫۲×۰٫۲ متر رندر می‌شود). استاندارد ما دقیقاً همین دو ضعف را هدف می‌گیرد:

1. **مقیاس واقعی:** خطای ابعاد فرش در AR کمتر از ~۳٪ (تأیید با متر در دنیای واقعی). baseline مقایسه در پایان‌نامه: نمونه‌ی تجاری.
2. **تولید تمام‌خودکار دارایی AR از یک عکس معمولی** — بدون هیچ ابزار دستی.
3. بافت AR با وضوح بالا (حداقل 2K)، نه عکس کوچک.
4. جست‌وجوی بصری **مقاوم روی ورودی جدید** (خط قرمز: دموی استاد نباید بشکند) + ارزیابی کمّی.
5. UI فارسی RTL در سطح محصول تجاری.
6. محصول مستقر: HTTPS، پنل ادمین، احراز هویت، تست، CI.

**تصمیم کاربر (۶ مرداد):** هیچ دارایی/داده‌ای از vajaar.ir برداشته **نمی‌شود** — فقط به‌عنوان baseline مقایسه در فصل ارزیابی استفاده می‌شود.

## ۳. استک نهایی (قفل‌شده)

| لایه | فناوری |
|------|--------|
| کلاینت | React + Next.js + TypeScript، به‌صورت PWA (قابل نصب، ولی در اصل یک لینک) |
| واقعیت افزوده | `<model-viewer>` — اندروید: **WebXR در کروم (مسیر اصلی)** + Scene Viewer فقط fallback (در ۲۰۲۵–۲۰۲۶ باگ‌دار شده) · iOS: Quick Look با `.usdz` |
| بک‌اند / API | Python 3.12 + FastAPI |
| دیتابیس | PostgreSQL + pgvector |
| ذخیره‌ساز فایل | عکس فرش‌ها و فایل‌های AR (`.glb`/`.usdz`) — پیش‌فرض: MinIO روی همان VPS |
| استقرار | Docker + دامنه + HTTPS روی VPS |
| CI | GitHub Actions (لینت + تست در هر کامیت) |

## ۴. معماری

```
┌───────────────────────────────────────────────┐
│  کلاینت — React PWA (سبک)                        │
│  کاتالوگ · جست‌وجوی بصری · مغز چیدمان ·          │
│  جست‌وجوی گفتگویی · دکمه‌ی «در اتاق من ببین» (AR) │
└───────────────────────┬───────────────────────┘
                        │ REST API
┌───────────────────────┴───────────────────────┐
│  بک‌اند — FastAPI                                │
│  • جست‌وجوی بصری: امبدینگ (DINOv2/CLIP) + pgvector│
│  • درک صحنه: تشخیص کف/دیوار + رنگ و سبک          │
│  • تطبیق سایز: مرجع A4 (دقیق) + تخمین عمق (سریع) │
│  • جست‌وجوی گفتگویی: مدل زبانی → فیلتر            │
│  • پردازش فرش جدید: تصحیح پرسپکتیو → glb/usdz     │
│  • احراز هویت پنل مدیریت                          │
└──────────┬──────────────────────┬───────────────┘
    ┌──────┴──────┐        ┌──────┴──────┐
    │ PostgreSQL  │        │ ذخیره‌ساز    │
    │ +pgvector   │        │ عکس/AR فایل  │
    └─────────────┘        └─────────────┘
           ▲
    ┌──────┴───────────────┐
    │ پنل مدیریت فروشنده     │
    │ افزودن/ویرایش فرش      │
    └──────────────────────┘
```

**قابلیت‌های AI:**
1. جست‌وجوی بصری — عکس فرش/اتاق → شبیه‌ترین فرش‌های انبار.
2. مغز چیدمان — تحلیل رنگ و سبک اتاق → پیشنهاد فرش هماهنگ.
3. تطبیق سایز — اندازه‌گیری کف از عکس: حالت دقیق با مرجع A4 (هوموگرافی کلاسیک) + حالت تخمینی با مدل عمق؛ مقایسه‌ی خطای دو حالت در پایان‌نامه.
4. جست‌وجوی گفتگویی — درخواست به زبان طبیعی → نتایج فیلترشده.

**نکته‌ی طراحی تأییدشده:** فرش = مستطیل تختِ بافت‌دار با ابعاد واقعی (نه مدل ۳D پیچیده). تحلیل نمونه‌ی تجاری نشان داد محصول واقعی هم همین کار را می‌کند — ما همان را خودکار و دقیق انجام می‌دهیم.

## ۵. برنامه‌ی داده (کاتالوگ)

- **لایه‌ی قهرمان (۵–۱۰ فرش):** فرش‌های واقعی خود کاربر (اسکن/عکس باکیفیت از خانه). دموی زنده‌ی AR دفاع با این‌هاست.
- **لایه‌ی حجم (۵۰–۱۰۰ فرش):** دیتاست‌های عمومی فرش ایرانی (Kaggle: «Iranian Carpet Dataset: Lechak-Toranj & Afshan»، «Persian RUGs and Carpets»؛ HF: rugs-1.9k) — برای پر کردن کاتالوگ و ارزیابی جست‌وجوی بصری. متادیتای اندازه/قیمت به‌صورت نمایشی ولی واقع‌گرایانه تولید می‌شود و در پایان‌نامه شفاف ذکر می‌گردد.
- **گزینه‌ی تقویتی (تصمیم در فاز ۱):** عکس‌های high-res از فروشگاه‌های آنلاین فرش با ذکر منبع، اگر کیفیت دیتاست‌ها کافی نبود.
- **از vajaar.ir هیچ‌چیز برداشته نمی‌شود.**

## ۶. فازها (۶ تا ۲۸ مرداد)

### فاز ۰ — پایه + اسپایک AR (۶–۷ مرداد) — مجری: Fable
- نصب Python 3.12 با `uv` (پایتون ۳٫۱۴ سیستم دست‌نخورده).
- git init + مخزن GitHub + ساختار monorepo (`frontend/` `backend/` `infra/`) + `.gitignore` (بدون secret و داده‌ی سنگین).
- Docker: Postgres + pgvector. اسکلت FastAPI و Next.js. CI پایه.
- **اسپایک AR (مهم‌ترین بخش):** ساخت اولین `.glb` مقیاس‌دار از عکس یکی از فرش‌های خود کاربر + تست WebXR روی گوشی اندروید کاربر + ساخت `.usdz` و تست Quick Look روی آیفون. ریسک اصلی پروژه همین روز اول کشته می‌شود.
- نیازمند حضور کاربر: گوشی‌ها + عکس/فایل فرش‌ها.

### فاز ۱ — کاتالوگ + ادمین + جست‌وجوی بصری (۸–۱۳ مرداد)
- Fable: مدل داده + مهاجرت‌ها، قرارداد API، پایپ‌لاین عکس→امبدینگ→pgvector، منطق و تنظیم جست‌وجوی بصری، ingest دیتاست‌ها.
- Opus (از روی اسپک Fable): صفحات کاتالوگ و جزئیات محصول، فرم‌های CRUD پنل ادمین، آپلود عکس.
- خروجی: جست‌وجوی بصری کارکننده روی **عکس جدید**.

### فاز ۲ — پایپ‌لاین خودکار AR (۱۴–۱۷ مرداد) — مجری: Fable
- تصحیح پرسپکتیو: تشخیص خودکار گوشه‌ها + ویرایش دستی گوشه‌ها در ادمین (تور ایمنی).
- تولید خودکار glb (بافت 2K+، ابعاد واقعی از متادیتا) و usdz.
- ادغام کامل «در اتاق من ببین» + **تست مقیاس با متر واقعی** (جدول خطا برای پایان‌نامه).

### فاز ۳ — لایه‌ی هوشمندی + صیقل (۱۸–۲۲ مرداد)
- Fable: تطبیق سایز (A4 + عمق)، مغز چیدمان (سگمنتیشن کف/دیوار + پالت رنگ + سبک).
- Opus: جست‌وجوی گفتگویی (LLM → فیلتر ساخت‌یافته)، صیقل کامل UI فارسی RTL.

### فاز ۴ — امنیت، تست، استقرار، ارزیابی (۲۳–۲۵ مرداد)
- Fable: احراز هویت، اعتبارسنجی، rate limit، HTTPS، استقرار VPS + دامنه، رفع حفره‌ها.
- Opus: تست‌های واحد و یکپارچه از روی اسپک.
- نوت‌بوک‌های ارزیابی: Precision@k جست‌وجوی بصری، جدول خطای سایز، جدول خطای مقیاس AR (در برابر baseline تجاری).

### فاز ۵ — پایان‌نامه و دفاع (۲۶–۲۷ مرداد + ۲۸ بافر)
- پیش‌نویس هر فصل از پایان همان فاز نوشته می‌شود (Fable: ساختار و فصل‌های فنی؛ Opus: گسترش متن فارسی).
- اینجا: نهایی‌سازی، اسلاید دفاع، دو تمرین کامل دمو.

## ۷. تقسیم کار Fable / Opus (صرفه‌جویی اعتبار)

- **Fable (کم ولی متمرکز):** معماری، اسکیما/قرارداد API، امبدینگ و تنظیم جست‌وجو، کل فاز ۲، سایز/چیدمان، امنیت/استقرار، دیباگ‌های سخت، ساختار پایان‌نامه، مرور دیف‌های Opus در مرز فازها.
- **Opus (حجم کار):** UI از روی اسپک، CRUD ادمین، جست‌وجوی گفتگویی، تست‌نویسی، صیقل ظاهری، متن فارسی فصل‌ها.
- **مکانیزم:** هر بسته‌ی Opus-آماده → Fable فایل `HANDOFF.md` می‌نویسد؛ کاربر سشن جدید با مدل Opus باز می‌کند: «HANDOFF.md را بخوان و اجرا کن».

## ۸. برنامه‌ی روز دفاع (افزونگی — دمو نباید بمیرد)

- مسیر ۱: نسخه‌ی مستقر روی VPS + دامنه (اصلی).
- مسیر ۲: اجرای کامل لوکال روی لپ‌تاپ + دسترسی گوشی از طریق تونل/شبکه‌ی محلی + QR (اگر اینترنت سالن قطع شد).
- دو گوشی از قبل تست‌شده (اندروید WebXR + آیفون Quick Look) با فرش‌های قهرمان.
- تمرین کامل دمو حداقل ۲ بار قبل از دفاع.

## ۹. اصول کیفیت (در همه‌ی فازها)

- 🔒 هیچ رمز/کلیدی در گیت (`.env` مخفی).
- 📦 داده‌ی سنگین (عکس، glb/usdz، وزن مدل) خارج از گیت.
- ✅ تست خودکار در هر کامیت (CI).
- 🧱 کد ماژولار و تمیز (نوت‌بوک فقط برای آزمایش و نمودار گزارش).
- 🛡️ سیستم روی ورودی جدید نشکند (خط قرمز اصلی).

## ۱۰. کارهای فیزیکی کاربر و زیرساخت

**کاربر انجام می‌دهد:**
- [ ] اعلام مدل دقیق گوشی اندروید (برای چک پشتیبانی ARCore) + تأیید در دسترس بودن آیفون.
- [ ] گذاشتن عکس/فایل اسکن ۴–۵ فرش خانه در یک پوشه و اعلام مسیر آن + ابعاد واقعی هر فرش با متر.
- [ ] اعلام نام کاربری GitHub (برای ساخت مخزن).
- [ ] فاز ۴: تهیه‌ی VPS (ترجیحاً خارج از ایران برای دسترسی به API مدل زبانی) + دامنه.
- [ ] فاز ۳ (در صورت LLM ابری): کلید API.

## ۱۱. کارهای باز

- [ ] انتخاب منبع تقویتی دیتا در صورت نیاز (فاز ۱).
- [ ] LLM جست‌وجوی گفتگویی: ابری یا محلی (فاز ۳).
- [ ] تأیید نهایی MinIO به‌عنوان ذخیره‌ساز (فاز ۴).
