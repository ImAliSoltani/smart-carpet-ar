# Origin of the API and of the files it stores. Development runs the backend on
# its own port; production puts both behind one host and sets this to it.
# Left unset, the code falls back to this same value.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
