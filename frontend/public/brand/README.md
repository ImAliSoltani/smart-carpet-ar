# دارایی‌های برند

فایل‌های این پوشه با **import ثابت** خوانده می‌شوند (`src/app/page.tsx`)، نه با مسیر رشته‌ای. دلیلش: `next/image` یک import ثابت را با هشِ محتوایش آدرس می‌دهد، پس عوض کردن عکس آدرسش را هم عوض می‌کند و هیچ مرورگری نسخه‌ی دیروز را سرو نمی‌کند — همان چیزی که وقتی نام‌ها ثابت رشته‌ای بودند اتفاق افتاد و عکس قدیمی از کش می‌آمد. روال جایگزینی همان تعویض یک فایل می‌ماند، طبق تصمیم نقشه‌ی راه: «جایگزینی نهایی باید تعویض یک فایل و اجرای یک دستور باشد، نه گشتن در صفحه‌ها.»

| فایل | کجا | نسبت | وضعیت |
|---|---|---|---|
| `hero-1.webp` | کارت راستِ بادبزن هیرو (کج، پشت) | ۴:۵ | ✅ نهایی |
| `hero-2.webp` | کارت وسط (بزرگ‌تر، جلو) | ۴:۵ | ✅ نهایی |
| `hero-3.webp` | کارت چپ (کج، پشت) | ۴:۵ | ✅ نهایی |
| `room-wide-before.webp`، `room-wide-after.webp` | هیروی صفحه‌ی خانه، از ۱۰۲۴ پیکسل به بالا | ۳:۲ | ✅ نهایی |
| `room-tall-before.webp`، `room-tall-after.webp` | همان هیرو، زیر ۱۰۲۴ پیکسل | ۴:۵ | ✅ نهایی |
| `room-card-*.webp` (پنج فایل) | بخش «برای کدام اتاق؟» صفحه‌ی خانه | ۲:۳ | ✅ نهایی |
| `icon-192.png`، `icon-512.png`، `icon-maskable-512.png` | منیفست PWA | ۱:۱ | 🟡 جای‌نگهدار — **ساخته‌شده، دستی ویرایش نشود** |

## عکس‌های اتاق (هیرو)

چهار فایل `room-*` دو جفت **قبل/بعد**اند: یک اتاق بدون فرش و همان اتاق با فرش. `home-hero.tsx` آن‌ها را دو طرف یک اسلایدر مقایسه می‌گذارد.

**قاعده‌ی اصلی: دو عکس هر جفت باید یک عکس باشند که فقط کفش فرق کرده.** جدا تولید کردنشان کار نمی‌کند — مبل و پنجره چند پیکسل جابه‌جا می‌شوند و وسط کشیدن دستگیره کل اتاق می‌پرد. روال درست: اول اتاق خالی تولید می‌شود، بعد **همان فایل** به ابزار داده می‌شود تا فرش را رویش بگذارد.

پیش از جایگزینی، این‌ها سنجیده شد و باید دوباره سنجیده شود:

- **اختلاف بیرون از ناحیه‌ی فرش باید نزدیک صفر باشد.** سنجه‌ی ما: میانگین اختلاف پیکسلی در نیمه‌ی بالایی زیر ۳ (اندازه‌گیری‌شده: ۲٫۰ و ۲٫۳). هر شکلِ روشنی در نقشه‌ی تفاوت که فرش نباشد، یعنی چیزی جابه‌جا شده.
- **هیچ چیز روی فرش نایستد.** میز و مبل و گلدان همه روی کف خالی بمانند، وگرنه ابزار مجبور است آن‌ها را هم دوباره بکشد و همان جابه‌جایی بالا اتفاق می‌افتد.
- **فایل «before» باید خروجی اصلی باشد**، نه دانلود دوباره؛ انکود دوباره رنگ و نویزش را با «after» فرق می‌دهد و مرزِ اسلایدر مثل یک خط دیده می‌شود.
- **لبه‌ی نزدیک فرش و ریشه‌اش باید داخل کادر باشد.** کادر از *بالا* بریده می‌شود (`object-bottom`)، پس دیوار و سقف قابل چشم‌پوشی‌اند و فرش نه.

نسخه‌ی PNG اصلی کنارشان می‌ماند. تبدیل، همان دستور انتهای این سند با کیفیت ۸۶.

## عکس‌های کارت اتاق

پنج فایل `room-card-*` پنل‌های آکاردئون «برای کدام اتاق؟»اند. **هیچ‌کدام تولید تازه نیستند**: هر فرش کاتالوگ یک عکس `__room` دارد که پرامپتش از روی *اتاق اول همان فرش* نوشته شده (`backend/scripts/catalog_profiles.py`)، پس برای هر پنج اتاق از قبل عکس واقعی وجود داشت. انتخاب از میان همان‌ها، از `data/catalog-gen/`:

| فایل | از فرش |
|---|---|
| `room-card-living-room.webp` | `kashan-1200-lachak-sormei` |
| `room-card-bedroom.webp` | `kerman-golfarang-kerem` |
| `room-card-dining-room.webp` | `arak-sultanabad-golfarang` |
| `room-card-office.webp` | `hamedan-hendesi-ghermez` |
| `room-card-kids-room.webp` | `kashan-kodak-firoozei` |

چرا این پنج‌تا از میان چهل‌تا:

- **اتاق باید بدون برچسب خوانده شود.** پنل جمع‌شده حدود ۱۸۰ پیکسل عرض دارد؛ عکسی که فقط یک کف پارکت و یک فرش نشان می‌دهد در آن عرض هر اتاقی می‌تواند باشد. هر پنج‌تا یک شیء دارند که اتاق را لو می‌دهد: تخت، میز غذاخوری، میز کار و قفسه، تخت کودک و میز کوچک.
- **مجموعه باید یک خانه به نظر برسد.** از هر اتاق، گرم‌ترین گزینه انتخاب شد؛ کاندیداهای با نور سرد و دیوار سفید کنار این چهارتا مثل عکس یک آپارتمان دیگر می‌افتادند.
- **فرش باید وسط کادر باشد.** برش پنل از دو طرف می‌برد، پس فرشی که به لبه‌ی عکس چسبیده در حالت جمع‌شده از کادر بیرون می‌ماند.

جایگزینی: یک فایل `__room` دیگر را با همین نام اینجا بگذارید (کیفیت ۸۲، همان دستور انتهای این سند). اگر فرشِ عکس عوض شد، **متن `alt` در `home-rooms.tsx` هم باید عوض شود** — آن‌جا رنگ و اثاث همین عکس‌ها توصیف شده است.

## آیکون‌ها

سه فایل `icon-*.png` این پوشه به‌علاوه‌ی `src/app/icon.png` و `src/app/apple-icon.png` **خروجی اسکریپت‌اند**، نه دارایی دستی. هر ویرایشی رویشان با اجرای بعدی پاک می‌شود.

```
uv run python ../scripts/build_brand_icons.py      # از پوشه‌ی backend
```

فعلاً نشان را خود اسکریپت می‌کشد: یک ترنج با لبه‌ی کنگره‌دار، دو سرترنج، و یک مغز طلایی. سنجه‌ی پذیرشِ سند برند ۲۴ پیکسل است و همان شکلِ کشیده‌شده را تعیین کرده — کنگره‌ی کم‌عمق و جزئیات ریز در ۲۴ پیکسل ناپدید می‌شوند.

**نام برند داخل آیکون نیست و نباید برود.** سند برند (بند ۴) می‌گوید نام فارسی هرگز از ابزار تصویری خواسته نشود؛ ضمناً Pillow روی این ماشین `raqm` ندارد، یعنی اصلاً نمی‌تواند خط فارسی را به هم بچسباند و حروف جدا از هم درمی‌آیند.

### جایگزینی با لوگوی واقعی

طبق قاعده‌ی نقشه‌ی راه («تعویض یک فایل و اجرای یک دستور»):

1. لوگوی نهایی را **مربع، با پس‌زمینه‌ی شفاف (RGBA)** در `assets/brand/mark.png` بگذارید.
2. همان دستور بالا را اجرا کنید.

هر پنج خروجی از همان یک فایل ساخته می‌شوند. هیچ کد دیگری — نه منیفست، نه چیدمان، نه هیچ صفحه‌ای — عوض نمی‌شود.

کارت وسط بزرگ‌تر است و کامل دیده می‌شود؛ دو کارت کناری زیر آن می‌روند و لبه‌ی داخلی‌شان پوشیده می‌ماند. **بهترین عکس را `hero-2` بگذارید.**

## وضعیت

هر سه عکس تولیدشده و نهایی‌اند (۱۴۰۵/۰۵/۱۴): یک اتاق، یک نور، یک نیمکت — مجموعه یک صحنه است نه سه عکس. نسخه‌ی PNG اصلی هم کنارشان می‌ماند.

## جایگزینی

فایل نهایی را با همین دو نام اینجا بگذارید. هیچ کد دیگری عوض نمی‌شود.

- نسبت ۴:۵ را نگه دارید، وگرنه `object-cover` لبه‌ها را می‌برد.
- ضلع بلند حداقل ۱۶۰۰ پیکسل.
- **هر سه از یک نور و یک فضا باشند.** بادبزن سه عکس را کنار هم می‌چیند؛ سه عکس با سه نور متفاوت، سه عکس می‌ماند و یک صحنه نمی‌شود.
- **هیچ متنی داخل تصویر نباشد** (سند برند، بند ۷): متن‌های سایت با قلم واقعی روی صفحه نوشته می‌شوند، برای انتخاب‌پذیری و دسترس‌پذیری و سئو.
- پالت گرم و خنثی بماند تا کنار زمینه‌ی `#FAFAFA` ناهماهنگ نشود.

تبدیل به WebP، اگر ورودی JPEG بود:

```
uv run python -c "from PIL import Image; im=Image.open('hero-wide.jpg').convert('RGB'); im.thumbnail((1600,1600)); im.save('hero-wide.webp','WEBP',quality=86,method=6)"
```
